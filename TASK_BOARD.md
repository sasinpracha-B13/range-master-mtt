# Task Board — Range Master MTT

> Active workstream tracker. Updated by Orchestrator + subagents (each role updates their own rows).
> Last updated: 2026-06-18 (v4.4.1C Module 5 Straight-Blocker Mix — +1 scenario (509 → 510, M5 33), both token gaps CLOSED, runtime-verified, NOT routed. Next: v4.4.2 runtime wire.).

---

## Active Epic

**v4.0.2 — Post-flop Module 1: Board Texture Trainer UI** (parent epic v4.0.x — Postflop Foundation)

First visible postflop UI surface. Consumes `App.postflop` namespace shipped in v4.0.1. Delivers: module entry point + question card + board cards + answer choices + multi-tier scoring + multi-section feedback. Mobile-first (375px). Reuses existing drill chrome where possible.

---

## Current Status

🟢 **v4.4.1C Module 5 Straight-Blocker Mix (PRODUCTION DATA + CACHE BUMP; 509 → 510; NO RUNTIME ROUTE)** — +1 scenario ships the straight-blocker lesson honestly as `mixed_indifference_river`: `Th 9h` (second pair) on `Jc 9d 8s / 4h / 2c` vs medium, reason_choice diff 4; best=mixed, acceptable=[bluff_catch_river], featured trap `blocker_bluff_catch_river` in bad, critical=[]. Teaches BOTH asymmetry roots (magnitude: Ah=100% of nut flush vs T=1-of-4≈25%; sidedness: the T cuts QT/T7 value AND KT/AT busted open-enders → two-sided nudge → mix). Implementer-owned derivation passed all 5 checks (QT/T7 only straights, both need a ten; hero no straight; rainbow flop = zero flush contamination; genuine mix; T9s pure BB flat, perfect line coherence). Brief's "gutshot" mislabel → open-ender (corrected); Q9s contrast refined (both mix — no single straight-card flips a verdict). **Gaps CLOSED:** `domination_river_fold` = distractor-only by design (in bad-list of 3 shipped scenarios; never author best=fold); straight-blocker = taught via mixed + curriculum note (asymmetry copy → `river_blocker_defense` concept at v4.4.2). NEW builder + per-sprint auditor + add-not-replace migration. **Audits: seed 1/0/0; production 510/0/0 both phases (M5 33 approved, M1–M4 unchanged). Runtime smoke-tested: 510/510, new scenario present, 0 console errors.** Cache bump 4.4.1B→4.4.1C. New doc `docs/specs/postflop-v4.4.1C-module5-straight-blocker-mix.md`; snapshot `GPT AUDIT/v4.4.1C/`. **Backlog: V1–V3 + full M4 review. Next: v4.4.2 M5 runtime wire.**

🟢 **v4.4.1B Module 5 Correctness Hotfix (PRODUCTION DATA + CACHE BUMP; COUNT UNCHANGED 509; NO RUNTIME ROUTE)** — consultant (advisory AI) review proposed fixes; implementer verified, applied, and **corrected 2 combinatorial errors in the consultant's reasoning**. **9 M5 scenarios patched, none added/removed.** FIX 1: critical→[] on 6 one-pair fold/call (4 listed + 2 the consultant missed; over-fold/over-call = bad, not severe punt). FIX 2: AhQc/AhJc monotonicity on Ad8s5c2hKd overbet (was reversed) → AhQc mixed→call, AhJc call→mixed; corrected the false "Ah blocks AK" claim (Ah only reduces AA; AhQc edge = kicker). FIX 3: #12 Ah9c large→medium; corrected false "single heart flush" (3-heart board needs 2; Ah = weak nut-blocker). VERIFY V1–V3 left UNCHANGED (chart/solver-gated): AKo-flat realism, A6o-flat, M4 AQo turn spot. NEW idempotent hotfix tool. **Production 509/0/0 PASS; 0 one-pair fold/call carry critical; arrays serialize correctly; runtime smoke-tested (ready=true, AhQc=call, AhJc=mixed, Ah9c=medium, 0 console errors).** Cache bump 4.4.1A→4.4.1B. **No scenarios added, no runtime/UI change.** New doc `docs/specs/postflop-v4.4.1B-module5-correctness-hotfix.md`; snapshot `GPT AUDIT/v4.4.1B/`. **Backlog: domination_river_fold (0) + distinct blocker_bluff_catch (1) + V1–V3 + full M4 review.** **Next: v4.4.1C expansion / v4.4.2 runtime wire.**

🟢 **v4.4.1A Module 5 Expansion (PRODUCTION DATA + CACHE BUMP; NO RUNTIME ROUTE)** — gap-first expansion **24 → 32** (production **501 → 509**), all M5 `approved`. **8 scenarios, quality-over-count, every spot strategically reviewed.** 2 new boards (A-high dry brick; flush-completing). **Gap-fills:** pot_odds_river_call 0→1, bluff_raise_river 1→2, thin_value role 1→2, mdf_defense_river 2→3, mixed_indifference_river 2→3, range_disadvantage_river_fold 2→3. **Anchors:** A.3 over-fold-trap call (AQ must call pot); B.2 nut-flush value-raise as a polarization pair with B.1 (bluff-raise) on one board. **Honestly DEFERRED:** domination_river_fold (0 — the obvious "fold weak TP" is poker-wrong: TP beats all bluffs = bluff-catch/mix, not a fold) and a 2nd blocker_bluff_catch (would duplicate R3.4); both go to v4.4.1B. **Strategic review caught + fixed 2 pre-migration errors** (A5 "domination" → AQ over-fold call; Ah9c R3.4-dup → nut-flush value-raise). NEW builder + per-sprint auditor (ports production R76–R93 + cross-corpus dup-ID) + add-not-replace two-phase migration. **Audits:** expansion seed **8/0/0 PASS**; production **509/0/0 PASS** (M5 approved 32; M1–M4 unchanged). **Runtime smoke-tested:** `[postflop] loaded 509/509`, ready=true, pool …/**32**, M5 inert until v4.4.2, zero console errors. Critical density kept low (2/5 action) to recalibrate the corpus toward 30–40%. Cache bump 4.4.1→4.4.1A. **No runtime route / UI change.** New doc `docs/specs/postflop-v4.4.1A-module5-expansion.md`; new snapshot `GPT AUDIT/v4.4.1A/`. **Next: v4.4.1B (toward 60; fill deferred) → v4.4.2 runtime wire.**

🟢 **v4.4.1 Module 5 Production Migration (PRODUCTION DATA + CACHE BUMP; NO RUNTIME ROUTE)** — first production-mutating M5 step. Migrated the 24 v4.4.0A-reviewed M5 seeds into production `postflop_scenarios.json`: **477 → 501** (251 M1 + 49 M2 + 85 M3 + 92 M4 + **24 M5**), all `auditStatus=approved` / `reviewStatus=v4.4.0A_strategic_reviewed` / `version=v4.4.1` / `street=river` / per-scenario `schemaVersion=1.3.0`. Two-phase idempotent migration via NEW `tools/migrate-module5-v4.4.1.ps1` (review_pending → -FlipApproved → approved; verifies 24 seeds + 477 non-M5 preserved; UTF-8 NO-BOM atomic write). Also refreshed the stale top-level file `description` (was "385 scenarios…/v4.2.3B"; now accurately 501 across M1–M5) in-script for reproducibility. **NEW M5 production-rule block R76–R93 in `tools/audit-postflop-ps.ps1`** (mirrors M4 R55–R75 + M5 seed river rules): river/spot/5-card-board/enum locks, vocab checks, answer-partition, conditional sizingLogic, flush/straight invariants, **busted-draws-never-call HARD (R90)**, no-draw-equity WARN, text-integrity HARD; + M5 stats block. The v4.4.1 pre-fixes ride along: 12 M5 concepts added to `postflop_concepts.json` (51→63); R10/R13 flop-only rules excluded for M5. **Cache bump:** appVersion `4.3.2C → 4.4.1`; SW VERSION `v4.3.2C → v4.4.1` (cache-first `postflop_scenarios.json` 477→501 + `postflop_concepts.json` 51→63 both changed). **Audits all green:** production **501/0/0 PASS** (M5 approved: 24; M1–M4 unchanged 251/49/85/92); M5 seed 24/0/0 PASS. **Runtime smoke-tested in local preview:** `[postflop] loaded 501/501 (schema 1.0.0)`, `App.postflop.ready=true error=null`, pool 251/49/85/92/**24**, M5 inert (no router until v4.4.2), zero console errors. Top-level file `schemaVersion` stays `1.0.0` (matches app `POSTFLOP_SCHEMA_VERSION` gate). **No runtime route / UI / renderer change.** New tool `tools/migrate-module5-v4.4.1.ps1`; new doc `docs/specs/postflop-v4.4.1-module5-production-migration.md`; new snapshot `GPT AUDIT/v4.4.1/`. **Next (auto-drive): v4.4.1A/B M5 expansion (24 → 60–90) → v4.4.2 M5 runtime wire (route + 5-card 320px renderer) → M6 architecture.**

🟢 **v4.4.0A Module 5 Strategic Seed Review (PLANNING-ONLY)** — reviewed the 24 v4.4.0 M5 seeds for river poker correctness. **24/24 PROMOTE (22 as-authored + 2 via REVISE), 0 REJECT.** Fixes: 2.2 AdTh weak-TP-vs-pot `call->mixed`; 6.2 `AcKh two-pair -> 8c8h set` (BB flats 88 unambiguously). Re-audit 24/0/0 PASS. Production untouched 477/0/0. New doc `docs/specs/postflop-v4.4.0A-module5-strategic-review-results.md`. **Next: v4.4.1 production migration (477 -> 501)** + M5 production audit rules + version bump (first production-mutating M5 step; NO runtime yet).

🟢 **v4.4.0 Module 5 River Defense OOP Architecture (PLANNING-ONLY)** — Owner pivoted the program to "build M5 + M6 as full modules, sprint by sprint" (M5 = River Defense OOP, M6 = River Betting IP, confirmed via AskUserQuestion). v4.4.0 = M5 architecture/seed-plan sprint mirroring the v4.3.0 M4 pattern. **NO production data / runtime / version change** (appVersion stays 4.3.2C; postflop_scenarios.json byte-identical 477/0/0). Module 5 = BB River Defense OOP: hand tree continues from M4 (BTN 3-barrel vs BB; BB faces the river bet OOP). Defining lesson: **river is showdown-only -- bluff-catch under polarization via MDF + blockers; busted draws bluff-raise-or-fold, never call.** **7 planning artifacts:** architecture doc (16 sections), schema-taxonomy (schemaVersion 1.3.0 with river fields + 12 river reasons + 12 river concepts + `riverLogic`), builder `tools/build-m5-seeds-v4.4.0.ps1`, 24-seed JSON (6 categories x 4; 18 action + 6 reason), audit-plan (M5.R01..R58), auditor `tools/audit-postflop-module5-seed.ps1`, GPT review package. **M5 seed audit 24/0/0 PASS.** **NEW river auditor rules:** R55 busted-draws-never-call (HARD), R56 no-draw-equity-language (WARN), R57 riverLogic-references-runout (WARN), R58 text-integrity (HARD). **Author strategic self-review done:** every made-hand claim verified on the full 5-card runout (flushes 5-of-suit, straights 5-consecutive, set/trips rankings vs unblocked villain combos, nut-flush-blocker calls/raises); 1 critical-flag downgrade + 1 blocker wording fix applied. Sizing coverage spans small/medium/large/overbet so MDF varies; over-fold trap AND station trap both represented. **Production audit unchanged 477/0/0; all M2/M3/M4 seed audits unchanged.** **Forbidden files all byte-identical** (postflop_scenarios.json, all v4.3.x builders/JSONs/migration/hotfix tools, audit-postflop-ps.ps1, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, index.html, service-worker.js). New snapshot `GPT AUDIT/v4.4.0/`. **Migration path:** v4.4.0A strategic review -> v4.4.1 production migration (477 -> 501) -> v4.4.1A/B expansion -> v4.4.2 runtime wire. **Next sprint: v4.4.0A strategic seed review** (NOT migration, NOT M6 yet).

🟡 **v4.3.2D real-beta-signal-review attempt** — **BLOCKED on data access; STAY OPTION A pending owner snapshot.** Doc-only ship; no code/scenario/version change. Re-inspection of the Chrome MCP browser at the live Netlify URL confirms the v4.3.2C signal-persistence fix is live (`compactAnswers` + `mode` in `_pfHistoryRecordSession` source) but `localStorage['rmtt_postflop_history']` STILL does not exist; enumeration of all 6 localStorage keys present shows preflop module-1 usage only (3 KB of range-form keys), zero postflop / M4 entries. The owner's reported M4 play sessions are on a different browser/device than the Chrome MCP instance. Per brief Section B + Section I, faking a decision is forbidden when owner data is inaccessible; the correct action is to document the blocker and request the owner export their M4 Beta QA Snapshot. **Three export methods documented** in the new doc Section 7: in-app snapshot button (recommended), DevTools console snapshot, DevTools threshold-check snippet. **Decision: STAY OPTION A** (deferral pending data, not a verdict on owner's M4 play). **Do not author. Do not start v4.3.3 or v4.4.0.** **Forbidden files all byte-identical** (postflop_scenarios.json, all v4.3.0/C/D/v4.3.2 builders + JSONs, all migration + hotfix tools incl. v4.3.2A/v4.3.2B, audit-postflop-ps.ps1 R55-R72, audit-postflop-module4-continuation-v4.3.2.ps1 R73/R74/R75, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, v4.3.2 / v4.3.2A / v4.3.2B / v4.3.2C base docs + v4.3.2C-optionA doc, index.html, service-worker.js). New doc `docs/specs/postflop-v4.3.2D-m4-real-beta-signal-review.md`. **Next action: owner pastes M4 Beta QA Snapshot in next conversation.**

🟡 **v4.3.2C Option A signal review** — STAY OPTION A. Doc-only ship; no code/scenario change. Live Netlify probe confirms `appVersion=4.3.2C` + signal-persistence fix verified live in `_pfHistoryRecordSession` source. Inspected Chrome MCP browser has zero `rmtt_postflop_history` localStorage entry; `_pfM4BetaQAStats()` returns `sessionCount=0` and all per-axis arrays empty -- correct empty state, not an instrumentation bug. **All 8 of 8 v4.3.2C decision-framework thresholds NOT met** (gating: M4 sessions 0 < 5 required; downstream metrics derive from session count). Decision rule applied: STAY OPTION A. **Do not author. Do not start v4.3.3 or v4.4.0.** Recommendation: re-run signal review after 7-14 calendar days of beta usage; hard ceiling 30 days before pivoting to UX/discoverability sprint if signal volume stays thin. New doc `docs/specs/postflop-v4.3.2C-option-a-signal-review.md` includes a paste-ready DevTools snippet for the project owner to re-run the threshold check from their own browser when beta usage data accumulates. **Forbidden files all byte-identical** (postflop_scenarios.json, all v4.3.0/C/D/v4.3.2 builders + JSONs, all migration + hotfix tools, audit-postflop-ps.ps1 R55-R72 + audit-postflop-module4-continuation-v4.3.2.ps1 R73/R74/R75, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, v4.3.2 / v4.3.2A / v4.3.2B / v4.3.2C base docs, index.html, service-worker.js).

🟢 **v4.4.1C committed + pushed** (`c199d33`) — Module 5 Straight-Blocker Mix on top of v4.4.1B (`b2746be`). +1 scenario (**509 → 510**, M5 33 approved): straight-blocker taught honestly as `mixed_indifference_river` (Th9h on Jc9d8s4h2c vs medium; magnitude + sidedness asymmetry roots; trap `blocker_bluff_catch_river` in bad). Both token gaps CLOSED (domination = distractor-only by design; straight-blocker = mixed + curriculum note → v4.4.2 concept copy). Seed 1/0/0; production 510/0/0 both phases; runtime smoke-tested 0 errors. Cache bump 4.4.1B→4.4.1C. New doc + `GPT AUDIT/v4.4.1C/`. (Status entry above carries full detail.)

🟢 **v4.4.1B committed + pushed** (`f144a68`) — Module 5 Correctness Hotfix on top of v4.4.1A (`a046582`). Consultant-reviewed + implementer-corrected; **9 M5 scenarios patched, count UNCHANGED 509**. FIX 1 critical→[] on 6 one-pair fold/call (incl. 2 the consultant missed); FIX 2 AhQc/AhJc overbet monotonicity (corrected false "Ah blocks AK"); FIX 3 #12 Ah9c large→medium (corrected false "single-heart flush"). VERIFY V1–V3 left unchanged. NEW idempotent hotfix tool. Production **509/0/0 PASS**; 0 one-pair fold/call critical; runtime smoke-tested (0 console errors). Cache bump 4.4.1A→4.4.1B. New doc + `GPT AUDIT/v4.4.1B/`. (Status entry above carries full detail.)

🟢 **v4.4.1A committed + pushed** (`a046582`) — Module 5 Expansion on top of v4.4.1 (`d102d5d`). Gap-first **24 → 32** (production **501 → 509**), all M5 `approved`. 8 strategically-reviewed scenarios on 2 new boards; gap-fills pot_odds/bluff_raise/thin_value-role/mdf/mixed/range_disadvantage; anchors A.3 over-fold call + B.2 nut-flush value-raise (polarization pair with B.1). DEFERRED honestly: domination_river_fold + 2nd blocker_bluff_catch → v4.4.1B. NEW builder + per-sprint auditor (ports R76–R93 + dup-ID) + add-not-replace migration. Expansion seed 8/0/0; production **509/0/0 PASS**; runtime smoke-tested (`[postflop] loaded 509/509`, M5 inert). Cache bump 4.4.1→4.4.1A. New doc + `GPT AUDIT/v4.4.1A/`. (Status entry above carries full detail.)

🟢 **v4.4.1 committed + pushed** (`e59a14d`) — Module 5 Production Migration on top of v4.4.0A (`a41a171`). First production-mutating M5 step: 24 M5 seeds → production (**477 → 501**), all `approved`. NEW `tools/migrate-module5-v4.4.1.ps1` (two-phase idempotent) + M5 production-audit block R76–R93 + M5 stats in `audit-postflop-ps.ps1`; `postflop_concepts.json` 51→63; cache bump 4.3.2C→4.4.1. Production audit **501/0/0 PASS**; M5 seed 24/0/0; runtime smoke-tested (`[postflop] loaded 501/501`, M5 inert until v4.4.2). Seed builder + JSON: mechanical metadata fixes only (doc §9). **No runtime route / UI / renderer change** — M5 data-only this sprint. New doc `docs/specs/postflop-v4.4.1-module5-production-migration.md`; new snapshot `GPT AUDIT/v4.4.1/`. (Status entry above carries full detail.)

🟢 **v4.3.2C-doc committed + pushed** (`c22ccc8`). State-doc reconciliation flipping v4.3.2C from staged to committed (`090f21f`).

🟢 **v4.3.2C committed + pushed** (`090f21f`) — Module 4 Beta Signal Collection / QA Instrumentation Review on top of v4.3.2B (`51633b1`). **Meta-level signal-collection review sprint with one minimal allow-listed runtime fix.** Reviews M4 signal collection layer (instrumentation, BetaQA dashboard, weak-spot review, concept drill, coverage matrix) before deciding v4.3.3 (M4 continuation) vs v4.4.0 (M5 architecture). **Defect found and fixed inline:** `_pfHistoryRecordSession` did NOT persist per-answer detail (answers[] array) or drill mode; BetaQA dashboard's multi-session per-axis aggregations have been silently empty since v4.2.6 introduction. Fix: persist compact answers[] array + mode field; defensive read on dashboard side. No strategy / scoring / route / UI / scenario data changes -- pure analytics persistence layer. localStorage budget impact ~48 KB at 50-session cap. **Cache bump:** appVersion 4.3.2B->4.3.2C; SW VERSION v4.3.2B->v4.3.2C. v4.3.1B mobile fix preserved unchanged. **Comprehensive review documented:** field coverage 23/23 stored or derivable post-fix; BetaQA dashboard answers 6.5 of 10 brief questions (7 partial / 3 deferred); weak-spot review 12/12 checks PASS; concept drill 7/7 checks PASS + concept coverage table (all 12 concepts >=7 scenarios); coverage vs signal matrix on M4=92 production data shows 12/12 actionReasons covered (3 thin: pot_odds=4, semi_bluff=3, protection no reason_choice), 12/12 concepts, all 5 actions, 19 boards, 69.6% critical density, 8.7% consensus_gto. **Analytics risk review (boardAbsoluteTexture vs scenarioTeachingAxis):** current logic uses board metadata as scenarioTeachingAxis correctly; no analytics path aggregates by `turnCategory`/`boardChange` across scenarios; verdict ACCEPTABLE no live bug; future schema recommendation documented for v4.4.0 architecture review. **Audits all green:** production 477/0/0 PASS unchanged; M4 continuation 20/0/0 PASS unchanged; M4 polish 19/0/0 unchanged; M4 expansion 29/0/0 unchanged; M4 original seed 24/0/0 unchanged; M3 24/0/0 PASS clean; M2 24/0/8 PASS; R29/R71/R72/R44b/R73/R74/R75 0 fires. **Decision framework with objective thresholds:** Option A (collect signals; recommended) -- 5 sessions / 60 hands / 8-of-12 reason coverage / 8-of-12 concept coverage / 3 critical buckets / 1 weak-review session minimum; Option B (v4.3.3) -- only if concentrated weakness identified; Option C (v4.4.0) -- only if M4 mature signals confirm. **No new scenarios. No strategy changes. No UI/route changes.** **Forbidden files all byte-identical** (postflop_scenarios.json, all v4.3.0/C/D/v4.3.2 builders + JSONs, all migration + hotfix tools incl. v4.3.2A + v4.3.2B, audit-postflop-ps.ps1 R55-R72, audit-postflop-module4-continuation-v4.3.2.ps1 R73/R74/R75, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, v4.3.2 + v4.3.2A + v4.3.2B docs unchanged). New doc `docs/specs/postflop-v4.3.2C-module4-beta-signal-review.md`. New snapshot `GPT AUDIT/v4.3.2C/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2C and immediately enter Option A. Signal-persistence fix unblocks dashboard multi-session aggregation; corpus is balanced; user signals will now drive v4.3.3-vs-v4.4.0 decision objectively.

🟢 **v4.3.2B-doc committed + pushed** (`224e539`). State-doc reconciliation flipping v4.3.2B from staged to committed (`51633b1`).

🟢 **v4.3.2B committed + pushed** (`51633b1`) — Module 4 R1 Metadata Consistency Hotfix on top of v4.3.2A (`e8975dd`). **Targeted metadata-only hotfix sprint. No new scenarios. M4 stays at 92/92.** v4.3.2A corrected R1's strategic verdict + prose but the board sub-document was not updated, leaving `turnCategory: draw_intensifier`, `boardChange: draw_added`, `equityShift: improves_bb_draws`, `drawCompletion: oesd_added` -- inconsistent with the corrected bluff-catch framing on a turn that completes 3 made straights (65/96/J9 on Ts 8s 4d / 7c). v4.3.2B fixes the metadata at the canonical builder source-of-truth: new `$f8R1` board variant declares `turnCategory: straight_complete / boardChange: polarizing / equityShift: polarizes_btn / drawCompletion: straight_completed` and R1 references `$f8R1` while A5 (combo_draw NFD semi-bluff) and A7 (nutted_value top set) on the same physical board keep `$f8` (`draw_intensifier`) because their strategic verdicts legitimately treat the turn primarily as draw-intensifying. **Taxonomy validation:** brief's proposed `boardChange: draw_completed` + `equityShift: polarizes_range` not in approved enums; substituted to closest-valid `polarizing` + `polarizes_btn` with rationale in v4.3.2B doc. **NEW HARD-level M4.R75 guard** added to v4.3.2 continuation auditor scoped to the T-8-4-7 board pattern with bluff_catcher framing (does NOT fire on A5 combo_draw or A7 nutted_value, who legitimately keep draw_intensifier classification). **NEW idempotent narrow-scope hotfix tool** `tools/hotfix-module4-v4.3.2B.ps1` re-syncs ONLY the `board` sub-document of the 1 target ID into production (no Invoke-Expression, no unsafe Remove-Item, atomic tmp+Move-Item; pre-flight + post-write verification including 91-of-91 non-target M4 byte-identity check + scenario-array order preservation + 4-field metadata sanity check). **Audits all green:** production 477/0/0 PASS; M4 continuation 20/0/0 PASS; M4 polish 19/0/0 unchanged; M4 expansion 29/0/0 unchanged; M4 original seed 24/0/0 unchanged; M3 24/0/0 PASS clean; M2 24/0/8 PASS; R29/R71/R72/R44b 0 fires across 92 M4; M4.R73/M4.R74/M4.R75 0 fires across 20 v4.3.2 continuation. **Targeted raw scan PASS:** R1 board.turnCategory=straight_complete, boardChange=polarizing, drawCompletion=straight_completed, equityShift=polarizes_btn; R1 v4.3.2A action/reason/role/showdown/prose preserved byte-identical. **Cache bump only:** appVersion `4.3.2A -> 4.3.2B`; SW VERSION `v4.3.2A -> v4.3.2B`. v4.3.1B `@media (max-width: 359px)` mobile fix preserved unchanged. **No runtime logic / UI / taxonomy / concepts / schema change.** **Forbidden files all byte-identical** (all v4.3.0/C/D builders + JSONs, all migration tools, v4.3.0C1 + v4.3.2A hotfix tools, audit-postflop-ps.ps1 R55-R72 unchanged, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, v4.3.2 + v4.3.2A docs unchanged). New tool `tools/hotfix-module4-v4.3.2B.ps1`. New doc `docs/specs/postflop-v4.3.2B-module4-metadata-hotfix.md`. New snapshot `GPT AUDIT/v4.3.2B/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2B. Module 4 = 92 scenarios with all strategic verdicts AND board metadata classifications now poker-correct and internally consistent. **Recommendation unchanged from v4.3.2 / v4.3.2A:** pause content expansion and collect M4 Limited Beta user signals via the existing Beta Lab dashboard for at least one feedback cycle before deciding v4.3.3 vs v4.4.0.

🟢 **v4.3.2A-doc committed + pushed** (`d074e56`). State-doc reconciliation flipping v4.3.2A from staged to committed (`e8975dd`).

🟢 **v4.3.2A committed + pushed** (`e8975dd`) — Module 4 Continuation Raw Poker-Content Hotfix on top of v4.3.2 (`12ff7c1`). **Targeted poker/content hotfix sprint. No new scenarios. M4 stays at 92 / 92.** Two scenario-level raw-content defects that escaped v4.3.2's mechanical 477/0/0 audit are corrected at the canonical continuation builder source-of-truth and re-synced into production via a new idempotent narrow-scope hotfix tool: (1) **R1 = QcQd on Ts 8s 4d 7c** -- the v4.3.2 author treated 7c as "draw-intensifier" with OESDs (98, J9) and gutshots (96, 65); in reality 7c COMPLETES J9/96/65 straights (only 98 is genuinely pair+OESD). QQ overpair-to-T is BEHIND the made-straight density and sits in BLUFF-CATCH territory, NOT protection-raise. Fix: recommendedAction check_raise_small -> call; actionReason protection_check_raise_turn -> bluff_catch_turn; heroHandRole strong_value -> bluff_catcher; conceptTags rebalanced; full prose rewrite. (2) **R2 = 7s5s on 9c 6c 3h 8c** -- the v4.3.2 author labelled hero bottom straight as nutted_value/showdownValue:nutted and described "charging made flushes" for value; in reality 8c completes a 3-club flush, hero holds ZERO clubs, and made flushes BEAT hero (plus T7 is a higher straight). Fix: recommendedAction check_raise_small -> call; actionReason value_check_raise_turn -> bluff_catch_turn; heroHandRole nutted_value -> bluff_catcher; showdownValue nutted -> high; conceptTags rebalanced (turn_check_raise_value -> turn_bluff_catcher); full prose rewrite explicitly stating hero loses to flushes + T7. **NEW M4.R73 + M4.R74 auditor guards added** to v4.3.2 continuation auditor (WARN level): R73 flags non-flush made hands on monotone/flush-completed turns where hero holds zero suit-cards but is labelled nutted_value/showdownValue:nutted (would have caught Defect 2 pre-migration); R74 prose-scans for "made flushes as value target" patterns when hero handClass is not flush/nut_flush/full_house. Both guards stay inside the per-sprint continuation auditor (not promoted to production R55-R72 to preserve byte-stability of the production auditor across v4.3.x). **NEW idempotent hotfix tool** `tools/hotfix-module4-v4.3.2A.ps1` (no Invoke-Expression; no unsafe Remove-Item; UTF-8 NO-BOM atomic tmp+Move-Item; pre-flight verifies prod=477/M4=92/all approved/target IDs present in source+production; post-write verifies field equality vs source on 11 syncFields + zero drift on 90 non-target M4 scenarios + scenario-array order preservation). **Audits all green:** production 477/0/0 PASS; M4 continuation 20/0/0 PASS (CR.R03 drift cleared post-hotfix); M4 polish 19/0/0 PASS unchanged; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged; M3 24/0/0 PASS clean; M2 24/0/8 PASS; R29/R71/R72/R44b 0 fires across 92 M4; M4.R73/M4.R74 0 fires across 20 v4.3.2 continuation. **Targeted text/poker scan PASS:** R1 no J9/96/65 framed as draws; R2 no "made flushes" framed as value target; R2 heroHandRole=bluff_catcher / showdownValue=high (not nutted_value/nutted). **Cache bump only:** appVersion `4.3.2 -> 4.3.2A`; SW VERSION `v4.3.2 -> v4.3.2A` (production data prose changed in 2 scenarios). v4.3.1B `@media (max-width: 359px)` mobile fix preserved unchanged. **No runtime logic / UI / taxonomy / concepts / schema change.** **Forbidden files all byte-identical** (all v4.3.0/C/D builders + JSONs, migration tools B/C/D/v4.3.2, v4.3.0C1 hotfix tool, audit-postflop-ps.ps1 R55-R72 unchanged, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, v4.3.2 doc unchanged). New tool `tools/hotfix-module4-v4.3.2A.ps1`. New doc `docs/specs/postflop-v4.3.2A-module4-content-hotfix.md`. New snapshot `GPT AUDIT/v4.3.2A/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2A. Module 4 = 92 scenarios with all strategic verdicts now poker-correct. **Recommendation unchanged from v4.3.2:** pause content expansion and collect M4 Limited Beta user signals via the existing Beta Lab dashboard for at least one feedback cycle before deciding v4.3.3 (further continuation 92 -> 100-110) vs v4.4.0 (M5 river-defense architecture).

🟢 **v4.3.2-doc committed + pushed** (`0859d2c`). State-doc reconciliation flipping v4.3.2 from staged to committed (`12ff7c1`).

🟢 **v4.3.2 committed + pushed** (`12ff7c1`) — Module 4 Coverage Continuation 72 -> 92 on top of v4.3.1B (`e9fadfc`). **20 NEW M4 scenarios authored, audited, strategically reviewed, and migrated to production.** New v4.3.2 builder + JSON + auditor + migration tool authored separately from v4.3.0 / v4.3.0C / v4.3.0D layers (source-of-truth integrity preserved). **Production count 457 -> 477** (251 M1 + 49 M2 + 85 M3 + **92 M4**). **Coverage targets all hit:** reason_choice 13 -> 21 (18.1% -> 22.8%); blocker_check_raise_turn 3 -> 6; mixed_indifference_turn 4 -> 7; check_raise_big 2 -> 3; all 12 actionReasons covered; 19 unique flop families (was 17; +F12 Kc 7s 2d / Qh overcard demote, +F13 9c 6c 3h / 8c flush+straight complete simultaneously). **Critical density 72.2% -> 69.6%** (target 65-75% MET; 12 of 20 v4.3.2 carry critical = 60% batch). **All 20 sourceConfidence=expert_judgment** (combined 8/92 = 8.7% consensus_gto). **Strategic review verdict: 20/20 PROMOTE.** 4 audit-driven REVISE during build (R3 drawCategory `gutshot -> backdoor_only`; A5 hero `KsQs -> AsQs` for true nut FD; R7 R72 prose artifact "wait -- recompute" cleaned; A1 `acceptable=[call]` added for NFD defensible call). **Audits all green:** production 477/0/0 PASS; M4 continuation 20/0/0 PASS; M4 polish 19/0/0 PASS unchanged; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged; M3 24/0/0 PASS clean; M2 24/0/8 PASS; R29/R71/R72/R44b 0 fires across 92 M4. **Cache bump:** appVersion `4.3.1B` -> `4.3.2`; SW VERSION `v4.3.1B` -> `v4.3.2` (production data changed; cache invalidation required). v4.3.1B `@media (max-width: 359px)` single-column TCC fix preserved unchanged. **No runtime logic / UI / taxonomy / concepts / schema change.** **Forbidden files all byte-identical** (all v4.3.0 / C / D builders + JSONs, migration tools, hotfix tool, audit-postflop-ps.ps1 R55-R72 unchanged, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json). New tools `tools/build-m4-continuation-v4.3.2.ps1` + `tools/audit-postflop-module4-continuation-v4.3.2.ps1` + `tools/migrate-module4-v4.3.2.ps1`. New seed JSON `docs/specs/postflop-v4.3.2-module4-continuation-seeds.json`. New doc `docs/specs/postflop-v4.3.2-module4-coverage-continuation.md`. New snapshot `GPT AUDIT/v4.3.2/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2. Module 4 stays Limited Beta -- 92 scenarios is approaching mature-runtime-beta density. **Recommendation:** pause content expansion and collect M4 Limited Beta user signals via the existing Beta Lab dashboard for at least one feedback cycle before deciding between v4.3.3 (further continuation 92 -> 100-110) or v4.4.0 (M5 river-defense architecture).

🟢 **v4.3.1B-doc committed + pushed** (`bef1765`). State-doc reconciliation flipping v4.3.1B from staged to committed (`e9fadfc`).

🟢 **v4.3.1B committed + pushed** (`e9fadfc`) — Real-Device Mobile QA + UX Polish on top of v4.3.1A (`59b8184`). **One mobile-layout bug found in iframe-based 320/360/375/414 QA harness, fixed with one CSS media query in index.html.** Real-device mobile QA brief executed across all 4 device-class viewports (320 = iPhone SE 1st gen / iPhone 5 / very narrow Android; 360 = typical Android mid-range; 375 = iPhone X-15 base; 414 = iPhone Plus / Pro Max). QA stack: same `.qa-serve.ps1` PowerShell HTTP server at `http://localhost:8765/` (gitignored via `*.ps1`); NEW `.qa-harness.html` iframe-based harness (each iframe gets its own viewport so CSS media queries fire correctly inside, working around Chrome's MCP `resize_window` not propagating to inner viewport); Claude-in-Chrome MCP browser tools driving programmatic injection of postflop drill state via `iframe.contentWindow.eval(...)`. Coverage: M4 entry / question (4-card board fits at 320, TURN amber pill clean, 5-step spot-tag chip flow wraps cleanly, hand-class chips fit, BB-OOP framing intact post-v4.3.1A) / answer feedback (8-block layout including TURN LOGIC prominent) / session complete (2x2 tier-chip grid + Review Signals + M4 Turn Defense Breakdown + 3 buttons) / Concept Library M4 group (12 concepts) / Beta Lab Academy + M4 mastery checklist; M1/M2/M3 + Preflop Drill / Browse / Settings regression all clean at 320/360/375/414. Cache/update QA verified working-as-designed (SW activate handler `caches.keys().filter(k => k !== CACHE_NAME).map(k => caches.delete(k))` cleanup confirmed leaves only current cache after `SKIP_WAITING` + reload). **Bug #1 found:** at 320 px CSS viewport `.tcc-action-grid` is `display:grid; grid-template-columns: 1fr 1fr` and the default `1fr` is `minmax(auto, 1fr)` where `auto` ≈ min-content. Tile min-content (`icon 26 + gap 8 + label-min ~50 + gap 8 + badge ~40 + padding 24 ≈ 156 px`) exceeds the panel's content area (~220 px) at 320 width, causing right-column tiles to overflow ~36 px past the panel's right edge under existing `body { overflow-x: hidden }`. User-visible: "Concept Library", "Postflop Progress", and **"Module 4 · Facing Turn Barrel OOP" tile + BETA badge + "72 turn-defense scenarios · Limited Beta" subtitle** all clipped on the right edge. **Severity:** layout/visual (M4 entry partially hidden on iPhone SE 320), not data or grading. Pre-existing fragility, became user-visible at v4.3.1 because the new long-copy M4 tile + BETA badge sits in the right column. **Fix:** new `@media (max-width: 359px)` block in index.html (lines 11790-11806) with `.tcc-action-grid { grid-template-columns: 1fr; } .tcc-action.is-primary { grid-column: 1; }`. <360 width → single-column stack of all 7 TCC tiles (M1, M2, Concept Library, Weak Spot Review, Postflop Progress, M3, M4) with full readable copy + BETA badge; 360+ unchanged (2-column grid preserved); 720+ unchanged (3-column via existing `@media (min-width: 720px)`). **Fix verification:** w320 maxRight 261 vs 320 (was 357 vs 320 = 36 px overflow); w360/w375/w414 unchanged. Visual confirms M4 tile + BETA badge + subtitle fully visible end-to-end at 320. **Cache bump:** appVersion `4.3.1A` → `4.3.1B`; SW VERSION `v4.3.1A` → `v4.3.1B` (CSS asset changed; v4.3.1A already deployed; bump invalidates stale cache so fix reaches users). **Programmatic tap-target audit:** all primary controls ≥ Apple HIG 44 x 44 across all 4 widths — `.postflop-choice-btn` 56x271..366 PASS; `.postflop-exit-btn` 49x44..58x44 PASS; `.postflop-next-btn` (Finish) 44x271..336 PASS; `.postflop-summary-btn` (Drill Weak) 49x271..366 PASS; `.postflop-restart-btn` (Drill again) 47x271..366 PASS; `.bottom-nav-item` 53x61 PASS. Three tertiary disclosures fall slightly under HIG (`.pf-teach-hint-row` 33; `.pf-rng-toggle` 36; `.postflop-back-btn` 41) — not v4.3.1 regressions, not in scope of M4 mobile-polish brief, documented for future polish sprint, NOT FIXED in v4.3.1B. **Audits all green:** production 457/0/0 PASS unchanged; all 5 seed audits unchanged; R29/R71/R72/R44b 0 fires across 72 M4. Production scenarios JSON byte-identical to v4.3.0D blob `d13bf697b561...`. **Final ship verdict:** SHIP v4.3.1B. Module 4 IS PLAYABLE on iPhone SE 320 with the M4 entry tile fully readable. **Forbidden files all byte-identical** (all v4.3.0/C/D builders + JSONs, migration tools, hotfix tool, audit-postflop-ps.ps1, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, postflop_scenarios.json). New doc `docs/specs/postflop-v4.3.1B-real-device-mobile-qa.md`. New snapshot `GPT AUDIT/v4.3.1B/` with INHERITANCE_NOTE.txt + MANIFEST_SHA256.txt. Netlify v4.3.1B live and verified at `https://range-master-mtt.netlify.app/` after auto-redeploy.

🟢 **v4.3.1A-doc2 committed + pushed** (`f11c154`). Micro state-doc reconciliation: refined Netlify-live wording in v4.3.1A row.

🟢 **v4.3.1A-doc committed + pushed** (`895cc88`). State-doc reconciliation flipping v4.3.1A from staged to committed (`59b8184`).

🟢 **v4.3.1A committed + pushed** (`59b8184`) — Live QA + Ship hotfix on top of v4.3.1 (`c1998cf`). **One copy bug found in live browser QA, fixed in index.html.** All 16 QA sections executed via Chrome-MCP: baseline gates green, console clean, M4 entry/route launches 12-question drill with `module='pf_turn_barrel_oop_def'` + `allM4=true`, 4-card board with TURN amber pill, hero hand suit symbols correct (As→A♠, 6c→6♣ DOM-verified), all 4 grading tiers (best/acceptable/bad/critical) verified action_choice + reason_choice with correct UI banners and tier classifications, R44b runtime 0 violations, 8-block feedback (Turn Logic 🎯 PROMINENT + 7 others), session summary "Module 4 · Turn Defense Breakdown (Limited Beta)" + Drill again routes to M4, 12 M4 concepts drillable + 35 candidate scenarios for turn_blocker_pressure, weak-spot empty-state toast + active path 5 distinct target reasons M4-only, mastery checklist Limited Beta thresholds (4 sessions / 75% in 2 / 9-of-12 reasons / weak-review / no critical) with no NaN/undefined, M1/M2/M3 regression all 4 modules grade correctly via normalizer, mobile column-width 320/360/375 layout fits cleanly, desktop 1280x900 0 console errors. **Bug #1 (copy bug, not grading/data):** M4 question prompt rendered BTN framing because `_pfBuildQuestionPrompt` had isM3 special branch but no parallel isM4. **Fix:** added isM4 branch mirroring isM3 with turn-defense framing extracting `flopCards` + `turnCard` from M4 schema for separated flop/turn rendering. Programmatic verification: M4 action_choice prompt now reads "BB has [hero] on [flop], turn [turn] (BTN open 2.5x, BB call, 100BB SRP). After flop check-call, BTN now barrels turn. What is BB's best action OOP?". M3/M2/M1 paths byte-identical (additive only). **Cache bump:** appVersion 4.3.1 → 4.3.1A; SW VERSION v4.3.1 → v4.3.1A (live deployed cache needs invalidation per Section P). **Audits all green:** production 457/0/0 PASS unchanged; all 5 seed audits unchanged; R29/R71/R72/R44b 0 fires across 72 M4. Production scenarios JSON byte-identical to v4.3.0D. **Final ship verdict:** SHIP v4.3.1A. Module 4 IS PLAYABLE as Limited Beta with correct BB-OOP framing throughout. Honest Limited Beta copy preserved. **Forbidden files all byte-identical** (all v4.3.0/C/D builders + JSONs, migration tools, hotfix tool, audit-postflop-ps.ps1, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json). New doc `docs/specs/postflop-v4.3.1-qa-results.md`. New snapshot `GPT AUDIT/v4.3.1A/` exported from final HEAD `895cc88` (v4.3.1A doc reconcile; substantive fix at `59b8184`). Netlify v4.3.1A live and verified at `https://range-master-mtt.netlify.app/`.

🟢 **v4.3.1-doc committed + pushed** (`1f2689d`). State-doc reconciliation flipping v4.3.1 from staged to committed (`c1998cf`).

🟢 **v4.3.1 committed + pushed** (`c1998cf`) — Module 4 Limited Beta Runtime Wire on top of v4.3.0D (`023430a`). **Runtime-only sprint.** Wires existing 72-scenario M4 corpus as playable Limited Beta. NO scenario data changes. Production count unchanged at 457; production scenarios JSON byte-identical to v4.3.0D manifest. Runtime additions in index.html: `getModule4Scenarios()` filter; `_PF_M4_REASON_LABELS` map for 12 _turn-suffixed reasons; `_pfM4TeachingFeedbackBlocksHtml` (turnLogic-PROMINENT 8-block layout); M4 first-time explainer (`_pfM4SessionCount` / `_pfM4IsFirstTime` / `_pfM4FirstTimeExplainerHtml`); M4 mastery (`_pfM4MasteryStats` / `Progress` / `ProgressHtml`) with Limited Beta thresholds (4 sessions / 75% in 2 / 9-of-12 reasons / weak-review / no-critical); M4 BetaQA dashboard (`_pfM4BetaQAStats` thin wrapper + `WeakSpotPreview` / `CriticalMonitor` / `DashboardHtml` / `CopySnapshotClick` / `SessionLeakHtml`); M4 session aggregations (`_pfM4RenderSessionAggregations`); `action_choice_m4` + `reason_choice_m4` choice guides; TRAINING_MODES.postflop.actions M4 entry with route `postflop:m4` + BETA badge; `runTrainingModeAction` `'postflop:m4'` case → `startPostflopDrill('pf_turn_barrel_oop_def', 12)`; `startPostflopDrill` + `startPostflopConceptDrill` + `startPostflopWeakSpotReview` extended with M4 routing + M4 empty-state toast; `renderPostflopQuestion` + `renderPostflopAnswer` + `renderPostflopComplete` extended with `isM4` branches (Module 4 context label, "Your Hand (BB)" title, drawCategory + showdownValue chips, 5-step spot-tag flow, 4-card board with turn-card pill on index 3, M4 first-time explainer, M4 teaching feedback routing, M4 module label + aggregations + leak hint + drill-again route on session summary); 12 M4 entries added to `_PF_CONCEPT_LIBRARY` (drillable, module='m4'); `_pfConceptLibraryHtml` adds M4 group "Module 4 — Turn Defense OOP (12 · Limited Beta)"; `_PF_CURRICULUM` M4 entry flipped from "Future module" to Limited Beta with 8-line syllabus; `_pfModuleStatus('m4')` → `'beta'`; `_pfAcademyHomeHtml` mounts m4MasteryHtml + m4BetaQAHtml; NEW CSS `.postflop-board-card.is-turn-card` (amber outline + `.pf-turn-pill`) is the only new visual primitive. **Programmatic grading QA:** 6/6 sample scenarios PASS all 4 invariants; R44b reason_choice integrity 0 violations across 13 reason_choice; simulated walkthrough confirms tier classification correct for all 5 actions. **Audits all green:** production 457/0/0 PASS unchanged; M4 polish 19/0/0 PASS unchanged; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged; M3 24/0/0 PASS clean; M2 24/0/8 PASS; R29/R71/R72/R44b 0 fires across 72 M4. **Cache bump:** appVersion 4.3.0D → 4.3.1; SW VERSION v4.3.0D → v4.3.1. **Production data verified byte-identical to v4.3.0D manifest** via SHA256 + blob OID match. **Forbidden files all byte-identical** (postflop_scenarios.json, all v4.3.0/C/D builders + JSONs, all v4.3.0B/C/D migration tools, hotfix tool, audit-postflop-ps.ps1, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json). **Module 4 IS PLAYABLE as Limited Beta** from TCC + Postflop Academy curriculum + Concept Library. Honest copy: "Limited Beta — 72 scenarios", "more scenarios coming", "display only mastery", "structured practice not final certification". New doc `docs/specs/postflop-v4.3.1-module4-limited-beta-wire.md`. New snapshot `GPT AUDIT/v4.3.1/` with SHA256 manifest exported from final HEAD `c1998cf`.

🟢 **v4.3.0D-doc committed + pushed** (`9792758`). State-doc reconciliation flipping v4.3.0D from staged to committed (`023430a`).

🟢 **v4.3.0D committed + pushed** (`023430a`) — Module 4 Coverage Polish 53 → 72 on top of v4.3.0C1 (`896e0ef`). **Production-data polish sprint.** Authored 19 NEW polish scenarios across 10 boards (9 existing M4 families + NEW F11 = `7s 5d 3h / 4c` low BB-favored straight-complete). **Production count 438 → 457** (M4 53 → 72). **Coverage gaps closed:** `blocker_check_raise_turn` 1 → 3 (+2 reason_choice with As-blocker engine on flush-complete + A-overcard turns); `reason_choice` qtype 6 → 13 (~18% of M4); `mixed_indifference_turn` 3 → 4 (TT underpair multi-FD); `consensus_gto` sourceConfidence 0 → 8 textbook spots. **Critical-flag density recalibrated:** combined M4 81.1% → 72.2% (target 70-75% MET) via polish authoring only — no existing scenarios touched. Two-phase migration (review_pending → approved). 19/19 strategic review PROMOTE / 0 REVISE / 0 REJECT. 5 audit-driven revisions during build (AsQs board collision; JhJs critical recalibrated; As9c R71 nut FD reclassification; TdTc + 8h7d reason_choice answer-id leakage). **NEW M4.R44b** (reason_choice answer integrity, mirrors production R04 strictness) added to polish auditor. R71 = 0 fires, R72 = 0 hits across full 72-scenario M4 corpus. **Audits:** production 457/0/0 PASS; M4 polish 19/0/0 PASS; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged; M3 24/0/0 PASS clean unchanged; M2 24/0/8 PASS unchanged; R29=0 preserved. **Cache bump:** appVersion 4.3.0D, SW VERSION v4.3.0D. **Runtime-lock preserved:** M4 NOT in TRAINING_MODES, no postflop:m4 route. **Forbidden files byte-identical** (v4.3.0 baseline builder + JSON; v4.3.0C expansion builder + JSON; production auditor; concepts; taxonomy; M2/M3/M4 seed auditors; v4.3.0B/C migration tools; v4.3.0C1 hotfix tool; ranges; manifest; preflop; gamification; M1/M2/M3 strategy fields). New tools `tools/build-m4-polish-v4.3.0D.ps1` + `tools/audit-postflop-module4-polish-v4.3.0D.ps1` + `tools/migrate-module4-v4.3.0D.ps1`. New seed JSON `docs/specs/postflop-v4.3.0D-module4-polish-seeds.json`. New doc `docs/specs/postflop-v4.3.0D-module4-coverage-polish.md`. New snapshot `GPT AUDIT/v4.3.0D/` with SHA256 manifest exported from final HEAD.

🟢 **v4.3.0C1-doc committed + pushed** (`c8d6ef7`). State-doc reconciliation flipping v4.3.0C1 from staged to committed (`896e0ef`).

🟢 **v4.3.0C1 committed + pushed** (`896e0ef`) — Module 4 Expansion Raw Content Hotfix on top of v4.3.0C (`19569e6`). **Targeted content-hotfix sprint. No new scenarios. M4 stays at 53 / 53.** Two raw-content defects that escaped v4.3.0C's mechanical 438/0/0 audit are corrected at the canonical builder source-of-truth and re-synced to production via a new idempotent hotfix tool: (1) **7s6h on Ac-7d-2s-4h** false-`gutshot` claim removed (no one-card straight reachable with hero+board ranks 2/4/6/7/A); reclassified `drawCategory: gutshot → none`, `heroHandRole: marginal_made_hand → bluff_catcher`, `actionReason: equity_realization_turn_call → bluff_catch_turn`, conceptTags rebalanced, "wait need 3+5..." authoring artifact cleaned out of all explanation prose. recommendedAction stays `call`. (2) **9d6d on Ts-8s-4d-7c** prose-only fix — strategic verdict (T-high straight check-raise small for value) and `handClass=straight` were correct, but turnLogic shipped with `"Loses only to JT and J9 (J9 makes 7-8-9-T-J wait needs J actually impossible without J on board)"`. Rewritten: hero has T-high straight 6-7-8-9-T, only higher straight is `7-8-9-T-J` which requires `J9` in villain, JT removed (it makes pair-of-tens not a higher straight), all "wait..." / "actually impossible" prose stripped. (3) Metadata fix `expansionStats.finalProductionTarget`: `"52 (24 baseline + 28 new)" → "53 (24 baseline + 29 new)"`; expansion `version: v4.3.0C → v4.3.0C1`. **NEW R72 text-integrity guard** (HARD error) added to `tools/audit-postflop-module4-expansion-v4.3.0C.ps1` AND `tools/audit-postflop-ps.ps1` (M4-scoped). Catches `wait`/`wait needs`/`wait need `/`actually impossible`/`... wait`/`...wait` substrings in `explanation.{short, turnLogic, rangeContext, handLogic, sizingLogic, commonMistake, takeaway}` and in `blockerNote`. **CR.R03 refinement** in expansion auditor — replaces "expansion ID exists in production" pre-migration check with content-drift check (fires only when an expansion ID is in production AND a synced field disagrees with the expansion source). **NEW hotfix tool** `tools/hotfix-module4-v4.3.0C1.ps1` (idempotent; no Invoke-Expression; no unsafe Remove-Item; UTF-8 NO-BOM atomic tmp+Move-Item; pre-flight gates on production total=438 / M4=53 / all approved / target IDs present in source+production; post-write verifies field equality vs source + zero drift on 436 non-target scenarios + scenario-array order preservation). **Audits all green:** production 438/0/0 PASS; expansion 29/0/0 PASS; M4 original seed 24/0/0 unchanged; M3 24/0/0 unchanged; M2 24/0/8 unchanged; R72 text-scan 0 hits across 53 M4. **Cache bump only:** appVersion `4.3.0C → 4.3.0C1`; SW VERSION `v4.3.0C → v4.3.0C1`. **Runtime-lock preserved:** M4 NOT in TRAINING_MODES.postflop.actions, no postflop:m4 route, no M4 runtime UI. **Forbidden files byte-identical** (original v4.3.0 builder, original M4 seed JSON, M2/M3/M4 seed auditors, both prior migration tools, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json). New tool `tools/hotfix-module4-v4.3.0C1.ps1`. New doc `docs/specs/postflop-v4.3.0C1-module4-content-hotfix.md`. New snapshot `GPT AUDIT/v4.3.0C1/` with SHA256 manifest exported from final HEAD `896e0ef`.

🟢 **v4.3.0C-doc committed + pushed** (`7399e6d`). State-doc reconciliation flipping v4.3.0C from staged to committed (`19569e6`).

🟢 **v4.3.0C committed + pushed** (`19569e6`). Module 4 Data Expansion (24 → 53). Authored 29 new expansion scenarios across 10 new board families. **Production count 409 → 438** (M4 24 → 53). **All 5 actions now represented:** fold 12 / call 21 / check_raise_small 15 / **check_raise_big 2** / **mixed 3**. **All 12 actionReasons covered** (only blocker_check_raise_turn thin). **6 turn categories all expanded.** Two-phase migration (review_pending → approved) via canonical tool with no Invoke-Expression / no unsafe Remove-Item. 29/29 strategic review PROMOTE. 4 critical-flag recalibrations applied. R71 bidirectional FD: 0 fires on full 53-scenario corpus (stays WARN). Concepts/taxonomy/auditor unchanged from v4.3.0B. **Audits:** production 438/0/0 PASS; M2/M3/M4 seed unchanged. **Cache bump:** appVersion 4.3.0C, SW VERSION v4.3.0C. **Runtime-lock preserved:** M4 NOT in TRAINING_MODES, no postflop:m4 route. **Forbidden files byte-identical** (original v4.3.0 builder, original M4 seed JSON, M2/M3/M4 seed auditors all untouched). New tools `tools/build-m4-expansion-v4.3.0C.ps1` + `tools/audit-postflop-module4-expansion-v4.3.0C.ps1` + `tools/migrate-module4-v4.3.0C.ps1`. New doc `docs/specs/postflop-v4.3.0C-module4-data-expansion.md`. New seed JSON `docs/specs/postflop-v4.3.0C-module4-expansion-seeds.json`. New snapshot `GPT AUDIT/v4.3.0C/` with SHA256 manifest exported from final HEAD.

🟢 **v4.3.0B-doc committed + pushed** (`c65ce16`). State-doc reconciliation flipping v4.3.0B from staged to committed (`95fe02f`).

🟢 **v4.3.0B committed + pushed** (`95fe02f`). Module 4 Production Migration (DATA + AUDIT). Migrated 24 strategically-reviewed M4 planning seeds into production `postflop/postflop_scenarios.json`. **Production count 385 → 409** (251 M1 + 49 M2 + 85 M3 + 24 M4). Production auditor extended with M4-aware rules R55-R71 (incl. NEW R71 bidirectional nut_flush_draw invariant: hero A-of-suit + 4-of-suit total + handClass not made-flush-class → drawCategory should be nut_flush_draw). R10/R13 scoped to non-M4 modules. Concepts file extended 39 → 51 (+12 M4-native concepts under category=module4). Taxonomy file extended with M4 module entry + heroHandRole.module4 (13 values) + actionReason.module4 (12 values) + 6 new top-level enums (turnCategory, boardChange, equityShift, drawCompletion, pairStatusChange, suitTextureTurn). New idempotent migration tool `tools/migrate-module4-v4.3.0B.ps1` with two-phase staged approval (review_pending → approved). **Audits:** production 409/0/0 PASS; M2 24/0/8 PASS unchanged; M3 24/0/0 PASS clean unchanged; M4 seed audit 24/0/0 PASS unchanged. R29=0 warnings preserved. R71 bidirectional FD warn: 0 fires in production. **Strategy-field survival check on 14 scenarios (11 REVISE + 3 PROMOTE):** 0 mismatches. **Cache bump only:** appVersion 4.2.6 → 4.3.0B; SW VERSION v4.2.6 → v4.3.0B. **Runtime-lock preserved:** M4 NOT in TRAINING_MODES.postflop.actions; no postflop:m4 route; no M4 runtime UI. **24 scenarios is migration-ready but NOT runtime-ready** — preview-locked until v4.3.0C expands to 50+. **Sprint-time bug discovered + recovered:** helper script's Invoke-Expression scope-leaked $path causing accidental delete of postflop_concepts.json + postflop_taxonomy.json; recovered via git restore + inline re-application; no deletion propagated to HEAD. **Forbidden files all byte-identical** (tools/build-m4-seeds, seed JSON, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy). New doc `docs/specs/postflop-v4.3.0B-module4-production-migration.md`. New snapshot `GPT AUDIT/v4.3.0B/` with SHA256 manifest exported from final HEAD.

🟢 **v4.3.0A-doc2 committed + pushed** (`69fe211`). Micro state-doc reconciliation: removed stale "Awaiting commit + push" wording from TASK_BOARD v4.3.0A banner.

🟢 **v4.3.0A-doc committed + pushed** (`ab26aee`). State-doc reconciliation flipping v4.3.0A from staged to committed (`0ea2910`).

🟢 **v4.3.0A committed + pushed** (`0ea2910`). Module 4 Seed Strategic Review + Repair (PLANNING-ONLY). Reviewed all 24 Module 4 planning seeds against Poker NLH MTT chipEV / 100BB / BTN-vs-BB SRP context using the 14-point per-scenario checklist. **Verdict counts:** 13 PROMOTE / **11 REVISE** / 0 REJECT. All 11 REVISE seeds repaired builder-first; JSON regenerated. **Repairs:** D-issue duplicate conceptTag fix on #5 9c9s; handClass `top_pair_weak_kicker` → `mid_pair` on #7 9h7h and #15 Tc9d (post-turn ranking); drawCategory `flush_draw`/`none` → `nut_flush_draw` on #10 AsKd and #11 AsJd (As + 4 spades); drawCategory `gutshot` → `oesd` on #14 KhQh (T-J-Q-K = 4-consecutive); drawCategory `none` → `gutshot` on #24 KhQd; false "small backdoor straight" / "gutshot to 7" / "wait" prose cleaned on #9 6s5s, #12 Tc9c, #23 As6s; critical=[call,check_raise_big] cross-category consistency on #20 QhJh. **Critical-flag calibration:** 18/24 (75%) have critical, evenly distributed 3/4 per category, 0 reason_choice with critical (correct). **Anti-filler:** 24 distinct strategic lessons across 6 turn categories x 4. **JSON 180108 B post-edit.** **M4 seed audit:** 24/0/0 PASS. **Production audit unchanged 385/0/0**. M2 + M3 seed audits unchanged. R29 = 0 warnings preserved. **Forbidden files byte-identical** (no production data, no runtime, no version bump). **TRAINING_MODES.m4 still preview/null.** New doc `docs/specs/postflop-v4.3.0A-module4-strategic-review-results.md`. New snapshot `GPT AUDIT/v4.3.0A/` with SHA256 manifest exported from the v4.3.0A commit (`0ea2910`) and refreshed at HEAD `ab26aee`.

🟢 **v4.3.0-preA-doc committed + pushed** (`5e74a18`). State-doc reconciliation flipping v4.3.0-preA from staged to committed (`9a05c30`).

🟢 **v4.3.0-preA committed + pushed** (`9a05c30`). Module 4 Seed Sanity Repair before Strategic Review (CORRECTIVE PLANNING). Corrective sprint on top of v4.3.0 (`3816723`). Mechanical M4 audit was 24/0/0 PASS but raw seed review caught 4 content/poker-logic bugs the auditor did not detect: (1) all 18 action_choice prompts shipped without hero hands -- root cause was PowerShell parsing `$hero?` as variable name `hero?` (silently empty); (2) Tc7c on 9d 8c 6h Kc was classified as oesd/semi_bluff but ALREADY makes the nut straight 6-7-8-9-T on the flop; (3) AhJs on Ks 8s 3d 2s claimed nut-spade blocker but Ah is the heart Ace not spade Ace; (4) AdJs on 9d 8c 6h Kc claimed gutshot but no straight draw exists. **Fixes applied to canonical builder + JSON regenerated:** Q-Action helper `${hero}?` brace fix (all 18 prompts now contain hero hand); Tc7c reclassified to straight/nutted_value/value_check_raise_turn; AhJs replaced with AsJd (true nut spade blocker); AdJs drawCategory gutshot→none + prose rewrite. **Auditor strengthened with 5 new rules** (M4.R50 no 'with ' suffix; M4.R51 prompt contains hero hand; M4.R52 nut_flush_draw requires A of 4-suited suit; M4.R53 nut-suit blocker claim requires hero A of that suit; M4.R54 handClass=straight requires 5 consecutive ranks). **Negative-test verified:** all 5 rules fire on injected bugs. **Critical-flag sanity:** Tc7c + AsJd reason_choice no critical (correct); AdJs action_choice critical=[call,check_raise_big] kept (severe punts). 0 reason_choice scenarios have critical. **Post-fix audits:** M4 seed audit 24/0/0 PASS; production audit unchanged 385/0/0; M2 seed audit unchanged 24/0/8 PASS; M3 seed audit unchanged 24/0/0 PASS clean. R29 = 0 warnings preserved. **Forbidden files byte-identical** (no production data, no runtime, no version bump). New net diff: builder ~+30 lines, auditor +93 lines (R50-R54), audit-plan +56 lines (R50-R54 doc), seed JSON regenerated 177918→178993 bytes. GPT AUDIT/v4.3.0-preA/ canonical snapshot exported from commit 9a05c30 with SHA256 manifest.

🟢 **v4.3.0 committed + pushed** (`3816723`). Module 4 Turn Defense OOP Architecture + Seed Plan (PLANNING-ONLY). Mirrors the v4.2.0 Module 3 planning pattern. Locks Module 4 scope to **BB Turn Defense OOP** (BB facing BTN turn barrel after flop check-call): NLH MTT 100BB SRP, BTN 2.5x → BB call → BTN c-bet small flop → BB call → BTN barrels turn → BB OOP turn decision. Out of scope: BTN IP turn barreling, donk-leading, multiway, 3-bet pots, ICM, river. **6 planning artifacts created**: (1) `docs/specs/postflop-v4.3.0-module4-turn-defense-oop-architecture.md` (16 sections); (2) `docs/specs/postflop-v4.3.0-module4-schema-taxonomy.md` (extends 1.1.0 → 1.2.0 with 4-card board structure, new turn-specific fields, new `slowplay_trap` heroHandRole, 12 `_turn`-suffixed actionReason values, 12 M4-native conceptTags, new defining `turnLogic` explanation field); (3) `docs/specs/postflop-v4.3.0-module4-seed-scenarios.json` (24 planning seeds across 6 turn categories x 4 each: brick / overcard / flush_complete / straight_complete / board_pair / draw_intensifier; 18 action_choice + 6 reason_choice; 6 distinct boards; ALL 24 carry `auditStatus=planning_only`, `reviewStatus=v4.3.0_seed_candidate`, schemaVersion `1.2.0`, module `pf_turn_barrel_oop_def`, plus `uniquenessNote` for anti-filler discipline); (4) `docs/specs/postflop-v4.3.0-module4-audit-plan.md` (49 mechanical rules `M4.R01..M4.R49`); (5) `docs/specs/postflop-v4.3.0-module4-gpt-review-package.md` (per-scenario reviewer template + 8 strategic-quality flags + 5 aggregate questions); (6) `tools/audit-postflop-module4-seed.ps1` (PS 5.1 mechanical auditor, ASCII-only). Builder script `tools/build-m4-seeds-v4.3.0.ps1` authored 24 seeds (177918 bytes JSON, 0 mojibake). **M4 seed audit verified PASS:** 24 scenarios / 0 hard errors / 0 warnings. **NO production data touched, NO runtime wiring, NO version bump in index.html / service-worker.js.** Forbidden files all byte-identical (postflop_scenarios/concepts/taxonomy + tools/audit-postflop-ps + tools/audit-postflop-module2-seed + tools/audit-postflop-module3-seed + ranges + manifest + preflop + gamification). Production audit unchanged 385/0/0. R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. TRAINING_MODES.postflop.actions.m4 still preview/null (locked until v4.3.0A+). New snapshot `GPT AUDIT/v4.3.0/` (later superseded by `GPT AUDIT/v4.3.0-preA/` after the v4.3.0-preA corrective commit).

🟢 **v4.2.6 committed + pushed** (`86d21c0`). Postflop Beta Feedback Loop + M3 Runtime QA Dashboard (RUNTIME-ONLY). Adds an internal-facing M3 Beta QA Dashboard to the Postflop Academy panel (collapsible, default closed) with: 6-metric grid; 🔍 Weak-spot review would target top 3 reasons; ⚡ Critical-mistake monitor (incl. crb-habit count); 4 by-axis breakdowns (actionReason / recommendedAction / heroHandRole / handClass); 🕘 Last 3 sessions; 📋 Copy M3 Beta QA Snapshot (clipboard-only JSON, privacy `_privacy` field). Sample-size honesty banner appears below 3 sessions / 30 answers. Empty state "Play Module 3 sessions to generate beta feedback." **Module-agnostic helper architecture:** `_pfBetaQAStatsForModule(moduleId)` reusable for M4+. New helpers: `_pfBetaQAStatsForModule`, `_pfM3BetaQAStats`, `_pfM3BetaQAWeakSpotPreview`, `_pfM3BetaQACriticalMonitor`, `_pfM3BetaQADashboardHtml`, `_pfM3BetaQACopySnapshotClick`, `_pfM3BetaSessionLeakHtml`. M3 session summary now surfaces "🔥 Biggest leak this session" + best-next-review hint (with honest "(early signal — small sample)" copy when answers < 5). **Privacy/local-only confirmed:** zero network calls; all data from `_pfHistoryLoad()`; clipboard-only copy. Browser QA mobile 375/360 + desktop 1280: 0 console errors, no horizontal overflow, grid wraps. Programmatic QA: simulated 3-session/15-answer history → all dashboard signals correct, copy snapshot 2,445-char valid JSON. M1/M2/M3 grading regression PASS. **Production audit unchanged 385/0/0** (zero data edits). R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. **Forbidden files untouched:** postflop_scenarios/concepts/taxonomy + tools + ranges + manifest + preflop all byte-identical. **Net diff:** index.html +500 lines (helpers + CSS + Academy mount + session summary leak hint), service-worker.js 1-line VERSION bump, 1 new doc. appVersion + SW v4.2.6. 10-step first-user manual QA script documented in v4.2.6 doc. New doc `docs/specs/postflop-v4.2.6-beta-feedback-loop.md`. New snapshot `GPT AUDIT/v4.2.6/`. Awaiting commit.

🟢 **v4.2.5-doc committed + pushed** (`8b80b37`). State reconciliation flipping v4.2.5 from staged to committed (`55fa676`).

🟢 **v4.2.5 committed + pushed** (`55fa676`). Module 3 Limited Beta UX Polish + Critical-Flag Review (UX POLISH). Three axes: (1) **Strategic critical-flag rebalance** — `check_raise_big` critical rate brought from 47/85 = 55.3% → **26/85 = 30.6%** via 21 surgical downgrades. Each downgrade carries concrete poker rationale (real equity backup, made-hand component, sizing error vs severe punt). 26 KEEPs are uniquely-worst-bad cases (5 naked-pair calls, 4 slowplay teachings, 17 BEST=fold naked-trash). **Zero changes to** answer.best/acceptable/bad, recommendedAction, actionReason, conceptTags, explanation text, board, heroHand, M1/M2 scenarios. Only `answer.critical` arrays edited on 21 M3 scenarios. (2) **ActionReason-keyed weak-spot review** — `_pfCurrentSessionWeakProfile` extended with `targetActionReasons` + `targetHeroHandRoles`; `_pfWeakScenarioScore` adds +50 actionReason boost + +35 heroHandRole boost. M3 weak-spot empty-state toast: "Play Module 3 sessions to unlock BB Defense weak-spot review." Verified: simulated 2-bad-blocker_raise session → top 4 candidates all `blocker_raise` scenarios. (3) **UX polish** — mobile chip wrapping at 320/360/380px micro-breakpoints (no horizontal overflow at any tested width); first-time M3 explainer (collapsible "About Module 3" block on Q1 of first M3 session, hides forever after); M3 Concept Library module-aware review signals (8 alias-concept actionReason mappings); Limited Beta progression hint after 3rd M3 session ("📈 Beta progress unlocked — now review your weakest BB Defense reasons"). **Browser QA mobile 320/360/375 + desktop: 0 console errors, all surfaces render correctly.** Programmatic QA: actionReason weak-spot scoring verified, M3 weak-spot empty-state fires, M1+M2 regression PASS. 5-scenario poker spot-check post-edit: all 5 PASS (3 KEEP critical + 2 downgraded grade correctly). Production audit unchanged 385/0/0. R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. **Forbidden files untouched:** concepts/taxonomy/audit tools/ranges/manifest/preflop all byte-identical. Net diff: index.html +210/-8, service-worker.js 1-line VERSION, scenarios.json 21 critical-array edits only, 1 new tool, 1 new doc. appVersion + SW v4.2.5. New doc `docs/specs/postflop-v4.2.5-module3-limited-beta-ux-polish.md`. New snapshot `GPT AUDIT/v4.2.5/`. Awaiting commit.

🟢 **v4.2.4-doc committed + pushed** (`cc5c209`). State reconciliation flipping v4.2.4 from staged to committed (`81d54c5`).

🟢 **v4.2.4 committed + pushed** (`81d54c5`). Module 3 Limited Beta Runtime Wire (RUNTIME WIRE). Wired Module 3 (Facing C-bet OOP) into the runtime as **"Limited Beta · 85 scenarios"** with the full learning loop. **TRAINING_MODES.m3 flipped:** preview/null/🔒 → secondary/postflop:m3/🛡️/BETA badge with "85 OOP defense scenarios · Limited Beta" hint. New `postflop:m3` route → `startPostflopDrill('pf_flop_cbet_oop_def', 10)`. **Schema normalization layer (architectural keystone):** new `_pfNormalizePostflopChoices` + `_pfNormalizePostflopAnswer` helpers + `_PF_M3_ACTION_LABELS` + `_PF_M3_REASON_LABELS` translate M3's string-form schema to M1/M2's object/array shape uniformly. `classifyPostflopAnswer` + `_findChoiceLabel` route through normalizers; M1/M2 grading byte-identical. **M3 question rendering:** BB-defending prompt framing; 4-axis chip row (handClass/heroHandRole/drawCategory/showdownValue); BB-OOP spot tags; M3-flavored choice guide. **NEW `_pfM3TeachingFeedbackBlocksHtml`:** Recommended Action (BB OOP) + 🎯 Defense Logic (prominent) + Hand Logic + Sizing Logic + 🃏 Blocker note + Range Context + 💡 Takeaway + ⚠️ Common Mistake. **Concept Library +10 M3 entries** (7 native + 3 alias) — new "Module 3 — BB Defense OOP (10 · Limited Beta)" group. **M3 concept drill** + **M3 weak-spot review** wired (M1/M2/M3 pools never cross-contaminate). **NEW `_pfM3RenderSessionAggregations`** by handClass/heroHandRole/actionReason. **NEW `_pfM3MasteryStats` + `_pfM3MasteryProgressHtml`** with Limited Beta thresholds (3/2/9/weak/critical) — title "Module 3 Limited Beta progress (display only)". `_PF_CURRICULUM` m3 entry → beta status + 85 scenarios + 8-line syllabus + "▶ Start Module 3 Limited Beta" button. **Browser QA mobile 375 + desktop: 0 console errors, all M3 surfaces render correctly. Poker QA 12 scenarios (10 M3 + 1 M1 + 1 M2 regression): all 12 PASS.** Production audit unchanged 385/0/0 (no data edits). R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. **No data files modified:** postflop_scenarios.json / postflop_concepts.json / postflop_taxonomy.json / tools/* all byte-identical. **Net diff: index.html +798/-61 (M3 wiring across 6 batches), service-worker.js 1-line VERSION bump.** appVersion + SW v4.2.4. **Module 3 IS NOW PLAYABLE as Limited Beta** from TCC, Postflop Academy curriculum, and Concept Library. New doc `docs/specs/postflop-v4.2.4-module3-limited-beta-wire.md`. New snapshot `GPT AUDIT/v4.2.4/`. Awaiting commit.

🟢 **v4.2.3B committed + pushed** (`e72eef6`). Module 3 Data Polish + Thin-Bucket Completion (DATA POLISH). Polished M3 from 62 → **85 production scenarios** (+23 across 5 new boards + 1 added to existing). **Production audit raised 362/0/0 → 385/0/0**. R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. **5 new boards:** Kh Qh 4s (K-high two-tone broadway), Kh Jh 4h (K-high monotone), Qd 7d 2c (Q-high two-tone dry), Ac Ad 7s (A-high paired), Ts 9s 5d (mid-connected two-tone). **ALL 5 thin-bucket targets MET:** blocker_raise 1→4, domination_fold 2→5, nut_flush_draw 1→3, slowplay_call 3→5, protection_raise 3→6. **15 textbook scenarios promoted to consensus_gto** (sets, naked overcards, nut flush, AA overpair) — first M3 sourceConfidence variety. Difficulty spread improved (8/59/15/3 across 2/3/4/5). Anti-suit-swap discipline maintained: 0 cross-bucket, 0 suit-only dupes, 0 missing uniquenessNotes. 12-scenario poker spot-check PASS. New tools `tools/build-polish-v4.2.3B.ps1` + `tools/migrate-polish-v4.2.3B.ps1`. **CRITICAL — Module 3 still NOT runtime-wired:** TRAINING_MODES.m3 still kind:preview/route:null. Runtime helpers byte-identical. appVersion + SW v4.2.3B. **Limited Beta readiness: M3 IS READY for v4.2.4 runtime wire** with "Limited Beta · 85 scenarios" label and scaled mastery thresholds. New doc `docs/specs/postflop-v4.2.3B-module3-data-polish.md`. New planning seed `docs/specs/postflop-v4.2.3B-module3-polish-seeds.json` (23 scenarios). New snapshot `GPT AUDIT/v4.2.3B/`. Awaiting commit.

🟢 **v4.2.3A committed + pushed** (`14fb380`). Module 3 Data Expansion (DATA EXPANSION). Expanded Module 3 from 24 → **62 production scenarios** (+38 across 8 new board families). **Production audit raised 324/0/0 → 362/0/0** (251 M1 + 49 M2 + 62 M3). R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. **8 new boards:** As9s4d (A-high two-tone), Ks8s3d (K-high two-tone), QsTs6d (Q-high two-tone dynamic), 7s5s3s (low monotone), 8c8d3s (paired low), 9d8c6h (low semi-connected), TcTh6s (paired T), 6c3d2h (rag). **Coverage gap fixes:** pot_odds_defense 0→5, blocker_raise 0→1, slowplay_call 1→3, protection_raise 1→3, domination_fold 1→2, bluff_catch 2→7, nut_flush_draw 0→1. Difficulty spread improved (was all diff 3; now 7 diff 2 + 45 diff 3 + 9 diff 4 + 1 diff 5). **Anti-suit-swap discipline:** every new scenario has uniquenessNote; filler scan = 0 cross-bucket boards / 0 suit-only dupes / 0 missing notes / 0 card collisions. **Strategic 12-spot-check PASS.** New canonical authoring tool `tools/build-expansion-v4.2.3A.ps1` (PSCustomObject-based scenario definitions) + migration tool `tools/migrate-expansion-v4.2.3A.ps1`. Mid-sprint text-integrity caught 1 Thai mojibake byte (≈→U+0E42), fixed inline. **CRITICAL — Module 3 still NOT runtime-wired:** TRAINING_MODES.m3 still kind:preview/route:null. Runtime helpers untouched (only appVersion + SW VERSION lines changed). appVersion + SW v4.2.3A. Training volume assessment: 62 in 50-80 target range; 8 of 9 actionReasons ≥3 (only blocker_raise still thin at 1). v4.2.3B optional vs v4.2.4 Limited Beta runtime wire is project-owner choice. New doc `docs/specs/postflop-v4.2.3A-module3-data-expansion.md`. New planning seed `docs/specs/postflop-v4.2.3A-module3-expansion-seeds.json` (38 scenarios). New snapshot `GPT AUDIT/v4.2.3A/`. Awaiting commit.

🟢 **v4.2.3-doc committed + pushed** (`b4b944f`). State reconciliation flipping v4.2.3 from staged to committed.

🟢 **v4.2.3 committed + pushed** (`e718f07`). Module 3 Migration to Production Data (DATA + AUDIT HARDENING). Migrated all 24 finalized v4.2.0 Module 3 (Facing C-bet OOP) planning seeds into production `postflop/postflop_scenarios.json`. **Production audit raised 300/0/0 → 324/0/0** (251 M1 + 49 M2 + 24 M3). R29 card-notation guard preserved at 0 warnings. M2 + M3 seed audits unchanged. Concepts file +10 (7 M3-native + 3 M3-alias) → 25 total. Taxonomy file extended with `heroHandRole`/`actionReason` arrays + `pf_flop_cbet_oop_def` module entry + `static` textureTag + `review_pending`/`planning_only` auditStatusValues. Production auditor extended with R30-R41 for Module 3 schema (R04/R05 generalized to handle string-form choices/best). Audit-plan doc renumbered R29-R40 → R30-R41 because R29 is owned by v4.2.2D/E text-integrity guard. New migration tool `tools/migrate-module3-v4.2.3.ps1` (idempotent, UTF-8 NO-BOM). Strategic spot-check on 8 critical M3 scenarios verified PASS by reviewer. **CRITICAL — Module 3 still NOT playable, NOT routable, NOT runtime-wired:** TRAINING_MODES.postflop.actions.m3 still `kind: 'preview'`, `route: null`. Runtime helpers byte-identical. Net runtime effect: `App.state.postflop.scenarios` 300 → 324 in memory but no UI surface routes the new 24. appVersion + SW bumped 4.2.2G → 4.2.3 (cache invalidation only). Training volume caveat documented (24 too thin for beta; v4.2.4/5 must expand to ~80–120). New doc `docs/specs/postflop-v4.2.3-module3-migration.md`. New snapshot `GPT AUDIT/v4.2.3/`. v4.2.4 (runtime wire) blocked on volume expansion. Reconciliation post-ship: state docs being flipped from "staged" to "committed + pushed at e718f07" (this commit).

🟢 **v4.2.2G committed + pushed** (`8c150ab`). Command Center Polish + Routing Honesty Pass (UX POLISH). Six v4.2.2F preview-review findings addressed without architecture change. Header sharper ("Choose Your Training Path", helper trimmed 22→12 words). Added `metaPills` field to TRAINING_MODES — Preflop now has 2 status pills (fixes asymmetry vs Postflop). "Boss Tests · Missions" renamed to **"Training Setup"** with hint "Choose drill, boss, or exam mode" — label now matches destination (Drill setup screen). "15 concept drills" → "Browse 15 concept drills" (honest about scroll-to-library). "Mastery + history" → "View mastery + history". Visual polish: selected corner indicator dot, panel top-edge accent line, stronger primary CTA gradient, unselected opacity 0.72. Architecture 100% preserved (TRAINING_MODES + 4 helpers unchanged, M3 still kind:preview/route:null, no onclick on M3 tile). Mobile 375 verified clean. appVersion + SW v4.2.2G. Production audit unchanged 300/0/0. M2 + M3 seed audits unchanged. New doc `docs/specs/postflop-v4.2.2G-command-center-polish.md`. UI captures in `GPT AUDIT/screenshots/v4.2.2G/`. v4.2.3 still paused. Awaiting commit.

🟢 **v4.2.2F committed + pushed** (`7680db9` + doc `159dd6f`). Product Mode System Foundation + Premium Home Command Center. Replaces v4.1.8 misleading home tabs with real foundation: `TRAINING_MODES` registry + 4 helpers (`getTrainingMode`/`setTrainingMode`/`getTrainingModeMeta`/`runTrainingModeAction`) + premium `.tcc-shell` Command Center with segmented mode selector + mode-aware command panel + safe CTA routing. **Preflop panel:** 6 actions (Quick/Deep/Weakness/Marginal Drill + Browse + Boss·Missions). **Postflop panel:** 6 actions (M1, M2 BETA, Concepts, Weak Spot, Progress, **M3 preview SOON**). M3 explicitly route=null, click shows toast, NOT playable. Mode persists via `App.state.settings.trainingMode`. Mobile 375×812 verified clean. Permanent UX principle documented (5-property rule for any mode/tab UI). Bottom-nav rewrite deferred to v4.3.x. No data/audit/preflop touched. Production audit 300/0/0 unchanged. M2 + M3 seed audits unchanged. appVersion + SW v4.2.2F. New doc `docs/specs/postflop-v4.2.2F-product-mode-system-foundation.md`. v4.2.3 migration still paused. Awaiting commit.

🟢 **v4.2.2E committed + pushed** (`3cc3704`). Final Text Integrity Repair + R29 Hardening. Third-order correction. v4.2.2D's combo-shorthand rule over-triggered on board references that escaped the board-rebuild, producing fake suited-hand strings like `KTs 2` (was `Kh Td 2s`). Plus 3 other classes leaked: `A — X` / `K — X` flush prose, `5 — /7 — /8` rank-x list. **14 scenarios / 18 text-field edits.** Repairs use `board.cards` to rebuild boards. R29 guard hardened with 3 new patterns (board_collapse, rank_dash_X, rank_dash_slash) → 6 total, verified 6/6 positive + 7/7 negative test cases. **15-field strategic integrity verified 0 changes.** Manual poker spot-check on 5 critical scenarios PASS (Kh Td 2s, Th 8h 3h, 8s 7s 5s — all strategic intent preserved). appVersion + SW v4.2.2E. Production audit unchanged 300/0/0, R29 = 0 warnings (true negative now). M2 seed audit 24/0/8. M3 seed audit 24/0/0. New doc `docs/specs/postflop-v4.2.2E-final-text-integrity-repair.md`. Awaiting commit.

🟢 **v4.2.2D committed + pushed** (`f53b425`). Postflop Card / Suit Notation Semantic Repair. 263 scenarios / 525 text edits. R29 guard added (3 patterns). 15-field strategic integrity verified. v4.2.2C blanket-replaced mojibake runs with em-dashes — including original suit symbols, leaving semantic damage like "BTN with K — K — on 6 — 5 — 4 —" and "A — -x". v4.2.2D applies context-aware repair using `board.cards` + `heroHand` arrays to reconstruct meaning. **263 scenarios touched, 525 text-field edits across 2 passes.** All 4 suspicious card-notation patterns reduced to 0. 186 legitimate em-dashes preserved as sentence punctuation. **15-field strategic integrity check: 0 changes** (answer keys / actionReason / conceptTags / auditStatus / etc. all untouched). Added R29 audit guard rule (warning-only) to `tools/audit-postflop-ps.ps1` detecting the 3 suspicious patterns to prevent regression. Browser QA mobile screenshot confirms clean prompts ("On Ah Kd 5c", "With AhKh on As 8d 3h"). appVersion + SW bumped to v4.2.2D. Production audit unchanged 300/0/0. M2 seed audit unchanged 24/0/8. M3 seed audit unchanged 24/0/0. New doc `docs/specs/postflop-v4.2.2D-card-suit-notation-repair.md`. Awaiting commit.

🟢 **v4.2.2C committed + pushed** (`8896504`). Runtime Text Encoding / Mojibake Hotfix. Cleaned 292/300 production scenarios + 8 ranges.json lines of CP874 mojibake (em-dash + suit symbols round-tripped through CP874 → long Thai-character runs). Source data fix is durable. Added `_pfFixMojibake` safety net to M1 expandable explanation sections that v4.0.10 hotfix missed. Caught + fixed a regression where the existing reverser stripped clean em-dashes (mapped 0x2014 → byte 0x97 which is invalid UTF-8 leading byte → fallback emitted U+0097 C1 control); fix tracks original chars in parallel buffer to restore on decode failure. Fixed `.install-banner` CSS to use `env(safe-area-inset-top)` so iOS PWA standalone mode no longer crowds the status bar. **appVersion + SW bumped to v4.2.2C.** Browser QA at mobile 375x812: 0 console errors, 0 mojibake in DOM, em-dashes preserved, install banner properly inset. Production audit unchanged 300/0/0. M2 seed audit unchanged 24/0/8. M3 seed audit unchanged 24/0/0. **No M3 productionization, no strategic content / answer-key changes.** New doc `docs/specs/postflop-v4.2.2C-runtime-text-encoding-hotfix.md`. Awaiting commit.

🟢 **v4.2.2B committed + pushed** (`1ee6cc1`). Module 3 Post-Commit Raw Verification + Migration Readiness Gate. 3 defects fixed (F6.2 conceptTags, F3.3 acceptable, summary metadata stale). Migration Path A green-lit. **Found and fixed 3 defects:** (1) F6.2 stale conceptTags `[check_raise_value, value_raise]` → `[bluff_catchers, pot_control, value_raise]` (matches v4.2.2 slowplay flip); (2) F3.3 acceptable list had `protection_raise` which doesn't fit pure-draw Td9d → moved to bad; (3) Top-level summary metadata block stale from v4.2.0 (missing slowplay_call, wrong byBestAction counts, wrong byReviewStatus) → full recompute + added `metadataRecomputedAt` freshness marker. All v4.2.2 strategic claims (F1.3, F5.3, F6.2, F5.4, F6.4) verified correct. slowplay_call vocab consistent across 6 locations. **Training Volume Gate:** 24 ACCEPTABLE for planning lock + production migration; NOT for stable runtime. Per-concept depth: only equity_realization_oop (8) at threshold; 6 of 7 below. **Migration decision: Path A — proceed with v4.2.3.** Recommended v4.2.3A data expansion sprint between v4.2.3 (migration) and v4.2.4 (runtime wire) to bring M3 from 24 → 40+ before playable beta. M3 seed audit 24/0/0 PASS clean. Production audit unchanged 300/0/0. M2 seed audit unchanged 24/0/8. New doc `docs/specs/postflop-v4.2.2B-module3-postcommit-verification.md`. Awaiting commit.

🟢 **v4.2.2 committed + pushed** (`14bbd82`). Module 3 Final Strategic Review + Planning Commit Lock. 22 FINAL_PASS + 2 FINAL_WARN + 0 BLOCKED. F1.3 + F5.3 UN-softened, F6.2 flipped to slowplay_call, vocab expanded 8 → 9 reasons. All 24 → v4.2.0_final.

🟢 **v4.2.1 committed + pushed** (`70ee74d`). Module 3 Seed Auditor + Initial Strategic Review. New script (Option A). 13 mech defects caught + fixed batch. 17 PASS + 7 WARN + 0 FAIL. All 24 flipped to v4.2.0_seed_reviewed.

🟢 **v4.2.0 committed + pushed** (`515a3c1`). Module 3 Architecture + Seed Plan. 5 docs in `docs/specs/postflop-v4.2.0-module3-*`. Planning-only.

🟢 **v4.1.9 committed + pushed** (`454c470`). Module 2 Data Expansion + Tester Pass. Module 2 grows from 35 to 49 production scenarios. Per-concept primary-tag depth target hit (8/8/8/8/11). Production audit raised 286/0/0 → 300/0/0. Browser QA 30/30 PASS. appVersion + SW v4.1.9.

🟢 **v4.1.8 committed + pushed** (`5eb12ac`). Home Mode Tabs + M2 Mastery + Concept-Pool Depth Audit. Home tabs (Preflop/Postflop entry tiles), M2 mastery checklist (5 criteria parallel to M1), M2 session summary aggregation by handClass+actionReason, depth audit doc identifying v4.1.9 expansion targets. Browser QA 32/32 PASS. appVersion + SW bumped to v4.1.8. Production audit 286/0/0 unchanged.

🟢 **v4.1.7 committed + pushed** (`d48ffa9`). Module 2 Curriculum Playable Beta — Module 2 fully playable from Curriculum + Concept Library; hand-aware question/answer rendering; M2 weak-spot routes to M2 pool; runtime loads 286. Module 2 is now playable. Final seed review 24/24 PASS. 24 v4.1.2 seeds flipped to `auditStatus: approved`; runtime now loads 286 (was 262). New M2 helpers: `getModule2Scenarios`, `_pfM2TeachingFeedbackBlocksHtml`. Updated: `startPostflopDrill` / `_pfChoiceGuide` / `_pfBuildQuestionPrompt` / `renderPostflopQuestion` / `renderPostflopAnswer` / `renderPostflopComplete` / `startPostflopConceptDrill` / `startPostflopWeakSpotReview` / `_pfModuleStatus` / `_pfModuleCardHtml`. M2 weak-spot review routed to M2 pool (no contamination). 5 M2 concepts in library flipped from previewOnly to drillable. Module 2 curriculum card → "▶ Start Module 2 Beta" button calling `startPostflopDrill('pf_flop_cbet_ip', 12)`. New CSS for hero card row, M2 chips, M2 action block, beta status pill, secondary syllabus button. Browser QA 35/35 PASS (M2 from curriculum + M2 concept drill + M2 weak-spot stays in M2 pool + M2 summary + M1 normal drill + M1 concept drill + preflop unaffected + mobile + console clean). appVersion 4.1.6 → 4.1.7, SW VERSION v4.1.6 → v4.1.7. Production audit unchanged 286/0/0. M2 seed audit unchanged 24/0/8. Two new docs in `docs/specs/`. Awaiting commit.

🟢 **v4.1.6 committed + pushed** (`ca3ea31`). Concept Library Module 2 Bridge (Path A preview-only). `_PF_CONCEPT_LIBRARY` extended with 5 Module 2 concepts (each `module: 'm2'` + `previewOnly: true`). Library renders Module 1 / Module 2 grouped sections; M2 cards show orange-tinted "trained in Module 2" tag + "🔒 Coming in Module 2 Beta" lock badge instead of drill button. Defense-in-depth: `startPostflopConceptDrill` refuses preview-only keys with toast. **Module 2 still NOT playable from curriculum** — `startPostflopDrill('pf_flop_cbet_ip', ...)` never called from runtime. Browser QA 25/25 PASS (M1 drill works, M2 refused, preflop unaffected, curriculum unchanged, mobile clean, console 0 errors). appVersion bumped 4.1.1 → 4.1.6; service-worker VERSION bumped v4.1.1 → v4.1.6. Production audit unchanged 286/0/0. Module 2 seed audit unchanged 24/0/8. No scenario data changes. Awaiting commit.

🟢 **v4.1.5 committed + pushed** (`38cf34b`). Module 2 Seed Cleanup + Baseline Migration + Audit Extension. Production audit gate raised from 262/0/0 to 286/0/0. 11 baseline migrated + 24 seeds appended. 5 new concepts added. R18-R28 added to production auditor. Module 2 still not playable.

🟢 **v4.1.4 committed + pushed** (`c1df014`). Module 2 Seed Review + Baseline Migration Decision (planning) — strategic 24-seed re-review (20 PASS / 4 WARN / 0 FAIL); Option C (Refactor/Migrate) chosen for baseline 11.

🟢 **v4.1.3 committed + pushed** (`eafdf6d`). Module 2 Audit Tooling — `tools/audit-postflop-module2-seed.ps1` (~620 LOC) implements 30 hard rules + 7 soft warnings + 9 coverage axes. Production auditor untouched.

🟢 **v4.1.2 committed + pushed** (`c6a24ac`). Module 2 Architecture + Data Plan — 5 planning docs: architecture, schema/taxonomy, 24 seed scenarios (21 PASS / 3 WARN / 0 FAIL), audit plan, GPT review package. Integrated Academy Path with applied-decision distinction.

🟢 **v4.1.1 committed + pushed** (`cf088d1`). Postflop Concept Library Drill Actions — each of the 10 Concept Library cards is now a one-tap entry into a focused 12-question Module 1 drill. Concept-mode badge on question screen + dedicated summary header. Optional Review-signal pill from latest session. 45/45 QA. Audit 262/0/0.

🟢 **v4.1.0 committed + pushed** (`843fa76`). Postflop Academy Foundation — curriculum map + mastery checklist + concept library + recommendation engine.

| Metric | Value |
|---|---|
| v4.0.0 planning | ✅ committed (`7849741`) + pushed |
| v4.0.1 implementation | ✅ committed (`2593e5c`) + pushed |
| `App.postflop` runtime | ✅ live; 31/31 approved scenarios loaded; frozen API |
| Browser QA result | ✅ 9/9 PASS (live verified at `http://localhost:8765/index.html`) |
| v4.0.2 planning sprint | 🟡 in progress — Architecture / UX / Data review / QA / Consolidation |
| v4.0.2 production code | ❌ not started; planning only |

---

## Parallel Workstreams

| Workstream | Owner | Status | Notes |
|---|---|---|---|
| Architecture Package | Architecture Subagent | ✅ Done + committed | `postflop/ARCHITECTURE.md` (21.4 KB) — committed in `7849741` |
| GTO Data Package | GTO Data Subagent | ✅ Done + committed | scenarios + concepts + taxonomy clean — committed in `7849741` |
| Audit Package | Audit Subagent | ✅ Done + committed | rules + browser viewer + sample report — committed in `7849741` |
| UX / UI Plan | UX Subagent | 🟡 Partial | Plan in `postflop/postflop_schema.md` § "UI / UX plan"; deeper wireframe pass deferred to v4.0.4 |
| Orchestrator Workflow Files | Orchestrator | ✅ Done + committed | All in `7849741` |
| v4.0.0 Planning Commit + Push | Orchestrator | ✅ Done | `7849741` pushed to origin/main |
| v4.0.1 Brief | Orchestrator | ✅ Done + committed | `docs/specs/brief-v4.0.1-schema-loader.md` — staged with v4.0.1 |
| v4.0.1 Implementation | DEV Integration Agent | 🟡 Staged | per `brief-v4.0.1-schema-loader.md`, awaiting commit approval |
| v4.0.1 QA | QA Agent | 🟡 Partial | All non-browser items pass; live browser load + console verification still needed |
| v4.0.2 Brief (Module 1 UI) | Orchestrator | 🚫 Blocked | Wait for v4.0.1 commit approval |

---

## Blockers

1. **Human review of v4.0.1 staged diff** (`index.html` +71/-3, `service-worker.js` +5/-1) — confirm the diff matches the brief exactly.
2. **Live browser verification of loader** — open the app, check DevTools console for `[postflop] loaded 31/31 scenarios (schema 1.0.0)`, run `App.postflop.ready` etc. (QA items 2–7 in v4.0.1 brief).
3. **Approval to commit** the staged v4.0.1 changes with message `v4.0.1: add postflop schema loader and audit gate`.
4. **Resolution of 5 open questions** (carried in `PROJECT_STATE.md` § 8) — can be deferred until v4.0.2 if not blocking.

---

## Next Actions (in order)

1. ✅ Workflow files created.
2. ✅ v4.0.0 planning package committed (`7849741`) and pushed.
3. ✅ v4.0.1 brief written and approved.
4. ✅ v4.0.1 implementation: 4 edits in `index.html` + 2 edits in `service-worker.js`. Audit re-confirmed 31/0/0.
5. ✅ State files updated (PROJECT_STATE.md, TASK_BOARD.md).
6. ⏸️ Human reviews staged diff, then approves the v4.0.1 commit.
7. ⏸️ Orchestrator commits with message `v4.0.1: add postflop schema loader and audit gate`.
8. ⏸️ Push happens only on a separate explicit "push" instruction.
9. ⏸️ On v4.0.1 commit + push: prepare `docs/specs/brief-v4.0.2-module1-board-texture-trainer.md` (planning only — first actual UI work).

---

## Recently Completed

- 2026-05-06: v4.2.6 Postflop Beta Feedback Loop + M3 Runtime QA Dashboard STAGED. Internal-facing dashboard mounted in Postflop Academy (6-metric grid, weak-spot preview, critical monitor, 4 by-axis breakdowns, last 3 sessions, copy snapshot button). Module-agnostic helpers reusable for M4+. Session summary "🔥 Biggest leak this session" hint. Privacy/local-only (zero network calls). Browser QA 375/360 + desktop clean. Programmatic QA verified. **Production audit unchanged 385/0/0; M3 strategy data byte-identical.** appVersion + SW v4.2.6. Awaiting commit.
- 2026-05-06: v4.2.5-doc reconciliation COMMITTED (`8b80b37`) + pushed. Flipped v4.2.5 docs from staged to committed (`55fa676`).
- 2026-05-06: v4.2.5 Module 3 Limited Beta UX Polish + Critical-Flag Review COMMITTED (`55fa676`) + pushed. crb critical rate 55.3% → 30.6% via 21 surgical downgrades (poker-justified). 26 KEEPs uniquely-worst-bad. ActionReason-keyed weak-spot review wired (top 4 = all blocker_raise verified). M3 first-time explainer + module-aware concept signals + Limited Beta progression hint. Mobile chip wrap 320/360/380px clean. **Forbidden files untouched.** Production audit unchanged 385/0/0. M1/M2 regression PASS. appVersion + SW v4.2.5. Awaiting commit.
- 2026-05-06: v4.2.4-doc reconciliation COMMITTED (`cc5c209`) + pushed. Flipped v4.2.4 docs from staged to committed (`81d54c5`).
- 2026-05-06: v4.2.4 Module 3 Limited Beta Runtime Wire COMMITTED (`81d54c5`) + pushed. Full learning loop wired: TCC tile flip (preview→available BETA), postflop:m3 route, schema normalization helpers (string-form M3 vs object/array M1/M2), `getModule3Scenarios`, M3 question rendering with BB-OOP framing + 4-axis chips, `_pfM3TeachingFeedbackBlocksHtml` with prominent defenseLogic, 10 M3 concepts in library, M3 concept drill + weak-spot review, M3 session aggregation, M3 Limited Beta mastery checklist (3/2/9/weak/critical scaled thresholds), curriculum card with Start button. **No data files touched** (postflop_scenarios.json + tools/* byte-identical). Production audit unchanged 385/0/0. Browser QA mobile 375 + desktop clean. Poker QA 12 scenarios PASS. M1/M2 regression-tested PASS. appVersion + SW v4.2.4. **Module 3 IS NOW PLAYABLE as Limited Beta.** Awaiting commit.
- 2026-05-06: v4.2.3B Module 3 Data Polish + Thin-Bucket Completion COMMITTED (`e72eef6`) + pushed. M3 grows 62 → 85 scenarios (+23 across 5 new boards + 1 added to existing 8c8d3s). Production audit raised 362/0/0 → 385/0/0 (R29 = 0 warnings preserved). ALL 5 thin-bucket targets met: blocker_raise 1→4, domination_fold 2→5, nut_flush_draw 1→3, slowplay_call 3→5, protection_raise 3→6. 15 textbook scenarios promoted to consensus_gto. Difficulty spread improved (8/59/15/3). Anti-suit-swap filler scan clean. 12-scenario poker spot-check PASS. **Module 3 still NOT runtime-wired; TRAINING_MODES.m3 still kind:preview/route:null.** appVersion + SW v4.2.3B. **Limited Beta readiness: M3 ready for v4.2.4 runtime wire.** Awaiting commit.
- 2026-05-06: v4.2.3A Module 3 Data Expansion COMMITTED (`14fb380`) + pushed. M3 grows 24 → 62 scenarios (+38 across 8 new boards). Production audit raised 324/0/0 → 362/0/0 (R29 = 0 warnings preserved). Coverage gap fixes: pot_odds_defense 0→5, blocker_raise 0→1, slowplay_call 1→3, protection_raise 1→3, domination_fold 1→2, bluff_catch 2→7, nut_flush_draw 0→1. Difficulty spread improved (was all diff 3; now 7/45/9/1 across 2/3/4/5). Anti-suit-swap discipline enforced via filler scan. 12-scenario poker spot-check PASS. New canonical authoring tool + migration tool. **Module 3 still NOT runtime-wired; TRAINING_MODES.m3 still kind:preview/route:null.** appVersion + SW v4.2.3A. Awaiting commit.
- 2026-05-06: v4.2.3-doc state reconciliation COMMITTED (`b4b944f`) + pushed. Flipped v4.2.3 docs from staged to committed (`e718f07`).
- 2026-05-06: v4.2.3 Module 3 Migration to Production Data COMMITTED (`e718f07`) + pushed. 24 M3 seeds migrated to production. Production audit raised 300/0/0 → 324/0/0 (R29 = 0 warnings preserved). Concepts +10 → 25. Taxonomy extended (heroHandRole, actionReason arrays, pf_flop_cbet_oop_def module, static textureTag, review_pending/planning_only auditStatus). Auditor extended R30-R41. Audit-plan renumbered R29-R40 → R30-R41. New `tools/migrate-module3-v4.2.3.ps1`. Strategic 8-scenario spot-check PASS. **Module 3 still NOT runtime-wired; TRAINING_MODES.m3 still kind:preview/route:null.** appVersion + SW v4.2.3. State reconciliation completed in follow-up commit.
- 2026-05-06: v4.2.2G Command Center Polish + Routing Honesty STAGED. 6 polish items (sharper header, Preflop metaPills, Training Setup rename, sharper Concept Library hint, selected dot indicator, panel top accent, stronger primary CTA). Architecture preserved 100%. Mobile QA clean. UI captures in screenshots/v4.2.2G/.
- 2026-05-06: v4.2.2F Product Mode System Foundation + Premium Home Command Center COMMITTED (`7680db9` + doc `159dd6f`) + pushed. TRAINING_MODES registry + 4 helpers + premium TCC Command Center. Preflop and Postflop panels with 6 actions each. M3 preview-only (not playable). Mode persists. Mobile 375 verified. Permanent UX 5-property principle documented. Bottom-nav deferred. No data touched. v4.2.2F versions.
- 2026-05-06: v4.2.2E Final Text Integrity Repair + R29 Hardening COMMITTED (`3cc3704`) + pushed. 14 scenarios / 18 edits fixing v4.2.2D over-trigger (KTs 2 board-collapse + A — X flush prose + 5 — / 7 — list). R29 hardened to 6 patterns verified by 13 test cases. 15-field strategic integrity 0 changes. 5-scenario poker spot-check PASS. R29 = 0 warnings (true negative). v4.2.2E versions.
- 2026-05-06: v4.2.2D Card/Suit Notation Semantic Repair COMMITTED (`f53b425`) + pushed. Context-aware repair of v4.2.2C over-normalization. 263 scenarios / 525 text-field edits. All 4 suspicious patterns now 0; 186 legitimate em-dashes preserved. 15-field strategic integrity verified 0 changes. New R29 audit guard. appVersion + SW v4.2.2D. Audits unchanged 300/0/0, 24/0/8, 24/0/0.
- 2026-05-06: v4.2.2C Runtime Text Encoding Hotfix COMMITTED (`8896504`) + pushed. Cleaned 292/300 production scenarios + ranges.json of CP874 mojibake. Added `_pfFixMojibake` safety net to M1 explanation render. Fixed reverser regression that stripped clean em-dashes. Fixed install banner safe-area-inset-top. appVersion + SW bumped to v4.2.2C. Mobile QA verified clean. No strategic changes.
- 2026-05-06: v4.2.2B Module 3 Post-Commit Verification COMMITTED (`1ee6cc1`) + pushed. 3 defects fixed (F6.2 conceptTags, F3.3 acceptable, stale summary). Migration Path A green-lit. Verified v4.2.2 claims against raw JSON; caught 3 defects (F6.2 stale conceptTags, F3.3 noisy acceptable, stale summary metadata) and fixed in-place. Training Volume Gate: 24 OK for planning + migration but not stable runtime. Migration Path A green-lit; v4.2.3A expansion recommended before v4.2.4 runtime. M3 audit 24/0/0 PASS clean. Production unchanged 300/0/0. M2 unchanged 24/0/8.
- 2026-05-06: v4.2.2 Module 3 Final Strategic Review + Planning Commit Lock COMMITTED (`14bbd82`) + pushed. 22 FINAL_PASS + 2 FINAL_WARN + 0 BLOCKED. All 24 flipped to v4.2.0_final. 3 second-pass changes (2 UN-softens of v4.2.1 + F6.2 flip + slowplay_call vocab re-introduction). Training Quality assessment: M3 needs major data sprint (v4.3.x). M3 audit unchanged 24/0/0. Production unchanged 300/0/0. M2 unchanged 24/0/8.
- 2026-05-06: v4.2.1 Module 3 Seed Auditor + Strategic Review COMMITTED (`70ee74d`) + pushed. New `tools/audit-postflop-module3-seed.ps1` (Option A). M3 audit caught 13 mechanical defects (critical ⊈ bad); fixed batch. Strategic review: 17 PASS + 7 WARN + 0 FAIL. 7 targeted JSON edits. All 24 scenarios flipped to reviewStatus=v4.2.0_seed_reviewed. Final audit: 24/0/0 PASS clean. New review doc. No production touched. No version bumps.
- 2026-05-06: v4.2.0 Module 3 Architecture + Seed Plan COMMITTED (`515a3c1`) + pushed. 5 new docs in `docs/specs/postflop-v4.2.0-module3-*`: architecture, schema-taxonomy, 24-seed JSON (6 board families × 4 hands; 18 action + 6 reason), audit plan (38 hard rules + 7 warnings), GPT review package (per-scenario risk flags). Villain sizing scoped to bet_small for v4.2.0; reason set trimmed 11 → 8. No production touched. No version bumps. Production audit unchanged 300/0/0. M2 seed audit unchanged 24/0/8.
- 2026-05-06: v4.1.9 Module 2 Data Expansion + Tester Pass COMMITTED (`454c470`) + pushed. M2 grows 35 → 49 production scenarios via 14 new scenarios on 14 new boards (blocker_pressure +4, value_betting +3, range_advantage_stab +3, pot_control +2, give_up_strategy +2). All 14 PASS final GPT/strategic review; flipped to auditStatus=approved + reviewStatus=v4.1.9_gpt_reviewed. Per-concept primary-tag depth target hit (8/8/8/8/11). Production audit raised 286/0/0 → 300/0/0. Browser QA 30/30 PASS. appVersion + SW v4.1.9. No schema/taxonomy/audit-script changes.
- 2026-05-05: v4.1.8 Home Mode Tabs + M2 Mastery + Concept-Pool Depth Audit COMMITTED (`5eb12ac`) + pushed. Home tabs (Preflop/Postflop), M2 mastery checklist parallel to M1, M2 summary aggregation by handClass+actionReason, depth audit doc. 32/32 QA. appVersion + SW v4.1.8.
- 2026-05-05: v4.1.7 Module 2 Curriculum Playable Beta COMMITTED (`d48ffa9`) + pushed.
- 2026-05-05: v4.1.7 Module 2 Curriculum Playable Beta STAGED. Module 2 is playable. 24 seeds flipped to approved (runtime loads 286). All M2 surfaces wired: curriculum start button, hand-aware question/answer rendering, all 5 M2 concept drills enabled, M2 weak-spot review (M2-only pool), M2 session summary. Browser QA 35/35 PASS. appVersion + SW bumped to v4.1.7.
- 2026-05-05: v4.1.6 Concept Library Module 2 Bridge COMMITTED (`ca3ea31`) + pushed. Path A preview-only.
- 2026-05-05: v4.1.5 Module 2 Seed Cleanup + Baseline Migration + Audit Extension COMMITTED (`38cf34b`) + pushed. Production audit gate raised to 286/0/0. 11 baseline migrated + 24 seeds appended. 5 new concepts added. R18-R28 added to production auditor.
- 2026-05-05: v4.1.4 Module 2 Seed Review + Baseline Migration Decision COMMITTED (`c1df014`) + pushed. Strategic 24-scenario re-review (20 PASS / 4 WARN / 0 FAIL). Baseline-11 migration decision: Option C (Refactor/Migrate).
- 2026-05-05: v4.1.3 Module 2 Audit Tooling COMMITTED (`eafdf6d`) + pushed. New seed auditor catches all v4.1.2 mechanical-error categories. 0 hard errors / 11 documented warnings / 15 PASS / 9 WARN / 0 FAIL. Production audit still 262/0/0.
- 2026-05-05: v4.1.2 fix-pass STAGED. Corrected 5 mechanical errors in seed scenarios (flush-vs-flush-draw mis-counts on monotone/two-tone boards: #11 made straight wrongly labelled combo_draw; #13 backdoor only wrongly labelled NFD with action over-aggression; #21 set wrongly assigned to NFD; #22 K-FD explanation claimed made flush; #24 low FD explanation claimed made 6-flush). Applied 5 labelling improvements. Schema vocabulary extended with `straight`, `flush`, `nut_flush` and a "suit-count discipline" rule. Post-fix verdict: 21 PASS / 3 WARN / 0 FAIL.
- 2026-05-05: v4.1.2 Module 2 Architecture + Data Plan STAGED (planning-only). 5 docs in `docs/specs/postflop-v4.1.2-module2-*` (architecture / schema-taxonomy / 24 seed scenarios / audit plan / GPT review package). Integration model = Integrated Academy Path with applied-decision distinction (Module 1 = "Read the board", Module 2 = "Choose the action with a hand"). Audit still 262/0/0. No production change. No commit.
- 2026-05-05: v4.1.1 Postflop Concept Library Drill Actions COMMITTED (`cf088d1`) + pushed. Each of 10 concept cards becomes an actionable 12-question focused drill entry point. New mode='concept' state + blue Concept Drill badge + dedicated summary. Optional Review-signal pill from latest session weak concepts.
- 2026-05-04: v4.1.0 Postflop Academy Foundation COMMITTED (`843fa76`) + pushed. Curriculum map + mastery + recommendation engine + concept library.
- 2026-05-04: v4.0.12 Postflop Drill Weak Spots Button COMMITTED (`79cfc2a`) + pushed. End-to-end teaching loop closed.
- 2026-05-04: v4.0.11 Postflop Session Learning Summary COMMITTED (`a2e4fae`) + pushed. Quality label + strongest/weakest concepts + family pattern notes + recommended next move.
- 2026-05-04: v4.0.10 Postflop Card Text Encoding Hotfix COMMITTED (`53eae80`) + pushed. CP874 mojibake reverser + clean prompt rebuilder.
- 2026-05-04: v4.0.9 Postflop Teaching Polish COMMITTED (`c38aafc`) + pushed. M1/M3/M4/L1/M2 fixes addressing v4.0.8 QA gaps.
- 2026-05-04: v4.0.8 Postflop Teaching Layer COMMITTED (`479b775`) + pushed.
- 2026-05-04: v4.0.8 Extended QA pass — 26/26 regression checks passed; identified 4 medium + 2 low severity issues feeding v4.0.9.
- 2026-05-04: v4.0.7 Module 1 expansion 20→251 scenarios COMMITTED (`1f5fe99`) + pushed. Audit 0/0. Template-correction + micro-fix incorporated.
- 2026-05-04: v4.0.7-template-correction STAGED (folded into v4.0.7 commit). Generic two_tone family split into 5 sub-families per rank-class + connectedness. paired_mid wording fixed.
- 2026-05-04: v4.0.7-hardened (superseded by template-correction pass). SourceConfidence rebalanced (97/146/0/0). SuitTexture rebalanced (130/98/15). Tracked tools/generate-postflop-module1.ps1 + tools/audit-postflop-ps.ps1. 30-sample GPT review package.
- 2026-05-04: v4.0.7 initial staging (superseded).
- 2026-05-04: v4.0.6 postflop repeat control + local history STAGED. Awaiting commit approval.
- 2026-05-04: v4.0.5-data committed (`87c741e`) + pushed (#14 sourceConfidence honesty downgrade).
- 2026-05-04: v4.0.5 GTO Validation Pass complete (report + patch plan committed alongside v4.0.5-data).
- 2026-05-04: v4.0.4 critical hotfix committed (`519df53`) + pushed; postflop answer interaction now works on desktop + mobile.
- 2026-05-04: v4.0.3 polish committed (`25fb45e`) + pushed.
- 2026-05-04: v4.0.2 deployed live to Netlify; tester real-play surfaced 4 UX issues feeding v4.0.3 + critical onclick bug feeding v4.0.4.
- 2026-05-04: v4.0.2 Module 1 UI committed (`5d21128`) + pushed to origin/main.
- 2026-05-04: v4.0.2-data seed fix committed (`473ce9a`) + pushed.
- 2026-05-04: v4.0.2 planning sprint committed (`377c844`) + pushed.
- 2026-05-04: v4.0.1 schema loader committed (`2593e5c`) + pushed; live browser QA 9/9 PASS.
- 2026-05-04: v4.0.0 planning package committed (`7849741`) + pushed to origin/main.
- 2026-05-04: Orchestrator workflow files created (PROJECT_STATE.md, AGENTS.md, TASK_BOARD.md, docs/, tools/audit-postflop.js).
- 2026-05-04: Postflop planning package (9 files, ~206 KB) — Audit Subagent verified 0/0.
- 2026-05-04 (prior): v3.8.2 shipped to Netlify (Viewport-Dominant Field FX).
- 2026-05-04 (prior): v3.8.1 (Anime Battle Field) shipped.
- 2026-05-04 (prior): v3.8.0 (Field FX pivot + lifecycle bug fix) shipped.

---

## Discovered Work (out-of-scope; not started)

Items found during v4.0.0 work that are **not** in scope for v4.0.0 but should be tracked:

- **(low priority)** Audit Subagent could run automatically on a pre-commit hook. Currently runs only when human opens the audit page or runs the Node script. → Tracked in `RISKS.md` R-07; deferred to v4.0.5.
- **(low priority)** GitHub Action to run audit on PRs touching `postflop/*.json`. → Tracked in `RISKS.md` R-07; deferred to v4.1.
- **(medium priority)** Concept coverage gaps: `nut_advantage_shift`, `ip_advantage`, `equity_realization` have 0 scenarios. → Tracked in `audit-report-sample.md` "v4.1 expansion targets".
- **(medium priority)** Q-high and J-high boards underrepresented (only 2 scenarios each). → Tracked in `RISKS.md` R-05; planned for v4.1 data expansion.

---

## Do Not Start Yet

These tasks are deliberately **blocked** until v4.0.0 planning is approved:

- ❌ Full postflop drill integration in `index.html`
- ❌ Postflop boss tests / missions / overall exams
- ❌ Postflop cosmetic rewards / Answer FX / Aura tie-ins
- ❌ Postflop Collection Book extensions
- ❌ Service worker version bump
- ❌ Module 3 (BB Defense vs C-bet) data — v4.1 territory
- ❌ Turn / river modules — v4.2+ territory
- ❌ ICM-aware adjustments — out of v4.0.0 scope
- ❌ Multi-way post-flop — out of v4.0.0 scope

Anyone (human or subagent) starting these tasks before approval should be redirected by Orchestrator.

---

## Update protocol

- Each subagent updates its own row when status changes (✅ done / 🟡 in progress / 🚫 blocked / ⏸️ paused).
- New blockers go under "Blockers" with clear ownership.
- Discovered work goes under "Discovered Work" — never silently fixed.
- Orchestrator does the bigger reorganizations (status sync after a session, archiving completed epics).
