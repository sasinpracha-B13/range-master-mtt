# Project State — Range Master MTT

> **READ THIS FIRST** before doing any work in this repo.
> Subagents: this file is your single source of truth for project context, current scope, and what is/is not allowed.
> Last updated: 2026-07-05 (v4.5.0-game G3 Tournament Mode — 8 levels × 5 hands, PIN-1 schedule invariant (perfect-play floor +15), ladder never scales regret, Range Reveal w/ negation guard + `_PF_REVEAL_SUPPRESS` (510 hit-list line-reviewed: 50 banded / 2 NEG / 1 LIST-SUPPRESSED / 460 no-chip), ghost run, trophies, bust attribution. Data byte-identical 510/0/0. Next: G4 — M6 River Betting IP program, spec/architecture first.).

---

## 1. Current Version

- **v4.5.0-game G3 Tournament Mode (RUNTIME/GAME-LAYER ONLY; DATA BYTE-IDENTICAL 510/0/0)**: Owner-approved spec (answered all 7 advisor criteria) built + QA'd; **both PINs closed**. **PIN 1 (perfect play cannot bust): schedule invariant** — total drift 5×Σ(`_PF_TOURNEY_DRIFT`) = −85 BB > −100 = −start, best-tier deltas ≥ 0 ⇒ **floor +15 BB on every seed incl. all-fold** (locked in spec §(c) + code comment; any drift-knob change must re-verify); deterministic sim table corrected to sim output (Best wins 105 🏆 / Acceptable busts hand 34 at FT / Bad busts hand 25 post-bubble / all-fold floor +15); in-engine archetype runs: perfect run WIN at hand 40 (139.5 = 100 + 117.5 − 78 exact reconciliation). **Run shape:** 8 levels × 5 hands, start 100 BB, drift ONLY between hands (🎰 labeled beat, deterministic, schedule printed on intro), pool ramp M2+M3 → +M4 → M4+M5 → M5/FT by street+difficulty (poker truth carries pressure — **no tournament multiplier on regret**, QA asserted in/out delta equality QdJd −45 both modes), Table Read interludes between levels, bust check after drift, 🏆 WIN at 40. **Range Reveal (PIN 2, amended + line-reviewed):** qualitative bands from authored prose regex whitelist; negation guard (~40-char window: not/n't/never/rarely/no longer); flush-dense requires range|value in-sentence; **per-scenario suppression list `_PF_REVEAL_SUPPRESS`** (owner line-review of all 510 rows: 9c9h continue-vs-raise false positive suppressed; KcJd value-lean upheld w/ authored support; "polar card" shorthand accepted-interpretation note in spec). **Final counts: 50 banded (polar 40 / merged-thin 4 / value-heavy 3 / bluff-heavy 2 / polar·value-lean 1), 2 NEG-SUPPRESSED, 1 LIST-SUPPRESSED, 460/510 no chip**; chip fires ONLY M4/M5 inside `mode==='tourney'` (runtime↔report cross-checked; drill mode chip-free verified). **Ghost run:** best+last series only (≤2 × ≤40 numbers <1KB), dashed-teal SVG sparkline, display-only. **Finish:** trophies 🥉cash/🥈FT/🏆win + Champion/Regular badges, first-win +500 XP once, ΣdecisionEV vs Σblinds reconciliation lines + bust attribution ("Busted by mistakes" vs "by blind pressure"), killer-hand rematch via redrill path, receipt prints TOURNAMENT. Strict `mode==='tourney'` isolation (no tourney branches in drill/daily/boss/revenge). Loader round-trips `rmtt_tourney` (build gate). M6 FT hook live as no-op: `level≥7 → pool.concat(allApproved('pf_river_value_ip'))`. QA: 320×700 all screens; 0 console errors; data hash == HEAD (65f0022d…); validator 510/0/0. Cache 4.4.6→4.5.0. Spec `docs/specs/game-g3-v4.5.0-tournament-mode-spec.md` + hit-list `docs/specs/game-g3-range-reveal-hitlist.md`; snapshot `GPT AUDIT/v4.5.0/`. **Next: G4 — M6 "River Betting IP" (`pf_river_value_ip`) as the Tournament FT content drop — spec/architecture first (bettor-side schema; frequency-vs-verifiability resolved at schema level; Range-Reveal-compatible prose from day one), stop at owner review.**

- **v4.4.6 G2.5 Juice Minor (RUNTIME/GAME-LAYER ONLY; DATA BYTE-IDENTICAL 510/0/0)**: Owner-approved spec built + QA'd with the **All-in binding pin satisfied**: ×5/0 judged on the GAME-TIER after whitelist promotion — proven with the all-in-on-mixed case (KcTd: teachTier acceptable → gameTier best → **625 points (125×5), Stack 0**); loss case proven Stack-untouched (points 0, Stack −9.75 = exact normal bad math). **🎲 All-in Confidence:** once/session, Score-only, hard-absent in Boss + Table Read (verified), UI copy "×5 if BEST", badge 🎲 Stone Cold (5 wins). **🛍️ Chip Shop:** separate Score-wallet (`lifetimeScore − spentScore`); **wallet-guard proven with real numbers** (buy 1,500 → xp 14,355 / lifetimeScore 14,995 / rank ALL unchanged; wallet −1,500); 7 items 4 slots (CSS-only, card faces stay white); equip persists reload. **🔥 Heater:** static tiers combo≥4/≥8 (zero gameplay effect) + "Breathe. Run it back." beat after punts (motion-gated). **🧾 Cash-out Receipt:** canvas, BB/points units, study-tool footer, local-only, incl. boss review. **🔧 m4/m5 card fix:** Start-buttons route identical to TCC, Locked gone, syllabi added. Loader round-trips 3 new fields (build gate passed). QA: 320 exact ×3 surfaces; 0 console errors; data hash == HEAD; 510/0/0. Cache 4.4.5→4.4.6. Spec `docs/specs/game-g25-v4.4.6-juice-minor-spec.md`; snapshot `GPT AUDIT/v4.4.6/`. **Next: G3 Tournament Mode (v4.5.0-game) — SPEC vs the 7 advisor criteria → review → build.**

- **v4.4.5 G2 Challenge Layer (RUNTIME/GAME-LAYER ONLY; DATA BYTE-IDENTICAL 510/0/0)**: Owner-approved spec built + QA'd; knobs ruled: **first-pass-only +200 XP** (retakes anytime, normal score only — verified with a retake case: pass1 +2,500 incl. bonus, retake +2,300 no repeat), pass gate 80%, Revenge **critical-only**, personas default ON. **Boss Exams (M1–M5, 5 trophies):** the ONE sanctioned deferred-feedback mode, keyed to strict `mode==='boss'` — exam screen strips strip/hints/guide/pattern/learn-tag/persona (QA: all 6 suppressed; leak-proof proven: normal/revenge/daily still immediate); 10 hands from difficulty≥4 pool w/ honest fill disclosure (m2 = 5×D4-5+5×D3); answers lock (🔒 flash → auto-advance); **end-review renders FULL standard teaching blocks for all 10** (Turn/River Logic ×10 verified — real functions, no abridged copies) + per-miss ↻ Re-drill + Drill-all-misses; trophy {earned,bestPct,attempts} + shelf on Player Card; intro screen states the first-pass-only rule. **Two QA-caught bugs fixed:** isBossQ hoisted-undefined ordering (suppression dead) + boss-complete non-idempotent (attempts/XP double-count) → finalize guard + stored outcome (attempts=1 across triple render). **Revenge Hands:** queue from real history criticals only (completed sessions), newest-first, unique, minus cleared, cap 8; normal immediate-feedback flow w/ ⚔️ REVENGE frame; clear-on-best persists ("✅ Punt avenged!"); full cycle QA'd end-to-end incl. queue drain + summary + no-drill-again; empty-state honest toast. **Villain personas (cosmetic only):** Big Blind Benny/M2, C-bet Carl/M3, Barrel Bob/M4, Triple-Barrel Trey/M5 — intros state the spot's structural line, quips are pure flavor; **line-by-line no-claims review passed** (zero frequencies/ranges/always-never); table frames only (never Table Read/boss); Settings toggle default ON. Badges +2 (🏆 Boss Slayer 5 trophies, ⚔️ Avenger 10 clears) + loader round-trips trophies/revenge (G1-class bug pre-empted). TCC tiles (⚔️ Boss Exams, 🩹 Revenge) + routes + per-card Boss buttons. 320×700 all 4 boss screens = 320 exact; 0 console errors; data hash == HEAD (65f0022d…); validator 510/0/0. Cache 4.4.4→4.4.5. **Known pre-existing issue (fix scoped into G2.5):** m4/m5 curriculum cards still show 🔒 Locked buttons despite Beta status. Spec `docs/specs/game-g2-v4.4.5-challenge-layer-spec.md`; snapshot `GPT AUDIT/v4.4.5/`. **Next: G2.5 Juice Minor (v4.4.6) — spec → review → build** (All-in Confidence [Score-only, 1/session, disabled in Boss + Table Read] + Cosmetic Shop [separate Score-wallet; XP/rank/lifetimeScore never decrease] + Heater State [FX-only] + Cash-out Receipt [BB/points units, local-only] + m4/m5 card fix); then G3 Tournament absorbs Range Reveal + Ghost Run as the drama layer.

- **v4.4.4 G1 Progression Core (RUNTIME/GAME-LAYER ONLY; DATA BYTE-IDENTICAL 510/0/0)**: Owner-approved spec (docs/specs/game-g1-v4.4.4-progression-core-spec.md) built + QA'd under the **retroactive-credit-integrity binding condition** — XP derives ONLY from real stored records: postflop `rmtt_postflop_history.sessions[].{best,acceptable,bad,critical}` → Σ(best×100+acc×40+bad×10) (base points, no combo — conservative), preflop `rmtt_stats.dailyHistory[date].{hands,correct}` → Σ(correct×25+(hands−correct)×5); **no estimation/synthetic backfill**; exam +200 is **live-only from v4.4.4** (no reliable stored per-exam record — scope-guard disclosed in Player Card copy). One-time idempotent seed (`xpSeeded` flag; verified re-seed does not double). **Worked example (real dev profile): 4 sessions [12/12/5/5 best] → postflopBase 3,400 + preflopBase 0 → 3,400 XP → 🎣 Grinder; formulaMatch:true.** **Player Level:** 8 ranks (Fish 0 / Grinder 2.5k / Reg 7.5k / Solid Reg 20k / Crusher 50k / Shark 100k / Elite Pro 200k / GTO Wizard 400k); live XP = the v4.4.3 Score stream (postflop hook in `_pfGameOnAnswer`, verified +125 w/ combo) + preflop hook in `updateDailyStats` (verified +25/+5 through the real path, real dailyHistory records written); rank-up FX via v4.4.3 pipeline (fanfare+confetti, toggles + reduced-motion respected). **Badge engine:** declarative 16-badge registry computed from real data only (strict tier='best'; whitelist promotions do NOT count) — Tight is Right I–IV, MDF Defender, Blocker Brain, River Rat, Turn Guardian, Table Reader, Clean Sheet, No-Punt Grinder, Combo King, Daily Devotee, Iron Streak, Making It, The Long Game; unlock FX fires exactly once, persisted in `rmtt_gamestats.badges`; QA unlocked 3 from real data (Tight I, Clean Sheet, Combo King) with live progress (No-Punt 4/10, River 26/100, Reader 10/100). **Player Card** (top of Progress tab): rank + XP bar + lifetime row + 16-cell badge case + integrity note (retroactive sources + exam scope-guard + "preflop mastery ranks + Chips separate/unchanged"); **mini strip** on Academy. **Existing preflop ranks (`rmtt_profile`) + Chips economy untouched.** **QA-caught bug fixed:** v4.4.3 `_pfGameStatsLoad` dropped unknown fields → G1 fields (xp/xpSeeded/xpSeed/badges) now round-trip; full evidence re-run green after fix. 320×700: card right 304 ≤ 320, no overflow; console 0 errors/warnings; data hash == HEAD blob (65f0022d…); validator 510/0/0. **Tooling note:** preview screenshot pipeline timed out persistently (fresh server, responsive eval, clean console) — Player Card visual proof = DOM assertions + geometry + full-content dump per owner-approved fallback; noted in snapshot MANIFEST. Cache bump 4.4.3→4.4.4. New spec doc committed; snapshot `GPT AUDIT/v4.4.4/`. **Next: G2 Challenge Layer (v4.4.5) — spec → review → build** (Boss Exams under the standing deferred-feedback-ONLY-in-Boss policy w/ full end-review + per-miss re-drill links; Revenge Hands from real history; villain personas cosmetic-only).

- **v4.4.3 Juice Pack (RUNTIME/GAME-LAYER ONLY; DATA BYTE-IDENTICAL 510/0/0)**: Owner-approved spec (docs/specs/postflop-v4.4.3-juice-pack-spec.md) built + QA'd. **Two-account model:** STACK = "Session EV" meter, regret-based vs authored tiers, never lies (best fold = 0 + "✋ Saved X BB"; M2 best check = "✋ Correct check ✓" per amendment #2; best call/bet = +0.5×stake; acc/bad/crit = −0.25/−0.75/−1.50×stake); SCORE/COMBO = explicit game currency (best 100/acc 40/bad 10/crit 0 × combo ×(1+0.25n) cap ×3, fire visuals). **Derived stakes from existing metadata only:** M3 2 / M2 3 / M4 5 / M5 by villainRiverSizing 7/13/20/30 BB → late-street punts auto-cost more (flop bad −1.5 vs river-overbet critical **−45 BB**); all deltas labeled "est. EV (heuristic)". **Mixed whitelist (amendment #1):** hard-coded per-scenario enumeration of ALL 12 mixed-best scenarios in the 510 with explicit indifference sets; promotion (game-tier best: EV 0 ⚖️ + combo extends + full points) applies ONLY to whitelisted (id, choiceId) pairs at tier=acceptable; teaching tier display untouched. Verified-then-committed: AdTh {mixed, call} deliberately excludes fold (authored bad; prose "leans call / folding too tight"). **Frames:** action_choice M2–M5 = "At the Table" (Stack+Score); M1 + all reason_choice = "🔍 Table Read" (Score only, EV dimmed). **Daily Challenge:** 5 hands/day deterministic (FNV-1a(date) + xorshift over id-sorted pools; slots [M1, M3, M4, M5, wild]); one attempt/day; 12-week heatmap + daily streak; Academy block + TCC tile 📅 + route postflop:daily. **Saved-BB ledger + "Tight is Right" badge** (25/100/300/1000, confetti on tier-up). **SFX:** 6 Web-Audio-synthesized sounds, zero assets; **animations** CSS transform/opacity only (deal stagger, river flip, chip fly, count-up tabular-nums, punt shake + red vignette, combo fire, confetti ≤40); Settings toggles Sound FX + Motion FX (sound default ON per owner); prefers-reduced-motion honored (JS gate + CSS belt-and-braces). **QA evidence all green:** perfect session EV honest (+58.5 w/ Saved 20 separate) while score climbs (2900, combo 12); best-fold Saved-BB no phantom gain; mixed both sides 0 EV + combo extends (teaching tier still shows acceptable); JsTs call NOT promoted (whitelist gating proof); M1/reason never move Stack; Daily deterministic across wiped-profile check, 2nd attempt blocked, persists reload; overbet punt −45 live w/ PUNT FX (screenshot); toggles work; 320×700 no overflow incl. strip/banner; 0 console errors end-to-end; data hash == HEAD blob (65f0022d…); validator 510/0/0. Cache bump 4.4.2→4.4.3. **PROGRAM PIVOT (owner directive):** the game program is now the MAIN track; M6 re-queued. **Approved roadmap:** G1 Progression Core (XP/rank/badges/profile) → G2 Challenge Layer (Boss Exams w/ approved deferred-feedback-ONLY-in-Boss policy: end-review renders FULL teaching per question + re-drill links; Revenge Hands; villain personas) → G3 Tournament Mode (flagship) → G4 M6 debuts INSIDE Tournament as content drop → G5 opt-in Blitz. Invariants standing: teaching correct + fully rendered (Boss = only sanctioned deferral), Stack never lies, timers opt-in only, disclaimer untouched, data byte-identical unless a sprint says otherwise. **Next: G1 (v4.4.4) spec → owner review → build.**

- **v4.4.2 Module 5 Limited Beta Runtime Wire (RUNTIME-ONLY; M5 PLAYABLE; PRODUCTION UNCHANGED 510/0/0)**: **Module 5 IS PLAYABLE as Limited Beta** — the 33-scenario river-defense corpus is wired end-to-end, mirroring the v4.3.1 M4 wire. **Runtime additions (all index.html):** `getModule5Scenarios()` (approved-only, 33); `_PF_M5_REASON_LABELS` (12 `_river` reasons, added to the normalizer chain); TCC tile `Module 5 · Facing River Barrel OOP` (🌊, 33 river-defense scenarios · Limited Beta, BETA badge) + route `postflop:m5` → `startPostflopDrill('pf_river_barrel_oop_def', 12)`; drill/concept-drill/weak-spot routing with M5 pool + empty-state toast; `_pfBuildQuestionPrompt` isM5 branch (flop/turn/river split, third-barrel framing); question screen with **6-chip action history** ending `BTN bets river (sizing)` (surfaces villainRiverSizing = the MDF driver), `_m5` choice guides, M5 first-time explainer (showdown-only / MDF ladder / blockers / busted-draws-never-call); **5-card board renderer** — Turn pill (amber) on idx 3 + NEW River pill (teal, `.is-river-card`) on idx 4 + `.is-five-card` row; `_pfM5TeachingFeedbackBlocksHtml` (River Logic PROMINENT + Blocker note); complete screen (M5 headline + `_pfM5RenderSessionAggregations` river breakdown + `_pfM5BetaSessionLeakHtml` + drill-again→M5); `_PF_CURRICULUM` m5 flipped Future→Limited Beta with 8-line syllabus; `_pfModuleStatus('m5')='beta'`; **12 M5 concept-library entries** (drillable) + "Module 5 — River Defense OOP" group — **v4.4.1C TODO delivered:** `river_blocker_defense` copy carries the flush-vs-straight asymmetry note (Ah = 100% one-sided lock vs straight-blocker = two-sided ~25% nudge); M5 mastery checklist (4 sessions / 75%×2 / **8-of-12 reasons** — not 12 because domination_river_fold is distractor-only by design / weak-review / no-critical); M5 BetaQA dashboard + Copy M5 Snapshot. **THE 320px SOLVE:** base 56px cards + 12px gap = 334px overflow at five cards → `.is-five-card` gap 6 / 48×70 = 268px; `@media ≤359px` 44×64 / gap 5 ≈ 247px. **Runtime QA (fresh profile, caches cleared): zero console errors across the whole flow**; loader 510/510, M5 pool 33, status beta; question screen verified (5 cards + both pills + 6 chips + BB framing + explainer + 5 choices); **320×700 screenshot-verified — board maxRight 283 ≤ 320, body.scrollWidth 320, no overflow**; grading 12/12 best = 12.0/12.0; answer screen River-Logic-prominent with M5 labels; complete screen with river breakdown + reason coverage; history/mastery/QA aggregate M5 sessions; concept drill (`river_mdf`) builds a 12-q M5-only queue; Academy renders M5 mastery + QA + concept group + curriculum card. **Cache bump** appVersion `4.4.1C→4.4.2`, SW `v4.4.1C→v4.4.2`. **Production data byte-identical this sprint** (510/0/0 PASS re-run; only index.html + service-worker.js changed). Honest Limited Beta copy throughout. New doc `docs/specs/postflop-v4.4.2-module5-limited-beta-wire.md`; snapshot `GPT AUDIT/v4.4.2/`. **Backlog:** V1–V3 (chart/solver), full M4 review. **Next: M6 — River Betting IP (v4.5.0 architecture; hero = BTN as the bettor: value/bluff selection, sizing, bluff:value ratios) — the FINAL postflop module, then the postflop curriculum M1–M6 is complete.**

- **v4.4.1C Module 5 Straight-Blocker Mix (PRODUCTION DATA + CACHE BUMP; 509 → 510; NO RUNTIME ROUTE)**: One new M5 scenario ships the straight-blocker lesson **honestly as `mixed_indifference_river`** (NOT `blocker_bluff_catch_river`), closing the last two v4.4.1A/B token gaps. **Scenario:** `pf_btn_v_bb_srp_100bb_river_Jc9d8s_2c_m5_reason_Th9h_v441c` — board `Jc 9d 8s / 4h / 2c` (brick river on a straight-possible runout, M5-first combo), hero `Th 9h` (second pair), medium sizing, reason_choice diff 4; best=`mixed_indifference_river`, acceptable=`[bluff_catch_river]` (AhJc-precedent partition; disclosed deviation from the brief's `[]`), bad includes the featured trap `blocker_bluff_catch_river`, critical=`[]`. **The lesson (both asymmetry roots):** magnitude — Ah blocks 100% of the nut flush vs a straight-blocker's 1-of-4 ≈ 25%; sidedness — the T sits in BOTH villain's value straights (QT/T7, the only straights on this board, both requiring a ten) AND his busted open-enders (KT/AT), so value and bluffs shrink together → blocker = nudge → mix. **Implementer-owned full derivation** (straight census: QT/T7 only, no one-card straights; hero = pair of nines NO straight; rainbow flop → zero flush contamination; beats-all-bluffs/loses-all-value below-median → genuine mix; T9s pure BB flat with perfect line coherence: flop pair+OESD, turn OESD live, river missed). Consultant's adversarial pre-review cleared the same candidate; brief's "gutshot" mislabel corrected to open-ender (4th consultant slip caught); Q9s contrast refined (Q9 also part-blocks → honest contrast: BOTH mix, no single straight-card flips a verdict). **Token-gap bookkeeping CLOSED:** `domination_river_fold` = distractor-only BY DESIGN (already in the bad-list of 3 shipped reason_choice; "0 examples" was a counting artifact; never author a best=fold for it — v4.4.1B structural finding); straight-blocker = "taught via mixed + curriculum note." **v4.4.2 TODO:** add the flush-vs-straight asymmetry note to the `river_blocker_defense` concept copy at runtime-wire time. **New source-of-truth layer:** `tools/build-m5-expansion-v4.4.1C.ps1` + per-sprint auditor `tools/audit-postflop-module5-expansion-v4.4.1C.ps1` (v4.4.1A layer re-issued; original untouched) + add-not-replace two-phase `tools/migrate-module5-expansion-v4.4.1C.ps1` (base check 509/32) + seed JSON. **Audits:** seed 1/0/0 PASS; production **510/0/0 PASS** both phases (M1–M4 unchanged 251/49/85/92; M5 33 approved). **Runtime smoke-tested:** caches cleared, `510/510 approved`, M5=33, new scenario present with correct fields, **0 console errors**. **Cache bump** appVersion `4.4.1B→4.4.1C`, SW `v4.4.1B→v4.4.1C`; file schemaVersion stays 1.0.0. New doc `docs/specs/postflop-v4.4.1C-module5-straight-blocker-mix.md`; snapshot `GPT AUDIT/v4.4.1C/`. **Remaining backlog: V1–V3 (chart/solver-gated) + full M4 review. Next (auto-drive): v4.4.2 M5 runtime wire (route + 5-card 320px renderer + concept-copy note) → M6.**

- **v4.4.1B Module 5 Correctness Hotfix (PRODUCTION DATA + CACHE BUMP; COUNT UNCHANGED 509; NO RUNTIME ROUTE)**: A consultant (advisory AI) poker review proposed corrections; the implementer independently verified, applied, and **corrected two combinatorial errors in the consultant's stated reasoning**. **9 existing M5 scenarios patched, no scenarios added/removed** (509 total, M5=32) via NEW idempotent narrow-scope `tools/hotfix-module5-v4.4.1B.ps1`. **FIX 1 (critical-tier recalibration):** `answer.critical → []` on **6** one-pair fold/call scenarios (KdQh, KcJd, QcJs, Ad8d + the 2 the consultant missed: Kd7s3c_7d KhJc & QcJd) — one-pair over-fold/over-call is a `bad` leak, not a severe punt; post-sweep 0 one-pair fold/call carry critical. **FIX 2 (monotonicity on Ad8s5c2hKd overbet):** the spot was non-monotonic (weaker AhJc=call cleaner than stronger AhQc=mixed); now AhQc `mixed→call` (bluff_catch_river; best=call/acc=[mixed]/bad=[fold,cr_small,cr_big]) and AhJc `call→mixed` (mixed_indifference_river; best/acc=[bluff_catch_river]). **Implementer correction:** the brief claimed "Ah blocks AK" — FALSE (villain AK uses Ac/As; Ad on board, Ah hero); the Ah only reduces villain AA. AhQc's edge is the KICKER. Explanations rewritten to correct logic; exploit "over-barrels in practice" sentence deleted. **FIX 3 (#12 Ah9c sizing):** `board.villainRiverSizing large→medium`. **Implementer correction:** brief said "single heart makes a flush" — FALSE on a 3-heart board (needs 2); conclusion (Ah = weak nut-blocker, blocks only nut-flush combos) holds; explanation rewritten with correct combinatorics + sizingLogic (medium ~29% clears; pot flush-dense = fold). **VERIFY V1–V3 left UNCHANGED** (gated on studio chart/solver): V1 AKo-flat realism on `…reason_AhKc_v440` (rare cold-flat + duplicates KdQh lesson → rework to AJs/ATs or add low-freq note); V2 Ad6c A6o-flat confirm vs chart; V3 M4 `8d6c3s_Qh …AhQc_v430C` AQo-flat + value-favored claim. **Audits all green:** production **509/0/0 PASS** (M5 32; sizing large 12/medium 11/small 5/overbet 4 after FIX 3). **Array serialization verified** (`["mixed"]` etc. proper arrays). **Runtime smoke-tested:** ready=true error=null, total 509, AhQc best=call/v4.4.1B, AhJc best=mixed_indifference_river, Ah9c sizing=medium, zero console errors. **Cache bump** appVersion `4.4.1A→4.4.1B`, SW `v4.4.1A→v4.4.1B`; file schemaVersion stays 1.0.0. **No scenarios added, no runtime route / UI / renderer change.** Unchanged: v4.4.1A expansion scenarios (except the patched ones), all builders/auditors/migration tools, audit-postflop-ps.ps1 (R76–R93 unchanged), concepts/taxonomy, M1–M4 (except none touched). New tool `tools/hotfix-module5-v4.4.1B.ps1`; new doc `docs/specs/postflop-v4.4.1B-module5-correctness-hotfix.md`; new snapshot `GPT AUDIT/v4.4.1B/`. **Backlog:** domination_river_fold (still 0) + distinct blocker_bluff_catch (still 1) + V1–V3 + full M4 review. **Next (auto-drive): v4.4.1C/B-expansion toward 60 (fill deferred) → v4.4.2 M5 runtime wire → M6.**

- **v4.4.1A Module 5 Expansion (PRODUCTION DATA + CACHE BUMP; NO RUNTIME ROUTE YET)**: Gap-first expansion of Module 5 from **24 → 32** (production **501 → 509**: 251 M1 + 49 M2 + 85 M3 + 92 M4 + **32 M5**), all M5 `approved`. **Quality-over-count: 8 scenarios, every spot strategically reviewed; targets were DEFERRED rather than shipped debatable.** Authored on 2 new boards (A = `Ac 7d 4s / 9h / 2c` A-high dry brick; B = `Kh 9h 5c / 4d / 2h` flush-completing) via NEW `tools/build-m5-expansion-v4.4.1A.ps1`. **Gap-fills:** `pot_odds_river_call` 0→1 (A.1, 88 underpair small-bet price call), `bluff_raise_river` 1→2 (B.1, AhJc nut-flush-blocker check-raise), `thin_value` role 1→2 (B.4, JhTh non-nut flush call-not-raise), `mdf_defense_river` 2→3 (A.2, A6 weak ace MDF defend), `mixed_indifference_river` 2→3 (B.3, KcTd no-blocker knife-edge), `range_disadvantage_river_fold` 2→3 (A.4, KQ capped give-up). **Two anchors (distinct, not gap-fills):** A.3 over-fold-trap CALL (AQ must call a pot bet) pairs with A.2 to teach the top-pair class defending across sizings; B.2 nut-flush `value_raise` (Ah3h check-raise big) is a deliberate **polarization pair** with B.1 on the SAME board (with the Ah you bluff-raise the blank or value-raise the made flush). Prompts state the river bet size (clarity improvement over v4.4.0 generic prompts). **Honestly DEFERRED (not shipped debatable):** `domination_river_fold` stays 0 — the obvious construction (fold weak top pair to a big bet) is poker-WRONG (top pair beats all bluffs → bluff-catch/mix, not a clean fold); needs a counterfeited-two-pair-style board, deferred to v4.4.1B. A 2nd `blocker_bluff_catch_river` stays 1 — every clean construction duplicated the approved R3.4; deferred until a distinct blocker mechanic. **Strategic review caught + fixed 2 pre-migration errors:** an A5 "domination fold" (actually a bluff-catch → replaced with the AQ over-fold-trap call) and an Ah9c blocker-call (R3.4 duplicate → replaced with the nut-flush value-raise). **NEW per-sprint auditor** `tools/audit-postflop-module5-expansion-v4.4.1A.ps1` ports the production R76–R93 rule set + cross-corpus duplicate-ID check (vs production + vs v4.4.0 seeds); seed audit **8/0/0 PASS**, no dup IDs. **NEW add-not-replace migration** `tools/migrate-module5-expansion-v4.4.1A.ps1` (two-phase, idempotent, preserves the existing 24 v4.4.1 M5; the 8 carry `version=v4.4.1A` / `reviewStatus=v4.4.1A_strategic_reviewed`). **Critical density intentionally low** (2 of 5 action: B.1 call A-high, B.2 fold the nut flush) to recalibrate the combined M5 corpus from ~61% toward the 30–40% band. **Cache bump:** appVersion `4.4.1 → 4.4.1A`; SW VERSION `v4.4.1 → v4.4.1A` (`postflop_scenarios.json` 501→509 changed). **Audits all green:** production **509/0/0 PASS** (M5 approved 32; M1–M4 unchanged 251/49/85/92); expansion seed 8/0/0; v4.4.1 M5 seed 24/0/0 unchanged. **Runtime smoke-tested (local preview):** after clearing the v4.4.1 SW cache, `[postflop] loaded 509/509 (schema 1.0.0)`, `ready=true error=null`, pool 251/49/85/92/**32**, M5 inert (no router until v4.4.2), zero console errors (first reload served the cached 501 — confirming the SW bump is required). Top-level file `schemaVersion` stays `1.0.0`. **No runtime route / UI / renderer change.** **Unchanged this sprint:** the v4.4.1 M5 production scenarios (24, byte-preserved), v4.4.0 planning artifacts + M5 seed builder/JSON/auditor, `audit-postflop-ps.ps1` (R76–R93 unchanged), `postflop_concepts.json`/`postflop_taxonomy.json`, all M1–M4 builders/JSONs/tools, ranges, manifest, preflop, gamification. New doc `docs/specs/postflop-v4.4.1A-module5-expansion.md`. New snapshot `GPT AUDIT/v4.4.1A/`. **Next (auto-drive): v4.4.1B M5 expansion toward 60 (fill deferred domination + distinct blocker_bluff_catch + reserved riverCategories) → v4.4.2 M5 runtime wire (route + 5-card 320px renderer) → M6.**

- **v4.4.1 Module 5 Production Migration (PRODUCTION DATA + CACHE BUMP; NO RUNTIME ROUTE YET)**: First production-mutating M5 step. The 24 strategically-reviewed v4.4.0A M5 seeds migrated into production `postflop/postflop_scenarios.json`: **477 → 501** (251 M1 + 49 M2 + 85 M3 + 92 M4 + **24 M5**), all `auditStatus=approved`, `reviewStatus=v4.4.0A_strategic_reviewed`, `version=v4.4.1`, `street=river`, per-scenario `schemaVersion=1.3.0`. **Two-phase migration** via NEW `tools/migrate-module5-v4.4.1.ps1` (mirrors `migrate-module4-v4.3.0B.ps1`: idempotent, no Invoke-Expression, no unsafe Remove-Item, UTF-8 NO-BOM atomic tmp+Move-Item; verifies 24 planning seeds + 477 non-M5 preserved; review_pending → -FlipApproved → approved). The migration also **refreshed the stale top-level file `description`** (was "385 scenarios: 251 M1 + 49 M2 + 85 M3" / v4.2.3B — stale since the M4 migration; now accurately states 501 across M1–M5) — done in-script so the data file stays reproducible from source. **NEW M5 production-rule block R76–R93 added to `tools/audit-postflop-ps.ps1`** mirroring the M4 R55–R75 block + the M5 seed auditor's river rules: module/street=river/game/schemaVersion=1.3.0 lock (R76); BB-vs-BTN river-defense spot lock heroRole=turn_check_caller_oop / villainRole=river_barreler_ip (R77); 5-card board structure flopCards+turnCard+riverCard+cards=5 (R78); M5 board enums incl. the M5-defining villainRiverSizing (R79); heroHand collision (R80); handClass/heroHandRole/drawCategory(none/busted_*)/showdownValue vocab (R81); action_choice + reason_choice schema + answer-partition disjointness with critical⊆bad (R82–R84); explanation completeness with **conditional sizingLogic** (required only for check-raises) (R85); conceptTags 1–4 in M5 vocab (R86); auditStatus/sourceConfidence (R87); flush=5-of-suit (R88) + straight=5-consecutive (R89) invariants; **busted-draws-never-call HARD (R90, the M5 signature rule)**; nut-suit blocker invariant (R91); no-draw-equity-realization WARN (R92); text-integrity self-correction-artifact HARD (R93). Plus an M5 stats block (riverCategory / villainRiverSizing / hcc / action / status). The earlier v4.4.1 pre-fixes (12 M5 concepts added to `postflop_concepts.json` 51→63; R10/R13 flop-only rules excluded for M5) ship in this sprint. **Bounded historical-source exception (disclosed):** `tools/build-m5-seeds-v4.4.0.ps1` + the 24-seed JSON received **mechanical metadata-correctness fixes ONLY** (42 lines / 24 scenarios: `suitTextureRiver` rainbow/flush_possible→two_tone ×24, R3 textureTags flush_possible→flushing ×4, R4 textureTags straight_possible→straightening ×4, R4 highCardClass 9_high→low ×4) — invalid enum values the v4.4.0 seed auditor missed but the production board-derivation rules reject; **ZERO change to any strategic field** (recommendedAction/answer/actionReason/heroHandRole/handClass/drawCategory/explanation/conceptTags/heroHand/board cards all byte-identical); seed re-audit 24/0/0 PASS. Documented in v4.4.1 doc §9. **Cache bump:** appVersion `4.3.2C → 4.4.1`; SW VERSION `v4.3.2C → v4.4.1` (cache-first runtime data files `postflop_scenarios.json` 477→501 AND `postflop_concepts.json` 51→63 both changed). **Audits all green:** production **501 / 0 / 0 PASS** (M5 status approved: 24, M1–M4 unchanged 251/49/85/92); M5 seed 24/0/0 PASS unchanged. **Runtime smoke-tested in local preview:** `[postflop] loaded 501/501 scenarios (schema 1.0.0)`, `App.postflop.ready=true error=null`, module pool 251/49/85/92/**24**, M5 inert (no router matches `pf_river_barrel_oop_def` until v4.4.2), zero console errors/warnings. **Top-level file schemaVersion stays `1.0.0`** (file-format version, matches the app's `POSTFLOP_SCHEMA_VERSION` gate — unchanged; only per-scenario M5 schemaVersion is 1.3.0). **Forbidden/unchanged this sprint:** all M5 v4.4.0 planning artifacts (architecture / schema-taxonomy / audit-plan / gpt-review docs), the M5 seed auditor, all M4/M3/M2/M1 builders + seed JSONs + migration/hotfix tools, `postflop_taxonomy.json`, ranges, manifest, preflop, gamification, M1–M4 strategy fields. **No runtime route / UI / renderer change** — M5 is data-only this sprint. New tool `tools/migrate-module5-v4.4.1.ps1`. New doc `docs/specs/postflop-v4.4.1-module5-production-migration.md`. New snapshot `GPT AUDIT/v4.4.1/`. **Next (auto-drive continues per owner):** v4.4.1A/B M5 data expansion (24 → 60–90), then v4.4.2 M5 runtime wire (TRAINING_MODES.m5 + `postflop:m5` route + 5-card board renderer that fits 5 cards at mobile 320px — the one net-new design problem), then M6 (River Betting IP) architecture.

- **v4.4.0A Module 5 Strategic Seed Review (PLANNING-ONLY)**: Strategic poker review of the 24 v4.4.0 M5 seeds against the river-correctness rubric. **Verdict: 24/24 PROMOTE (22 as-authored + 2 via REVISE), 0 REJECT.** Two builder-first revisions applied + regenerated + re-audited (24/0/0 PASS): (1) **2.2 AdTh** weak-kicker top pair vs a pot-size A-high bet changed `best=call -> best=mixed` (calling AT vs pot on A-high is genuinely borderline given villain's value-rich pot range; the A-blocker makes it a mix, not a clean call); (2) **6.2** swapped hero `AcKh` (top two pair, relied on BB flatting AKo preflop -- frequency-dependent) -> `8c8h` (set; BB unambiguously flats 88), preserving the identical "call don't raise into a polar overbet" lesson with no preflop-range ambiguity. Coverage reconfirmed: over-fold trap (R1.1/R5.1/R5.2/R6.4) AND station trap (R2.1/R3.3/R4.3) both present; river-defining reasons all showcased; sizing spans small/medium/large/overbet; every made-hand claim re-verified on the full 5-card runout. **Production audit unchanged 477/0/0**; seeds remain planning_only. New doc `docs/specs/postflop-v4.4.0A-module5-strategic-review-results.md`. Snapshot `GPT AUDIT/v4.4.0A/`. **Next: v4.4.1 production migration (477 -> 501)** -- the first production-mutating M5 step (migration tool + add M5 production rules to audit-postflop-ps.ps1 + version bump). NO runtime wire yet.

- **v4.4.0 Module 5 River Defense OOP Architecture (PLANNING-ONLY)**: Owner pivoted the program from "wait for M4 user signals" to "build M5 + M6 as full modules, sprint by sprint" (confirmed via AskUserQuestion: M5 = River Defense OOP, M6 = River Betting IP; build depth = full modules sprint-by-sprint). v4.4.0 is the M5 architecture/seed-plan sprint, mirroring the v4.3.0 M4 planning pattern exactly. **NO production data touched, NO runtime wiring, NO version bump** (appVersion stays 4.3.2C, SW v4.3.2C; postflop_scenarios.json byte-identical at 477/0/0). **Module 5 = BB River Defense OOP**: hand tree continues from M4 (BTN open 2.5x / BB call -> BTN cbet small / BB call -> BTN barrel turn / BB call -> BTN bets river -> BB river decision OOP). The defining M5 lesson: **the river is showdown-only -- no draw equity; bluff-catching under polarization via MDF + blocker-driven defense; busted draws are bluff-raise-or-fold, never call.** **7 planning artifacts created:** (1) `docs/specs/postflop-v4.4.0-module5-river-defense-oop-architecture.md` (16 sections); (2) `docs/specs/postflop-v4.4.0-module5-schema-taxonomy.md` (schemaVersion 1.2.0 -> 1.3.0: adds `riverCard`, 5-card `cards`, `riverCategory`, `runoutTexture`, `riverDrawCompletion`, `villainRiverSizing`, `suitTextureRiver`, `riverLogic` explanation field; 12 river actionReasons incl. river-defining `blocker_bluff_catch_river` / `mdf_defense_river` / `missed_draw_give_up`; NO equity-realization reason by design; 12 river conceptTags; heroHandRole adds `thin_value` / `dominated_bluff_catcher` / `blocker_bluff` / `missed_draw`; drawCategory restricted to none/busted_*); (3) `tools/build-m5-seeds-v4.4.0.ps1` (canonical builder, ASCII-only, atomic write); (4) `docs/specs/postflop-v4.4.0-module5-seed-scenarios.json` (24 planning seeds across 6 river categories x 4: brick / overcard / flush_complete / straight_complete / board_pair / scare_card; 18 action_choice + 6 reason_choice; all `auditStatus=planning_only`, `reviewStatus=v4.4.0_seed_candidate`, schemaVersion 1.3.0, module `pf_river_barrel_oop_def`); (5) `docs/specs/postflop-v4.4.0-module5-audit-plan.md` (rules M5.R01..R58 incl. 4 NEW river rules: R55 busted-draws-never-call HARD, R56 no-draw-equity-language WARN, R57 riverLogic-references-runout WARN, R58 text-integrity HARD); (6) `tools/audit-postflop-module5-seed.ps1` (PS5.1 mechanical auditor); (7) `docs/specs/postflop-v4.4.0-module5-gpt-review-package.md` (v4.4.0A strategic-review template). **M5 seed audit: 24 / 0 / 0 PASS.** **Author strategic self-review performed** (mechanical-pass-is-not-enough discipline carried from M4): verified every made-hand claim on the full 5-card runout -- K-high flush (5 hearts), nut/bottom straights (5 consecutive verified), trips on paired board, set rankings vs villain combos (AA/KK sets possible where cards unblocked), nut-flush-blocker calls/bluff-raises. One critical-flag downgrade applied (folding weak top-pair AdTh to a pot-size bet is not a severe punt) and one blocker wording tightened (reduces vs blocks AA/KK). **6 reason_choice scenarios showcase the river-defining reasons** (bluff_catch / range_disadvantage / blocker_bluff_catch / thin_value_call / missed_draw_give_up). **Sizing coverage spans all 4** (small R5, medium R1/R4, large R2/R3, overbet R6) so MDF varies across the set. Both the over-fold trap (must-call) and station trap (must-fold) are represented. **Production audit unchanged 477/0/0**; all M4/M3/M2 seed audits unchanged. **Forbidden files all byte-identical** (postflop_scenarios.json, all v4.3.0/C/D/v4.3.2 builders + JSONs + migration/hotfix tools, audit-postflop-ps.ps1, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, index.html, service-worker.js). New snapshot `GPT AUDIT/v4.4.0/`. **Migration path:** v4.4.0A (strategic seed review) -> v4.4.1 (production migration 477 -> 501) -> v4.4.1A/B (expansion 24 -> 60-90) -> v4.4.2 (runtime wire; 5-card mobile renderer is the one net-new design problem). **Next:** v4.4.0A strategic seed review of the 24 M5 seeds before any production migration; then M6 (River Betting IP) architecture follows the same pattern. **STOP after v4.4.0 planning commit** (no migration, no runtime, no M6 yet -- next sprint is v4.4.0A review).

- **v4.3.2D real-beta-signal-review attempt (post-owner-reported-M4-play)**: **BLOCKED on data access. STAY OPTION A pending owner snapshot export.** Re-inspection of `https://range-master-mtt.netlify.app/` from the Chrome MCP browser confirms `appVersion=4.3.2C`, `M4 pool=92`, signal-persistence fix verified live, but `localStorage['rmtt_postflop_history']` STILL does not exist on this browser. Enumeration of all 6 localStorage keys present (`rmtt_streaks`, `rmtt_profile`, `rmtt_progress`, `rmtt_settings`, `rmtt_stats`, `rmtt_disclaimer`) shows extensive preflop module-1 usage (3 KB of preflop range-form progress keys like `10BB_BB_vs_raise_K5o`) but ZERO postflop / M4 entries. **The owner's reported M4 play sessions are on a different browser/device than the Chrome MCP instance connected to this Claude Code session.** Per the v4.3.2D brief Section B + Section I, faking a decision is forbidden when owner data is inaccessible -- the correct action is to document the blocker and request the owner export their M4 Beta QA Snapshot. **Three export methods documented** in `docs/specs/postflop-v4.3.2D-m4-real-beta-signal-review.md` Section 7: (A) recommended -- click the in-app "Copy M4 Beta QA Snapshot" button on the M4 BetaQA dashboard; (B) DevTools console snapshot via paste-ready JS expression; (C) DevTools threshold-check snippet (from v4.3.2C-optionA Section 8). **Decision: STAY OPTION A.** This is a deferral pending data export, NOT a verdict that the owner's M4 play was insufficient. **No code change. No version bump.** appVersion stays `4.3.2C`; SW VERSION stays `v4.3.2C`; `postflop_scenarios.json` byte-identical; all M4 builders / auditors / migration / hotfix tools byte-identical; M1/M2/M3 strategy fields byte-identical; `postflop_concepts.json` + `postflop_taxonomy.json` byte-identical; `index.html` + `service-worker.js` byte-identical; v4.3.2 / v4.3.2A / v4.3.2B / v4.3.2C / v4.3.2C-optionA docs unchanged. Doc-only ship under the v4.3.2C umbrella. New doc `docs/specs/postflop-v4.3.2D-m4-real-beta-signal-review.md`. **Next action required from owner: paste the M4 Beta QA Snapshot from their actual playing browser/device into the next conversation.** Once snapshot arrives, follow-up sprint will parse it, compute thresholds against real data, and apply the v4.3.2C decision rule for Option A/B/C.

- **Option A signal-review status (post-v4.3.2C)**: STAY OPTION A. Live Netlify probe of `https://range-master-mtt.netlify.app/` from the Chrome MCP browser confirms `appVersion=4.3.2C`, `M4 pool=92`, signal-persistence fix verified live (`_pfHistoryRecordSession` source contains `compactAnswers` and `mode: s.mode || 'normal'`). `localStorage['rmtt_postflop_history']` does NOT exist on the inspected browser; `_pfM4BetaQAStats()` returns `sessionCount=0`, all per-axis row arrays empty, `lastWeakProfile=null`. **All 8 of 8 v4.3.2C decision-framework thresholds NOT met** (sessions 0/5; hands 0/60; actionReason coverage 0/8; conceptTag coverage 0/8; critical buckets 0/3; weak-spot review usage 0/1; reason_choice volume n/a; action_choice volume n/a). The decision rule's gating threshold (>=5 M4 sessions) is unsatisfied; downstream weakness-concentration analysis (Option B) and maturity-confirmation analysis (Option C) cannot be performed without data. **Decision: STAY OPTION A. Do not author. Do not start v4.3.3 or v4.4.0.** Recommended next-review cadence: 7-14 calendar days of beta usage, then re-run the threshold check (DevTools snippet provided in `docs/specs/postflop-v4.3.2C-option-a-signal-review.md` Section 8). Hard ceiling: 30 calendar days; if signal volume remains thin past then, pivot to UX/discoverability sprint before forcing a content decision. **No code change in this Option A review** -- index.html / service-worker.js / postflop_scenarios.json / all M4 builders + JSONs + tools / M1-M2-M3 strategy fields / concepts.json / taxonomy.json all byte-identical to v4.3.2C. Doc-only ship under the v4.3.2C umbrella; no version bump; cache lifecycle unchanged. New doc `docs/specs/postflop-v4.3.2C-option-a-signal-review.md`.

- **Latest deployed to Netlify**: `v4.3.2C` (live and verified) — Module 4 Beta Signal Collection / QA Instrumentation Review on top of v4.3.2B. appVersion 4.3.2C, SW VERSION v4.3.2C, M4 pool 92 scenarios (unchanged), 477 total production (unchanged); BetaQA dashboard signal-persistence gap fixed (per-answer detail + drill mode now persisted in localStorage session records, unblocking multi-session per-axis aggregation that has been silently empty since v4.2.6). v4.3.1B 320-px TCC fix preserved unchanged. No scenario data / strategy / taxonomy / UI / route changes. (v4.4.1 / v4.4.1A / v4.4.1B are data-only ships of M5 to production but M5 is NOT yet routed; Netlify auto-deploy on push not separately verified this session.)
- **Last committed + pushed**: `v4.4.6` (`8ef0d80`) — G2.5 Juice Minor on top of v4.4.5 (`4b0ba65`). All-in (game-tier pin proven on mixed) + Chip Shop (wallet guard proven) + Heater + Receipt + m4/m5 card fix. Data byte-identical, 510/0/0, 0 console errors. Cache 4.4.5→4.4.6. Snapshot `GPT AUDIT/v4.4.6/`. **Next: G3 Tournament spec.** Predecessors: `v4.4.5-doc` (`4b0ba65`); `v4.4.5` (`4bce564`); `v4.4.4` (`c91aba3`).
- **Last committed + pushed (HISTORICAL, G2 CHALLENGE LAYER)**: `v4.4.5` (`4bce564`) — G2 Challenge Layer on top of v4.4.4 (`7d07e6f`). Boss Exams M1–M5 (sanctioned deferral, strict mode gate, leak-proof; first-pass-only +200 verified w/ retake; full end-review + per-miss re-drill; trophies + shelf) + Revenge Hands (critical-only, full cycle QA'd) + personas (no-claims line review passed) + badges +2. 2 QA-caught bugs fixed (isBossQ hoisting, boss idempotency). Data byte-identical, 510/0/0, 0 console errors, 320 exact ×4 screens. Cache 4.4.4→4.4.5. Snapshot `GPT AUDIT/v4.4.5/`. **Next: G2.5 Juice Minor (v4.4.6) spec.** Predecessors: `v4.4.4-doc` (`7d07e6f`); `v4.4.4` (`c91aba3`); `v4.4.3` (`af321b7`); `v4.4.2` (`e3afa06`).
- **Last committed + pushed (HISTORICAL, G1 PROGRESSION CORE)**: `v4.4.4` (`c91aba3`) — G1 Progression Core on top of v4.4.3 (`1fadac2`). XP (retroactive from real records only: postflop tier-counts base + preflop dailyHistory; exam +200 live-only scope-guard; idempotent; worked example 3,400 XP → Grinder, formulaMatch:true) + live hooks both sides + 8-rank Player Level + 16-badge engine (3 unlocked in QA) + Player Card (Progress) + Academy strip. Preflop ranks + Chips untouched. QA-caught loader round-trip bug fixed. Data byte-identical, 510/0/0, 0 console errors, 320 clean; screenshot-tooling timeout → DOM-assertion fallback per owner approval (MANIFEST note). Cache 4.4.3→4.4.4. Snapshot `GPT AUDIT/v4.4.4/`. **Next: G2 Challenge Layer (v4.4.5) spec.** Predecessors: `v4.4.3-doc` (`1fadac2`); `v4.4.3` (`af321b7`); `v4.4.2` (`e3afa06`); `v4.4.1C` (`c199d33`).
- **Last committed + pushed (HISTORICAL, JUICE PACK)**: `v4.4.3` (`af321b7`) — Juice Pack game layer on top of v4.4.2 (`a4e499c`). Two-account model (Session-EV Stack regret-based + Score/combo), derived stakes (river-overbet punt −45BB vs flop −1.5), hard-coded mixed whitelist (12; AdTh verified deliberate), Table-Read frame, Daily Challenge (deterministic/1-day/heatmap/streak), Saved-BB + Tight-is-Right badges, 6 synthesized SFX + CSS FX + toggles + reduced-motion. **Data byte-identical (blob 65f0022d…), validator 510/0/0, teaching untouched, 0 console errors, 320×700 clean.** Cache 4.4.2→4.4.3. **PROGRAM PIVOT: game = main track; roadmap G1→G5 approved (M6 = G4 Tournament content drop; Boss deferred-feedback policy approved w/ full end-review + re-drill links).** Spec doc committed; snapshot `GPT AUDIT/v4.4.3/`. **Next: G1 Progression Core (v4.4.4) — spec → review → build.** Predecessors: `v4.4.2-doc` (`a4e499c`); `v4.4.2` (`e3afa06`); `v4.4.1C` (`c199d33`); `v4.4.1B` (`f144a68`); `v4.4.1A` (`a046582`); `v4.4.1` (`e59a14d`).
- **Last committed + pushed (HISTORICAL, M5 RUNTIME WIRE)**: `v4.4.2` (`e3afa06`) — Module 5 Limited Beta Runtime Wire on top of v4.4.1C (`5e30268`). **RUNTIME-ONLY; M5 IS PLAYABLE.** Full v4.3.1-pattern wire: TCC tile 🌊/BETA + `postflop:m5` route (12q) + drill/concept/weak-spot routing + isM5 prompt builder + 6-chip river history + **5-card board (amber Turn + teal River pills; `.is-five-card` CSS solves 320px — screenshot-verified maxRight 283 ≤ 320)** + River-Logic-prominent feedback + M5 session summary/leak hint + curriculum flip + 12 drillable M5 concepts (incl. the v4.4.1C blocker-asymmetry note) + M5 mastery (8-of-12; domination distractor-only) + M5 BetaQA + Copy Snapshot. Production data byte-identical (510/0/0 re-run); QA: 0 console errors end-to-end, grading 12/12, all M5 surfaces render. Cache bump 4.4.1C→4.4.2. New doc `docs/specs/postflop-v4.4.2-module5-limited-beta-wire.md`; snapshot `GPT AUDIT/v4.4.2/`. **M1–M5 all live. Next: v4.5.0 M6 River Betting IP architecture — the final postflop module.** Predecessors: `v4.4.1C-doc` (`5e30268`); `v4.4.1C` (`c199d33`); `v4.4.1B-doc` (`b2746be`); `v4.4.1B` (`f144a68`); `v4.4.1A` (`a046582`); `v4.4.1` (`e59a14d`); `v4.4.0A` (`5e2da16`); `v4.4.0` (`7845dae`).
- **Last committed + pushed (HISTORICAL, M5 STRAIGHT-BLOCKER MIX)**: `v4.4.1C` (`c199d33`) — Module 5 Straight-Blocker Mix on top of v4.4.1B (`b2746be`). **+1 scenario (509 → 510, M5 33 approved)**: the straight-blocker lesson shipped honestly as `mixed_indifference_river` (Th9h second pair on Jc9d8s4h2c vs medium; both asymmetry roots — magnitude 25% vs 100%, sidedness two-sided vs one-sided; featured trap `blocker_bluff_catch_river` in bad). Implementer-owned full derivation passed (QT/T7 only straights, hero no straight, rainbow flop = zero flush contamination, genuine mix, T9s pure flat). **Both token gaps CLOSED:** `domination_river_fold` distractor-only by design; straight-blocker taught via mixed + curriculum note (→ `river_blocker_defense` copy at v4.4.2). NEW builder + per-sprint auditor + add-not-replace migration. Seed 1/0/0; production **510/0/0 PASS** both phases; runtime smoke-tested (510/510, 0 console errors). Cache bump 4.4.1B→4.4.1C. New doc `docs/specs/postflop-v4.4.1C-module5-straight-blocker-mix.md`; snapshot `GPT AUDIT/v4.4.1C/`. **Backlog: V1–V3 + full M4 review. Next: v4.4.2 M5 runtime wire.** Predecessors: `v4.4.1B-doc` (`b2746be`); `v4.4.1B` (`f144a68`); `v4.4.1A-doc` (`0240f78`); `v4.4.1A` (`a046582`); `v4.4.1-doc` (`d102d5d`); `v4.4.1` (`e59a14d`); `v4.4.0A` (`5e2da16`); `v4.4.0` (`7845dae`).
- **Last committed + pushed (HISTORICAL, M5 CORRECTNESS HOTFIX)**: `v4.4.1B` (`f144a68`) — Module 5 Correctness Hotfix on top of v4.4.1A (`a046582`). Consultant-reviewed + implementer-corrected; **9 M5 scenarios patched, count UNCHANGED 509** (M5=32). FIX 1 critical→[] on 6 one-pair fold/call (4 listed + 2 the consultant missed); FIX 2 AhQc/AhJc overbet monotonicity (AhQc mixed→call, AhJc call→mixed; corrected the false "Ah blocks AK" claim); FIX 3 #12 Ah9c large→medium (corrected the false "single-heart flush" claim). VERIFY V1–V3 left unchanged (chart/solver-gated). NEW idempotent `tools/hotfix-module5-v4.4.1B.ps1`. Production **509/0/0 PASS**; 0 one-pair fold/call carry critical; arrays serialize correctly; runtime smoke-tested (AhQc=call, AhJc=mixed, Ah9c=medium, 0 console errors). Cache bump 4.4.1A→4.4.1B. New doc `docs/specs/postflop-v4.4.1B-module5-correctness-hotfix.md`; snapshot `GPT AUDIT/v4.4.1B/`. **Backlog: domination_river_fold (0) + distinct blocker_bluff_catch (1) + V1–V3 + full M4 review.** Predecessors: `v4.4.1A-doc` (`0240f78`); `v4.4.1A` (`a046582`); `v4.4.1-doc` (`d102d5d`); `v4.4.1` (`e59a14d`); `v4.4.0A` (`5e2da16`); `v4.4.0` (`7845dae`).
- **Last committed + pushed (HISTORICAL, M5 EXPANSION 24→32)**: `v4.4.1A` (`a046582`) — Module 5 Expansion on top of v4.4.1 (`d102d5d`). Gap-first **24 → 32** (production **501 → 509**), all M5 `approved`. 8 strategically-reviewed scenarios on 2 new boards via NEW `tools/build-m5-expansion-v4.4.1A.ps1` + NEW per-sprint auditor (ports production R76–R93 + cross-corpus dup-ID) + NEW add-not-replace two-phase migration. Gap-fills: pot_odds_river_call 0→1, bluff_raise_river 1→2, thin_value role 1→2, mdf/mixed/range_disadvantage 2→3. Anchors: A.3 over-fold-trap call (AQ), B.2 nut-flush value-raise (polarization pair with B.1). DEFERRED honestly: domination_river_fold (0; obvious construction is poker-wrong) + 2nd blocker_bluff_catch (R3.4 dup) → v4.4.1B. Strategic review caught + fixed 2 pre-migration errors. Cache bump 4.4.1→4.4.1A. Production audit **509/0/0 PASS**; expansion seed 8/0/0; runtime smoke-tested (`[postflop] loaded 509/509`, M5 inert until v4.4.2). **No runtime route / UI / renderer change.** New doc `docs/specs/postflop-v4.4.1A-module5-expansion.md`. New snapshot `GPT AUDIT/v4.4.1A/`. **Next (auto-drive): v4.4.1B toward 60 (fill deferred) → v4.4.2 runtime wire → M6.** Predecessors: `v4.4.1-doc` (`d102d5d`); `v4.4.1` (`e59a14d`); `v4.4.0A-doc` (`a41a171`); `v4.4.0A` (`5e2da16`); `v4.4.0` (`7845dae`); `v4.3.2C-doc` (`c22ccc8`); `v4.3.2C` (`090f21f`).
- **Last committed + pushed (HISTORICAL, M5 PRODUCTION MIGRATION)**: `v4.4.1` (`e59a14d`) — Module 5 Production Migration on top of v4.4.0A (`a41a171`). First production-mutating M5 step: 24 strategically-reviewed M5 (River Defense OOP) seeds migrated into production `postflop_scenarios.json` (**477 → 501**; 251 M1 + 49 M2 + 85 M3 + 92 M4 + 24 M5), all `auditStatus=approved` / `reviewStatus=v4.4.0A_strategic_reviewed`. NEW `tools/migrate-module5-v4.4.1.ps1` (two-phase idempotent). NEW M5 production-audit block R76–R93 + M5 stats in `audit-postflop-ps.ps1` (busted-draws-never-call HARD R90 is the M5 signature). `postflop_concepts.json` 51→63. Cache bump appVersion 4.3.2C→4.4.1 / SW v4.3.2C→v4.4.1. Production audit **501/0/0 PASS**; M5 seed 24/0/0 PASS; runtime smoke-tested (`[postflop] loaded 501/501`, M5 inert until v4.4.2). M5 seed builder + JSON got mechanical metadata-correctness fixes only (zero strategic change; doc §9). Top-level file schemaVersion stays 1.0.0. **No runtime route / UI / renderer change.** New doc `docs/specs/postflop-v4.4.1-module5-production-migration.md`. New snapshot `GPT AUDIT/v4.4.1/`. **Next (auto-drive): v4.4.1A/B M5 expansion 24→60–90 → v4.4.2 M5 runtime wire → M6.** Predecessors: `v4.4.0A-doc` (`a41a171`); `v4.4.0A` (`5e2da16`); `v4.4.0` (`7845dae`); `v4.3.2C-doc` (`c22ccc8`); `v4.3.2C` (`090f21f`); `v4.3.2B-doc` (`224e539`); `v4.3.2B` (`51633b1`).
- **Last committed + pushed (HISTORICAL, MODULE 4 BETA SIGNAL REVIEW)**: `v4.3.2C` — Module 4 Beta Signal Collection / QA Instrumentation Review on top of v4.3.2B (`51633b1`). **Meta-level signal-collection review sprint with one minimal allow-listed runtime fix.** Brief Section H explicitly permits "missing module id in session history" / "localStorage/session analytics fix" inline. v4.3.2C implements exactly that. **Defect found:** `_pfHistoryRecordSession` (index.html ~line 39785) persisted session metadata (id, date, module, scenarioIds, score-counts) but did NOT persist the per-answer array. The BetaQA dashboard's `_pfBetaQAStatsForModule` (~line 36414) reads `session.answers[]` to aggregate per-axis breakdowns (`rowsByActionReason`, `rowsByRecommendedAction`, `rowsByHeroHandRole`, `rowsByHandClass`, `weakConcepts`, `weakActionReasons`, `weakHeroHandRoles`, `lastWeakProfile`, `criticalCount` lifetime) -- those reads silently returned empty/zero across sessions because `answers[]` was never persisted. **In-session weak-spot review and grading remained correct** (read in-memory `App.state.postflopDrill.answers`); only multi-session dashboard aggregation was broken. Long-standing gap dating to v4.2.6 BetaQA introduction. **Fix:** `_pfHistoryRecordSession` now persists a compact per-answer array mirroring the in-memory shape (scenarioId, questionType, choiceId, tier, score, isCritical, conceptTags) plus a `mode` field (default 'normal'; populated to 'weak_spots' / 'concept' for those drill types via `d.mode`). Caller now forwards `d.mode || 'normal'`. localStorage budget impact: ~80 bytes per answer x 12 answers x 50-session cap = ~48 KB total, well within quota. Defensive read on dashboard side handles pre-fix sessions (`Array.isArray(sObj.answers) ? sObj.answers : []`). **No strategy / scoring / route / UI / scenario data changes.** **Comprehensive signal-collection review documented in `docs/specs/postflop-v4.3.2C-module4-beta-signal-review.md`:** field coverage table (23/23 fields stored or derivable post-fix); BetaQA dashboard review answering 10 brief-defined questions; weak-spot review review with 12 checks all PASS; concept drill review with 7 checks all PASS + concept coverage table; coverage vs signal matrix on M4=92 production data showing 12/12 actionReasons covered (3 thin: pot_odds_turn_call=4, semi_bluff_check_raise_turn=3, protection_check_raise_turn no reason_choice), all 12 concepts >=7 scenarios drillable, all 5 recommendedActions covered, 19 unique boards, 69.6% critical density, 8.7% consensus_gto. **Analytics risk review (boardAbsoluteTexture vs scenarioTeachingAxis):** v4.3.2B's $f8R1 board variant (R1 straight_complete metadata while A5/A7 same physical board keep draw_intensifier metadata) raised the question of whether current analytics treat board metadata as scenario-teaching axis vs absolute board texture. Audit of all dashboard + weak-spot + concept-drill paths confirmed: **current logic uses board metadata as scenarioTeachingAxis correctly**; no analytics path aggregates by `turnCategory` / `boardChange` / `equityShift` / `drawCompletion` across scenarios. Verdict: ACCEPTABLE; no live bug. Future schema recommendation (boardAbsoluteTexture additive fields) documented for v4.4.0 architecture review if/when a future dashboard wants to aggregate by absolute board texture; NOT implemented in v4.3.2C. **Cache bump:** appVersion `4.3.2B -> 4.3.2C`; SW VERSION `v4.3.2B -> v4.3.2C` (runtime persistence change). v4.3.1B `@media (max-width: 359px)` mobile fix preserved unchanged. **Audits all green:** production 477/0/0 PASS unchanged; M4 continuation seed 20/0/0 PASS unchanged; M4 polish 19/0/0 PASS unchanged; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged; M3 24/0/0 PASS clean unchanged; M2 24/0/8 PASS unchanged; R29/R71/R72/R44b/R73/R74/R75 0 fires unchanged. **Decision framework for next sprint** documented with objective thresholds: **Option A (recommended)** -- collect user signals; thresholds: 5 sessions / 60 hands / 8-of-12 reason coverage / 8-of-12 concept coverage / 3 distinct critical buckets / 1 weak-review session; **Option B (v4.3.3)** -- user-signal-driven M4 continuation 92->100-110 only if concentrated weakness identified; **Option C (v4.4.0)** -- M5 River Defense Architecture only if M4 mature signals confirm. **Forbidden files all byte-identical:** `postflop_scenarios.json`, all v4.3.0/C/D/v4.3.2 builders + JSONs, all migration + hotfix tools (B/C/D/v4.3.0C1/v4.3.2A/v4.3.2B), `audit-postflop-ps.ps1` (R55-R72 unchanged), `audit-postflop-module4-continuation-v4.3.2.ps1` (R73/R74/R75 unchanged), M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, `postflop_concepts.json`, `postflop_taxonomy.json`, v4.3.2 / v4.3.2A / v4.3.2B docs unchanged. New doc `docs/specs/postflop-v4.3.2C-module4-beta-signal-review.md`. New snapshot `GPT AUDIT/v4.3.2C/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2C and immediately enter Option A (collect user signals). Signal-persistence fix unblocks dashboard multi-session aggregation; corpus is balanced; user signals will now drive the v4.3.3-vs-v4.4.0 decision objectively. Predecessors: `v4.3.2B-doc` (`224e539`); `v4.3.2B` (`51633b1`); `v4.3.2A-doc` (`d074e56`); `v4.3.2A` (`e8975dd`); `v4.3.2-doc` (`0859d2c`); `v4.3.2` (`12ff7c1`); `v4.3.1B-doc` (`bef1765`); `v4.3.1B` (`e9fadfc`); `v4.3.1A-doc2` (`f11c154`); `v4.3.1A-doc` (`895cc88`); `v4.3.1A` (`59b8184`); `v4.3.1-doc` (`1f2689d`); `v4.3.1` (`c1998cf`); `v4.3.0D-doc` (`9792758`); `v4.3.0D` (`023430a`); `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.2C:** This is the SECOND signal-collection deficiency caught by reviewing instrumentation rather than mechanical audits (the first was v4.3.1A's M4 prompt-framing bug found via live browser QA). Pattern lesson: **dashboard renderers should be unit-testable with empty/missing-field histories.** The BetaQA dashboard reading `session.answers[]` from a session record that didn't persist that field has been silently broken since v4.2.6 BetaQA introduction. The fact that the dashboard still RENDERED (without errors) made the deficiency invisible -- empty arrays just produce empty tables, not console errors. Three pattern lessons banked: (1) **Renderers depending on persisted data must be paired with persisters writing that data** -- v4.2.6 introduced both the dashboard and the persister, but the persister did not include the answers[] array the renderer expected. A simple "round-trip test" (write-then-read with assertion that all read fields exist) would have caught this. Future runtime dashboards should ship with such round-trip tests. (2) **Long-standing dormant bugs surface in instrumentation review sprints** -- v4.3.2C is the first sprint to explicitly REVIEW signal collection. Without this review, the gap would have remained until users complained about "the dashboard always shows zero." Periodic instrumentation-review sprints are valuable independently of new feature work. (3) **Allow-list discipline matters in narrow-scope sprints** -- the v4.3.2C brief explicitly listed "localStorage/session analytics fix" as an allowed inline change, distinguishing it from "scoring / weak-spot logic bug" (STOP and report as v4.3.2D). The persistence gap is firmly in the analytics-fix category (the WEAK-SPOT LOGIC works correctly; only multi-session aggregation was empty), so the inline fix was correctly authorized. **Module 4 stays Limited Beta** with all instrumentation now correctly capturing the signals the dashboard surfaces. Recommendation: enter Option A (collect Beta Lab user signals for at least one feedback cycle) with the v4.3.2C dashboard fix in place; v4.3.3 vs v4.4.0 decision should follow real-user data, not authorial intuition.

- **Last committed + pushed (HISTORICAL, MODULE 4 R1 METADATA CONSISTENCY HOTFIX)**: `v4.3.2B` (`51633b1`) — Module 4 R1 Metadata Consistency Hotfix on top of v4.3.2A (`e8975dd`). **Targeted metadata-only hotfix** addressing a leftover from v4.3.2A: the R1 scenario (`pf_btn_v_bb_srp_100bb_turn_Ts8s4d_7c_m4_reason_QcQd_v432`) had its strategic verdict + prose fully corrected in v4.3.2A (check_raise_small/protection_check_raise_turn -> call/bluff_catch_turn; heroHandRole strong_value -> bluff_catcher; full prose rewrite acknowledging the made straights 65/96/J9 on Ts 8s 4d / 7c) but the board sub-document was NOT updated, leaving stale `turnCategory: draw_intensifier`, `boardChange: draw_added`, `equityShift: improves_bb_draws`, `drawCompletion: oesd_added` metadata that contradicts the corrected verdict (bluff-catch only makes sense relative to made-straight density, which the metadata then denies). **Source-of-truth-first fix:** new `$f8R1` board variant in `tools/build-m4-continuation-v4.3.2.ps1` carries the corrected metadata `turnCategory: straight_complete / boardChange: polarizing / equityShift: polarizes_btn / drawCompletion: straight_completed`; R1 scenario now references `$f8R1` while A5 (NFD semi-bluff) and A7 (top set value-raise) on the same physical board keep `$f8` (`draw_intensifier`) because their strategic verdicts legitimately treat the turn primarily as draw-intensifying (combo_draw and nutted_value heroHandRoles, respectively, where the made-straight density is acknowledged but the strategic emphasis is the draw axis). **Taxonomy validation:** the brief's proposed `boardChange: draw_completed` and `equityShift: polarizes_range` are NOT in the auditor's approved enums; substituted to closest-valid `polarizing` and `polarizes_btn` respectively (documented in v4.3.2B doc). Auditor enum vocabulary NOT modified; postflop taxonomy / concepts JSON NOT modified. **NEW HARD-level M4.R75 guard** added to v4.3.2 continuation auditor: on the specific physical board where flop ranks contain `{T, 8, 4}` and turn rank is `7`, scenarios where heroHandRole=bluff_catcher AND handClass in {overpair, underpair, top_pair_*, mid_pair, bottom_pair, second_pair} cannot have turnCategory=draw_intensifier OR drawCompletion=oesd_added OR boardChange=draw_added. Scoped to the bluff-catcher framing specifically so A5 (combo_draw NFD semi-bluff) and A7 (nutted_value top set) on the same board do NOT fire R75. After v4.3.2B fix, R75 fires zero times across the 20 v4.3.2 continuation scenarios. **NEW idempotent narrow-scope hotfix tool** `tools/hotfix-module4-v4.3.2B.ps1` re-syncs ONLY the `board` sub-document of the 1 target ID into production preserving 91 of 91 non-target M4 byte-identical (verified) + 385 non-M4 untouched + scenario-array order preserved + sanity-spot-checks the 4 metadata fields land at the expected straight_complete / polarizing / polarizes_btn / straight_completed values. **Cache bump only:** appVersion `4.3.2A -> 4.3.2B`; SW VERSION `v4.3.2A -> v4.3.2B` (production data metadata changed in 1 scenario; v4.3.2A already deployed; bump invalidates stale cache so users pick up corrected classification). v4.3.1B `@media (max-width: 359px)` mobile fix preserved unchanged. **No runtime logic / UI / taxonomy / concepts / schema change.** **Audits all green:** production 477/0/0 PASS; M4 continuation seed (v4.3.2) 20/0/0 PASS; M4 polish (v4.3.0D) 19/0/0 PASS unchanged; M4 expansion (v4.3.0C) 29/0/0 PASS unchanged; M4 original seed (v4.3.0) 24/0/0 PASS unchanged; M3 24/0/0 PASS clean; M2 24/0/8 PASS; R29/R71/R72/R44b 0 fires across 92 M4; M4.R73/M4.R74/M4.R75 0 fires across 20 v4.3.2 continuation. **Targeted raw scan PASS:** R1 board.turnCategory='straight_complete' (not draw_intensifier); R1 board.boardChange='polarizing' (not draw_added); R1 board.drawCompletion='straight_completed' (not oesd_added); R1 board.equityShift='polarizes_btn' (not improves_bb_draws); R1 prose preserved from v4.3.2A (call/bluff_catch_turn vs straight-complete turn); R1 recommendedAction/actionReason/heroHandRole/showdownValue all preserved from v4.3.2A. **Forbidden files all byte-identical:** all v4.3.0/C/D builders + JSONs, all migration tools (B/C/D/v4.3.2), v4.3.0C1 hotfix tool, v4.3.2A hotfix tool (`hotfix-module4-v4.3.2A.ps1`), audit-postflop-ps.ps1 (R55-R72 unchanged), M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json (51 concepts), postflop_taxonomy.json. v4.3.2 doc + v4.3.2A doc byte-identical (not re-edited). New tool `tools/hotfix-module4-v4.3.2B.ps1`. New doc `docs/specs/postflop-v4.3.2B-module4-metadata-hotfix.md`. New snapshot `GPT AUDIT/v4.3.2B/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2B. Module 4 = 92 scenarios with all strategic verdicts AND all board metadata classifications now poker-correct and internally consistent. **Recommendation unchanged from v4.3.2 / v4.3.2A:** pause content expansion and collect M4 Limited Beta user signals via the existing Beta Lab dashboard for at least one feedback cycle before deciding v4.3.3 vs v4.4.0. Predecessors: `v4.3.2A-doc` (`d074e56`); `v4.3.2A` (`e8975dd`); `v4.3.2-doc` (`0859d2c`); `v4.3.2` (`12ff7c1`); `v4.3.1B-doc` (`bef1765`); `v4.3.1B` (`e9fadfc`); `v4.3.1A-doc2` (`f11c154`); `v4.3.1A-doc` (`895cc88`); `v4.3.1A` (`59b8184`); `v4.3.1-doc` (`1f2689d`); `v4.3.1` (`c1998cf`); `v4.3.0D-doc` (`9792758`); `v4.3.0D` (`023430a`); `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.2B:** Five consecutive sprints with strategic-review-catches-mechanical-audit-PASS-bugs (v4.3.0C1 prose; v4.3.1A render framing; v4.3.1B layout overflow; v4.3.2A strategic verdicts; v4.3.2B metadata leftover). Pattern lesson: **content corrections must include a metadata-consistency follow-up pass.** v4.3.2A fixed the strategic verdict + prose for R1 but left the board sub-document with stale draw_intensifier classification because the board metadata wasn't part of the same field-set the strategic-review process explicitly checks. v4.3.2B's M4.R75 catches this future class of bug mechanically. Three pattern lessons banked: (1) **Same physical board can carry different turnCategory across scenarios** -- when the strategic verdict frames the turn differently (draw-intensifier for NFD/set; straight-complete for bluff-catcher), the metadata SHOULD reflect that framing axis. The auditor permits this through scoped rule design (R75 only fires on the bluff-catcher framing). (2) **Hotfix scope discipline** -- v4.3.2A had narrow scope (strategic verdict + prose) and shipped fast; the metadata follow-up was easy to defer/forget. Future strategic-correction sprints should include an explicit "metadata audit pass" checklist item. (3) **Auditor enum substitution should be documented** -- the brief proposed `boardChange: draw_completed` and `equityShift: polarizes_range` which are NOT in the approved enums; substituted to closest-valid `polarizing` and `polarizes_btn` with explicit rationale in v4.3.2B doc. Future enum extensions (if needed) should require a separate proposal sprint that updates concepts/taxonomy JSON and the production R55-R72 auditor coordinately, not ad-hoc inside a hotfix. **Module 4 stays Limited Beta** with all 92 scenarios now strategically AND metadata-consistent. Recommendation unchanged: collect real-user signals via Beta Lab before deciding v4.3.3 vs v4.4.0.

- **Last committed + pushed (HISTORICAL, MODULE 4 CONTINUATION RAW POKER-CONTENT HOTFIX)**: `v4.3.2A` (`e8975dd`) — Module 4 Continuation Raw Poker-Content Hotfix on top of v4.3.2 (`12ff7c1`). **2 scenario-level content defects fixed via canonical-source-first hotfix; production count + M4 count unchanged at 477 / 92.** Two raw poker misreads survived v4.3.2's structurally-green audit and were caught in strategic review. **Defect 1 (R1 = pf_btn_v_bb_srp_100bb_turn_Ts8s4d_7c_m4_reason_QcQd_v432):** v4.3.2 mislabelled the 7c turn on Ts 8s 4d as a draw-intensifier with OESDs (98, J9) and gutshots (96, 65). In reality 7c COMPLETES multiple straights: J9 makes 7-8-9-T-J, 96 makes 6-7-8-9-T, 65 makes 4-5-6-7-8 — only 98 is genuinely pair+OESD. QQ overpair-to-T is BEHIND every made straight + every set; raising folds out air bluffs (which we beat) and isolates against straights (which crush us). **Strategic verdict reverted from check_raise_small/protection_check_raise_turn -> call/bluff_catch_turn**; heroHandRole strong_value -> bluff_catcher; conceptTags rebalanced; full prose rewrite acknowledging completed straights. **Defect 2 (R2 = pf_btn_v_bb_srp_100bb_turn_9c6c3h_8c_m4_reason_7s5s_v432):** v4.3.2 mislabelled hero 7s 5s with bottom straight 5-6-7-8-9 as nutted_value/showdownValue:nutted and described "charging made flushes" for value. In reality the 8c turn completes a 3-club flush; hero holds ZERO clubs (7s5s = both spades) so hero has NO flush blocker and LOSES to every made flush + to T7 made T-high straight. Made flushes are NOT value targets. **Strategic verdict reverted from check_raise_small/value_check_raise_turn -> call/bluff_catch_turn**; heroHandRole nutted_value -> bluff_catcher; showdownValue nutted -> high; conceptTags rebalanced (turn_check_raise_value -> turn_bluff_catcher); full prose rewrite explicitly stating hero loses to flushes + T7. **Source-of-truth-first fix:** corrected canonical builder `tools/build-m4-continuation-v4.3.2.ps1` (R1 + R2 scenario blocks); regenerated `docs/specs/postflop-v4.3.2-module4-continuation-seeds.json`; new idempotent narrow-scope `tools/hotfix-module4-v4.3.2A.ps1` re-syncs the 2 target IDs into production preserving 90 of 90 non-target M4 byte-identical (verified) + 385 non-M4 untouched + scenario-array order preserved. **NEW M4.R73 + M4.R74 auditor guards added:** R73 flags non-flush made hands (handClass=straight/set/two_pair/top_two_pair/trips/full_house) on monotone or flush-completed turns where hero holds ZERO cards of the completed suit but is labelled heroHandRole=nutted_value or showdownValue=nutted (would have caught Defect 2 pre-migration). R74 prose-scans for "made flushes as value target" patterns when hero handClass != flush/nut_flush/full_house (would have caught Defect 2 prose pre-migration). Both WARN-level (not HARD) to avoid rare-edge-case false positives. **Hotfix tool guarantees:** idempotent (safe re-run); no Invoke-Expression; no unsafe Remove-Item; UTF-8 NO-BOM atomic tmp+Move-Item write; pre-flight verifies prod=477/M4=92/all approved/target IDs present in source+production; post-write verifies field-equality vs source on syncFields + zero-drift on 90 non-target M4 scenarios + scenario-array order preserved. **Cache bump only:** appVersion 4.3.2 -> 4.3.2A; SW VERSION v4.3.2 -> v4.3.2A (production data prose changed in 2 scenarios; v4.3.2 already deployed; bump invalidates stale cache so users pick up corrected verdicts). v4.3.1B `@media (max-width: 359px)` mobile fix preserved unchanged. **No runtime logic / UI / taxonomy / concepts / schema change.** **Audits all green:** production 477/0/0 PASS; M4 continuation seed (v4.3.2) 20/0/0 PASS (CR.R03 drift cleared post-hotfix); M4 polish (v4.3.0D) 19/0/0 PASS unchanged; M4 expansion (v4.3.0C) 29/0/0 PASS unchanged; M4 original seed (v4.3.0) 24/0/0 PASS unchanged; M3 24/0/0 PASS clean; M2 24/0/8 PASS; R29/R71/R72/R44b 0 fires across 92 M4; M4.R73/M4.R74 0 fires across 20 v4.3.2 continuation. **Targeted text/poker scan PASS:** R1 no J9/96/65 framed as draws; R2 no "made flushes" framed as value target; R2 heroHandRole=bluff_catcher (not nutted_value) and showdownValue=high (not nutted). **Forbidden files all byte-identical:** all v4.3.0/C/D builders + JSONs, migration tools (B/C/D/v4.3.2), v4.3.0C1 hotfix tool, audit-postflop-ps.ps1 (R55-R72 unchanged), M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json (51 concepts), postflop_taxonomy.json. v4.3.2 doc `docs/specs/postflop-v4.3.2-module4-coverage-continuation.md` byte-identical (not re-edited). New tool `tools/hotfix-module4-v4.3.2A.ps1`. New doc `docs/specs/postflop-v4.3.2A-module4-content-hotfix.md`. New snapshot `GPT AUDIT/v4.3.2A/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2A. Module 4 stays Limited Beta -- 92 scenarios with all strategic verdicts now poker-correct. **Recommendation unchanged from v4.3.2:** pause content expansion and collect M4 Limited Beta user signals via the existing Beta Lab dashboard for at least one feedback cycle before deciding v4.3.3 (further continuation) or v4.4.0 (M5 river-defense architecture). Predecessors: `v4.3.2-doc` (`0859d2c`); `v4.3.2` (`12ff7c1`); `v4.3.1B-doc` (`bef1765`); `v4.3.1B` (`e9fadfc`); `v4.3.1A-doc2` (`f11c154`); `v4.3.1A-doc` (`895cc88`); `v4.3.1A` (`59b8184`); `v4.3.1-doc` (`1f2689d`); `v4.3.1` (`c1998cf`); `v4.3.0D-doc` (`9792758`); `v4.3.0D` (`023430a`); `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.2A:** Mechanical-PASS-is-not-enough discipline reinforced AGAIN -- this is the fourth sprint in a row where strategic review > mechanical audit (v4.3.0C1 prose bugs, v4.3.1A render-path framing bug, v4.3.1B layout overflow bug, v4.3.2A two raw poker misreads). Three pattern lessons banked from this hotfix: (1) **Turn-completion analysis must enumerate WHICH straights/flushes the turn lands** -- the v4.3.2 R1 author treated 7c on T-8-4 as "draw-intensifier" without listing what 65/96/J9 actually do (they're MADE STRAIGHTS, not draws). The auditor's turn-category metadata IS in the schema but represents only the dominant axis; the strategic-review human/AI must independently enumerate completed-straight combinatorics. (2) **Flush-complete monotone turns + non-flush made hand + zero suit blocker = NEVER nutted** -- this is now mechanically encoded as M4.R73 WARN. R2 (7s5s nutted_value on 9c6c3h8c) was the first instance to expose this pattern; R73 prevents future occurrences. (3) **Prose patterns reveal cognitive errors** -- "charges made flushes" when hero is non-flush is a tell that the author misunderstood who's ahead. M4.R74 prose-scan catches this class of error. **Auditor evolution roadmap going forward:** the production auditor (audit-postflop-ps.ps1, R55-R72) is held byte-stable across v4.3.x to avoid retro-fail surprises on already-approved scenarios; new content-quality rules ride along the per-sprint seed auditors (continuation/polish/expansion) for catch-at-author-time enforcement. Combined-axis quadruple-uniqueness check (board, role, reason, action) remains the next major auditor evolution target. **Module 4 stays Limited Beta** with all 92 scenarios now strategically defensible. Recommendation unchanged: collect real-user signals via Beta Lab before deciding v4.3.3 vs v4.4.0.

- **Last committed + pushed (HISTORICAL, MODULE 4 COVERAGE CONTINUATION 72 -> 92)**: `v4.3.2` (`12ff7c1`) — Module 4 Coverage Continuation 72 -> 92 on top of v4.3.1B (`e9fadfc`). **20 NEW M4 scenarios authored, audited, strategically reviewed, and migrated to production.** Continuation seeds canonical at NEW `tools/build-m4-continuation-v4.3.2.ps1` (separate from v4.3.0 baseline / v4.3.0C expansion / v4.3.0D polish builders -- source-of-truth integrity preserved). NEW continuation seed JSON `docs/specs/postflop-v4.3.2-module4-continuation-seeds.json`. NEW continuation auditor `tools/audit-postflop-module4-continuation-v4.3.2.ps1` mirrors v4.3.0D polish auditor with v4.3.2-specific id-pattern (`_v432`) and reviewStatus (`v4.3.2_continuation_candidate`); cross-corpus duplicate-ID check vs production AND vs v4.3.0D polish AND vs v4.3.0C expansion; CR.R03 drift check; bidirectional R71 nut-FD WARN; R72 text-integrity HARD; R44b reason_choice answer-id integrity HARD. NEW migration tool `tools/migrate-module4-v4.3.2.ps1` -- idempotent, no Invoke-Expression, no unsafe Remove-Item, two-phase staged approval (review_pending then approved). **Production count 457 -> 477** (251 M1 + 49 M2 + 85 M3 + **92 M4** = 24 baseline + 29 expansion + 19 polish + 20 continuation). **Coverage targets all hit:** reason_choice 13 -> 21 (18.1% -> 22.8%), blocker_check_raise_turn 3 -> 6, mixed_indifference_turn 4 -> 7, check_raise_big 2 -> 3, all 12 actionReasons covered with healthy distribution, 19 unique flop families (was 17; +F12 Kc 7s 2d / Qh overcard demote, +F13 9c 6c 3h / 8c flush+straight complete simultaneously). **Critical density 72.2% -> 69.6%** (recalibrated within 65-75% target band; 12 of 20 v4.3.2 carry critical = 60% batch density). **All 20 sourceConfidence=expert_judgment** (no consensus_gto promotion -- combined corpus stays 8/92 = 8.7% consensus_gto). **Strategic review verdict: 20/20 PROMOTE.** 2 audit-driven REVISE during build (R3 drawCategory `gutshot -> backdoor_only` after re-verification that hero 89 + board 2-4-7-A produces zero one-card straights; A5 hero `KsQs -> AsQs` after R52 invariant flagged that K-spade is not the nut-FD-Ace); R7 R72 prose artifact "wait -- recompute" cleaned to crisp blockerNote; A1 `acceptable=[call]` added (NFD makes call defensible even though best stays fold). 2 NEW board families F12/F13 with 4 + 3 v4.3.2 scenarios respectively; existing F1/F4/F5/F6/F7/F8/F9/F10/F11 each reused for 1-2 v4.3.2 scenarios with explicit uniquenessNote distinguishing each from baseline. **Audits all green:** production 477/0/0 PASS; M4 continuation 20/0/0 PASS; M4 polish 19/0/0 PASS unchanged; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged; M3 24/0/0 PASS clean unchanged; M2 24/0/8 PASS unchanged; R29/R71/R72/R44b 0 fires across 92 M4. **Cache bump:** appVersion `4.3.1B` -> `4.3.2`; SW VERSION `v4.3.1B` -> `v4.3.2` (production data changed; v4.3.1B already deployed to Netlify; bump invalidates stale cache for users to pick up the new 92-scenario pool). v4.3.1B `@media (max-width: 359px)` single-column TCC fix preserved unchanged at index.html line 11801-11804. **No runtime logic change**, no UI change beyond version-string bump, no taxonomy/concepts edits, no schema change. **Forbidden files all byte-identical:** all v4.3.0 / v4.3.0C / v4.3.0D builders + JSONs, all migration tools (B/C/D), hotfix tool, audit-postflop-ps.ps1 (R55-R72 unchanged), M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json (51 concepts), postflop_taxonomy.json. New doc `docs/specs/postflop-v4.3.2-module4-coverage-continuation.md`. New snapshot `GPT AUDIT/v4.3.2/` (local-only, gitignored). **Final ship verdict:** SHIP v4.3.2. Module 4 stays Limited Beta -- 92 scenarios is approaching mature-runtime-beta density. **Project-owner recommendation:** collect M4 Limited Beta user signals via the existing Beta Lab dashboard for at least one cycle before deciding between v4.3.3 (further continuation) or v4.4.0 (M5 river-defense architecture). Predecessors: `v4.3.1B-doc` (`bef1765`); `v4.3.1B` (`e9fadfc`); `v4.3.1A-doc2` (`f11c154`); `v4.3.1A-doc` (`895cc88`); `v4.3.1A` (`59b8184`); `v4.3.1-doc` (`1f2689d`); `v4.3.1` (`c1998cf`); `v4.3.0D-doc` (`9792758`); `v4.3.0D` (`023430a`); `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.2:** Quality-discipline continuation, not raw-count expansion. The 20 new scenarios were designed gap-first (reason_choice depth +8, blocker_check_raise +2 action_choice, mixed +3, check_raise_big +1, action gap-fills +6) rather than family-first. Three pattern lessons banked: (1) **Reason_choice diagnosis is the primary teaching axis for mature modules** -- the 8 reason_choice spots in v4.3.2 each test discrimination between 2-3 plausible reasons (e.g. R3 range_disadvantage vs board_change vs domination on a brick fold; R7 slowplay vs value_check_raise on a polar straight-complete). This is harder strategic content than action_choice and reflects M4 maturing toward "diagnostic" rather than "decision" practice. (2) **The strategic-review-after-audit pattern keeps surfacing real bugs** that mechanical audits cannot catch. v4.3.0C had 2 prose/draw bugs survive; v4.3.1A had a render-path framing bug survive; v4.3.1B had a layout-overflow bug survive; v4.3.2 had R3 drawCategory + A5 KsQs + R7 R72 + A1 acceptable-array all caught at strategic review. The auditor must KEEP growing teeth -- specifically a `blockerNote_consistency` check that validates "nut spade blocker" claims actually match A-of-spade in hero hand (A5 would have been caught pre-audit). (3) **Combined corpus dynamics matter at 92 scenarios** -- single-axis distribution is not enough; combined `(board, role, reason, action)` quadruples must be unique to avoid filler. Spot-check across 6 blocker_check_raise spots confirmed 6 distinct boards + dynamics. This combinatorial-uniqueness check is the next auditor evolution. **Module 4 stays Limited Beta** -- 92 scenarios is in the "stable-runtime-beta" range but not yet "full release" mature; the recommendation is to PAUSE content expansion and collect real-user weak-spot data via the Beta Lab dashboard for at least one feedback cycle before deciding the next direction.

- **Last committed + pushed (HISTORICAL, M4 REAL-DEVICE MOBILE QA + UX POLISH)**: `v4.3.1B` (`e9fadfc`) — Real-Device Mobile QA + UX Polish on top of v4.3.1A (`59b8184`). **One mobile-layout bug found in iframe-based 320/360/375/414 QA harness, fixed in index.html.** Real-device mobile QA + UX polish brief executed across all 4 device-class viewports per the v4.3.1B brief (320 = iPhone SE 1st gen / iPhone 5 / very narrow Android; 360 = typical Android mid-range; 375 = iPhone X-15 base; 414 = iPhone Plus / Pro Max). QA stack: same `.qa-serve.ps1` PowerShell HTTP server at `http://localhost:8765/` (gitignored via `*.ps1`); NEW `.qa-harness.html` iframe-based harness (each iframe gets its own viewport so CSS media queries fire correctly inside, working around Chrome's MCP `resize_window` not propagating to inner viewport); Claude-in-Chrome MCP browser tools driving programmatic injection of postflop drill state via `iframe.contentWindow.eval(...)` (because top-level `const App` is lexically scoped, not on `window`). M4 mobile QA covered: entry / question screen (4-card board fits at 320, TURN amber pill positions cleanly above 4th card without occluding rank/suit, 5-step spot-tag chip flow wraps cleanly, hand-class chip row fits, BB-OOP framing intact post-v4.3.1A) / answer feedback (BAD-tier verdict pill + "Your pick / GTO best" + yellow headline + Recommended Action chip + TURN LOGIC prominent + HAND/SIZING/BLOCKER/Range/Takeaway/Common-Mistake blocks + concept tags) / session complete (Module 4 header + 2x2 tier-chip grid + Review Signals + Recommended Next Move + M4 Turn Defense Breakdown + Drill Weak Spots / Drill again / Back to Home) / Concept Library (full M4 12-concept group renders with bold title + 3-line description + amber "trained in Module N" pill + full-width "Drill this concept" button) / Academy + BetaQA (Beta Lab section + Postflop Academy header + YOUR PROGRESS + RECOMMENDED NEXT STEP + CURRICULUM cards with Module 4 BETA pill + M4 mastery checklist parallel to M1/M2/M3). M1/M2/M3 + Preflop regression at 320/360/375/414: M1 (board texture) / M2 (flop c-bet IP) / M3 (BB defense OOP) / M4 (turn defense OOP) all render cleanly; Drill / Browse / Settings tabs all render cleanly. Cache/update QA verified working-as-designed: SW activate handler's `caches.keys().filter(k => k !== CACHE_NAME).map(k => caches.delete(k))` cleanup leaves only the current cache after `SKIP_WAITING` + reload; no fix needed. **Bug #1 found:** at 320 px CSS viewport, `.tcc-action-grid` (CSS at index.html ~line 11700) is `display:grid; grid-template-columns: 1fr 1fr` and the default `1fr` is `minmax(auto, 1fr)` where `auto` ≈ min-content. Tile min-content (`icon 26 + gap 8 + label-min ~50 + gap 8 + badge ~40 + padding 24 ≈ 156 px`) exceeds the panel's content area (~220 px) at 320 width, causing right-column tiles to overflow ~36 px past the panel's right edge under existing `body { overflow-x: hidden }`. User-visible consequences: "Concept Library", "Postflop Progress", and **"Module 4 · Facing Turn Barrel OOP" tile + BETA badge + "72 turn-defense scenarios · Limited Beta" subtitle** all clipped on the right edge. **Severity:** layout/visual bug (M4 entry tile partially hidden on iPhone SE 320), not data or grading. Pre-existing layout fragility, not a v4.3.1 regression — but became user-visible because v4.3.1 added the long-copy M4 tile to the right column. **Fix:** new `@media (max-width: 359px)` block (lines 11790-11806 of index.html) with `.tcc-action-grid { grid-template-columns: 1fr; } .tcc-action.is-primary { grid-column: 1; }`. At <360 width all 7 TCC tiles stack single-column with full readable copy + BETA badge; at 360+ unchanged (2-column grid preserved); at 720+ unchanged (3-column via existing `@media (min-width: 720px)`). **Fix verification:** w320 maxRight=261.1 (was 356.5, now fits within 320); w360 maxRight=356.5 < 360 unchanged; w375 maxRight=356.5 < 375 unchanged; w414 maxRight=372.2 < 414 unchanged; visual at 320 confirms M4 tile + BETA badge + subtitle fully visible end-to-end. **Cache bump:** appVersion `4.3.1A` → `4.3.1B`; SW VERSION `v4.3.1A` → `v4.3.1B` (CSS asset changed; v4.3.1A already deployed; bump required to invalidate stale Netlify cache so fix reaches users). **Programmatic tap-target audit:** all primary controls ≥ Apple HIG 44x44 across all 4 widths — `.postflop-choice-btn` 56x271..366 PASS; `.postflop-exit-btn` 49x44..58x44 PASS; `.postflop-next-btn` (Finish) 44x271..336 PASS; `.postflop-summary-btn` (Drill Weak) 49x271..366 PASS; `.postflop-restart-btn` (Drill again) 47x271..366 PASS; `.bottom-nav-item` 53x61 PASS. Three tertiary disclosures fall slightly under HIG (`.pf-teach-hint-row` 33; `.pf-rng-toggle` 36; `.postflop-back-btn` 41) — not v4.3.1 regressions, not in scope of M4 mobile-polish brief, documented for future polish sprint, NOT FIXED in v4.3.1B. **Programmatic horizontal-overflow probe:** body.scrollWidth ≤ innerWidth at all 4 widths post-fix; no element extends past viewport on the postflop screen at any width. **Audits all green:** production 457/0/0 PASS unchanged from v4.3.1A (no scenario data changed); all 5 seed audits unchanged; R29/R71/R72/R44b 0 fires across 72 M4. Production scenarios JSON byte-identical to v4.3.0D (blob `d13bf697b561...`). **Final ship verdict:** SHIP v4.3.1B. Module 4 IS PLAYABLE on iPhone SE 320 with the M4 entry tile fully readable; honest Limited Beta copy preserved end-to-end. **Forbidden files all byte-identical:** all v4.3.0/v4.3.0C/v4.3.0D builders + JSONs, all migration tools, hotfix tool, audit-postflop-ps.ps1, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json, postflop_scenarios.json. New doc `docs/specs/postflop-v4.3.1B-real-device-mobile-qa.md`. New snapshot `GPT AUDIT/v4.3.1B/` with INHERITANCE_NOTE.txt + MANIFEST_SHA256.txt. Predecessors: `v4.3.1A-doc2` (`f11c154`); `v4.3.1A-doc` (`895cc88`); `v4.3.1A` (`59b8184`); `v4.3.1-doc` (`1f2689d`); `v4.3.1` (`c1998cf`); `v4.3.0D-doc` (`9792758`); `v4.3.0D` (`023430a`); `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.1B:** Real-device mobile QA matters as much as live browser QA. v4.3.1A's live QA caught a render-path copy bug (BB-vs-BTN framing) that audits couldn't see; v4.3.1B's mobile QA caught a layout fragility (CSS grid track overflow at 320) that the desktop-or-emulator QA at v4.3.1A also couldn't see because it was still running at desktop viewport widths inside the emulator. Three pattern lessons banked: (1) **iframe-based mobile QA harness works** — Chrome's MCP `resize_window` does not propagate to the inner viewport (page-level innerWidth stays at 2560 even after resize), but each iframe gets its OWN independent viewport sized by its CSS width attribute, so CSS media queries (`max-width: 320 / 360 / 380 / 480`) fire correctly inside. The `.qa-harness.html` pattern (4 iframes side-by-side at 320/360/375/414, all loading the same local server) gives true per-width responsive QA in a single Chrome window. This workflow should be the standard for any sprint that touches CSS or layout. (2) **Min-content overflow is the silent killer in CSS Grid** — `1fr` defaults to `minmax(auto, 1fr)`, which means tracks WILL grow past the parent if a child's min-content exceeds the available 1fr share. Combined with `body { overflow-x: hidden }`, the overflow is invisible to the user but text is silently clipped. Going forward, any new entry added to `.tcc-action-grid` (or any 2-column grid with content-bearing tiles) needs a sanity check at 320 px width before ship; the iframe harness now gives us a 30-second probe for that. (3) **Pre-existing fragility surfacing on new content is normal** — the M3 + Concept Library + Postflop Progress tiles all clipped at 320 too, but the v4.3.1 M4 tile (long copy + BETA badge) made the clip visually obvious at the bottom-right of the grid. The fix is module-agnostic (single-column at <360 helps M3, M4, and any future M5/M6 entries equally) and respects the brief's "minimal CSS-only" constraint (one new media query block, no JS, no scenario data, no taxonomy, no audit logic, no schema, no strategy field). **Module 4 IS PLAYABLE on iPhone SE 320.** Final ship verdict: SHIP v4.3.1B.

- **Last committed + pushed (HISTORICAL, MODULE 4 LIMITED BETA LIVE QA + SHIP)**: `v4.3.1A` (`59b8184`) — Live QA + Ship hotfix on top of v4.3.1 (`c1998cf`). **One bug found in live browser QA, fixed in index.html.** Programmatic + interactive Chrome-MCP browser QA executed all 16 sections of the v4.3.1-qa brief: baseline gates (production 457/0/0, all 4 M4 audits + M2/M3 + R29/R71/R72/R44b), local server start, app load + console-clean check (0 errors), Postflop TCC panel + M4 [BETA] tile + "72 turn-defense scenarios · Limited Beta" copy, M4 route launch (`startPostflopDrill('pf_turn_barrel_oop_def', 12)` produces `module='pf_turn_barrel_oop_def'`, `queueLen=12`, `allM4=true` no leakage), 4-card board with TURN amber pill on index 3, 5-step spot-tag chip flow (BB OOP / BTN IP / SRP / BTN open 2.5x · BB call / BTN cbet small · BB call / BTN barrels turn), hero-hand suit symbols correct (As→A♠, 6c→6♣ verified DOM-level), all 4 grading tiers (best/acceptable/bad/critical) tested action_choice + reason_choice with correct UI banners and tier classifications (best=1.00 green, acceptable=0.50 amber, bad=0.00, critical=0.00 red+critical-feedback), R44b reason_choice answer-id integrity 0 violations runtime, 8-block feedback layout via `_pfM4TeachingFeedbackBlocksHtml` (Recommended Action / **Turn Logic PROMINENT 🎯** / Hand / Sizing / Blocker / Range / Takeaway / Common Mistake), M4 session summary (handClass + heroHandRole + actionReason coverage breakdown, "Module 4 · Turn Defense Breakdown (Limited Beta)" header, Drill Weak Spots + Drill again routing to M4), M4 concept drill (12 concepts, all `previewOnly=false`, drillable; turn_blocker_pressure pulls 35 candidate scenarios M4-only with allM4=true; queueLen=12), M4 weak-spot review empty-state toast + active path with 5 distinct target reasons + M4-only pool, M4 mastery + BetaQA dashboard render with correct labels/thresholds (4 sessions / 75% in 2 / 9-of-12 reasons / weak-review / no critical), no NaN/undefined in dashboard HTML, M1/M2/M3 regression with all 4 modules grading bestId correctly via normalizer (251/49/85/72 pool sizes), mobile column-width 320/360/375 layout (no horizontal overflow at document level, 5-step spot-tag chip row wraps cleanly, 4-card board fits with TURN pill not occluding rank/suit, action buttons stack and fill width, feedback blocks readable), desktop 1280x900 (0 console errors, 0 warnings, no overflow, M4 surfaces don't overcrowd Academy), Netlify auto-deploy verified active (live appVersion=4.3.1, M4 pool 72, M4 in TRAINING_MODES). **Bug #1 found:** M4 question prompt rendered with BTN framing ("what should BTN do most often?") because `_pfBuildQuestionPrompt` had a special M3 branch but no parallel M4 branch — M4 fell through to the M1/M2 BTN-framing default. **Severity:** copy/text bug, not grading or data bug — the classifier worked correctly throughout. **Fix:** added `isM4 = scenario.module === 'pf_turn_barrel_oop_def'` branch in `_pfBuildQuestionPrompt` mirroring the M3 branch with turn-defense framing; extracts `flopCards` and `turnCard` from `board.flopCards` / `board.turnCard` so flop and turn render separately ("BB has [hero] on [flop], turn [turn] (BTN open 2.5x, BB call, 100BB SRP). After flop check-call, BTN now barrels turn. What is BB's best action OOP?"). Programmatic verification confirmed fix works for both action_choice and reason_choice qtypes; M3/M2/M1 paths byte-identical (additive only). **Cache bump:** appVersion `4.3.1` → `4.3.1A` and SW VERSION `v4.3.1` → `v4.3.1A` (per Section P: "fix in index.html, rerun focused QA, bump appVersion/SW only if deployed cache needs invalidation"). Live Netlify v4.3.1 had the bug; the v4.3.1A push triggered Netlify auto-redeploy and live is now verified at appVersion 4.3.1A with the fix in place. **Audits all green:** production 457/0/0 PASS unchanged; all 5 seed audits unchanged; R29/R71/R72/R44b 0 fires across 72 M4. Production scenarios JSON byte-identical to v4.3.0D (no data changes). **Final ship verdict:** SHIP v4.3.1A. Module 4 IS PLAYABLE as Limited Beta with correct BB-OOP framing throughout; honest Limited Beta copy preserved. **Forbidden files all byte-identical:** all v4.3.0/v4.3.0C/v4.3.0D builders + JSONs, all migration tools, hotfix tool, audit-postflop-ps.ps1, M2/M3/M4 seed auditors, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json. New doc `docs/specs/postflop-v4.3.1-qa-results.md`. New snapshot `GPT AUDIT/v4.3.1A/` with INHERITANCE_NOTE.txt + MANIFEST_SHA256.txt. Predecessors: `v4.3.1-doc` (`1f2689d`); `v4.3.1` (`c1998cf`); `v4.3.0D-doc` (`9792758`); `v4.3.0D` (`023430a`); `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.1A:** Live browser QA matters. The v4.3.1 audits were 100% green (production 457/0/0, all M4 seed audits 0/0/0, R29/R71/R72/R44b all 0 fires) but a real user-facing copy bug was sitting in the question-prompt rendering path that no audit could detect because audits only check the source data fields, not the runtime render path. The bug was BB-vs-BTN framing — a learner reading "what should BTN do most often?" on a Module 4 question would be confused because Module 4 hero is BB (defending OOP). The fix is mechanically minimal (one new branch parallel to the existing M3 branch) but the lesson is large: **runtime QA is independent from data QA**. Three pattern lessons banked: (1) **module-aware runtime branching needs explicit registration** — the M3 branch in `_pfBuildQuestionPrompt` was added in v4.2.4 (`if (isM3)`) but the v4.3.1 wire didn't add a parallel `isM4` branch. The lesson: when wiring a new module, audit every module-aware function in index.html for missing parallel branches. (2) **Browser QA via MCP is now reproducible** — the QA workflow used a local PowerShell HTTP server + Claude-in-Chrome MCP browser tools, captured screenshots at multiple viewports, programmatically verified DOM text + suit-symbol rendering, simulated injected drill states for grading + session-summary verification. This workflow should be the standard going forward for any runtime-wire sprint. (3) **Cache invalidation discipline** — v4.3.1 was already deployed to Netlify when the bug was found. The fix triggers a v4.3.1A bump (appVersion + SW VERSION) so the deployed PWA cache invalidates and learners pick up the corrected version. The bump is cosmetic at the data layer (no scenario changes) but mandatory at the runtime layer (changed render path). **Module 4 IS PLAYABLE as Limited Beta.** Final ship verdict: SHIP v4.3.1A.

- **Last committed + pushed (HISTORICAL, MODULE 4 LIMITED BETA RUNTIME WIRE)**: `v4.3.1` (`c1998cf`) — Module 4 Limited Beta Runtime Wire. **Runtime-only sprint on top of v4.3.0D (`023430a`).** Wires the existing 72-scenario M4 corpus as a playable Limited Beta. NO scenario data changes. NO migrations. NO concept/taxonomy JSON edits. **Production count unchanged at 457** (251 M1 + 49 M2 + 85 M3 + 72 M4); production scenarios JSON byte-identical to v4.3.0D manifest. Runtime additions (all in index.html + service-worker.js): (1) **`getModule4Scenarios()`** approved-only filter; (2) **`_PF_M4_REASON_LABELS`** map for the 12 _turn-suffixed actionReasons + extended `_pfNormalizePostflopChoices` lookup; (3) **`_pfM4TeachingFeedbackBlocksHtml`** with `turnLogic` PROMINENT (parallel to M3's `defenseLogic`) — 8 blocks: Recommended Action / Turn Logic / Hand Logic / Sizing / Blocker Note / Range Context / Takeaway / Common Mistake; (4) **M4 first-time explainer** (`_pfM4SessionCount` / `_pfM4IsFirstTime` / `_pfM4FirstTimeExplainerHtml`) with BB-OOP turn-defense framing, 5-action menu, 12 reasons, 4-card board guidance, "Limited Beta — 72 scenarios" copy; (5) **M4 mastery checklist** (`_pfM4MasteryStats` / `_pfM4MasteryProgress` / `_pfM4MasteryProgressHtml`) with Limited Beta thresholds: 4 sessions / 75% in 2 sessions / 9-of-12 actionReasons / weak-review used / no critical leaks in latest; (6) **M4 BetaQA dashboard** (`_pfM4BetaQAStats` thin wrapper around `_pfBetaQAStatsForModule` + `_pfM4BetaQAWeakSpotPreview` / `_pfM4BetaQACriticalMonitor` / `_pfM4BetaQADashboardHtml` / `_pfM4BetaQACopySnapshotClick` / `_pfM4BetaSessionLeakHtml`) — 6-metric grid + per-axis breakdowns + critical monitor + last-3-sessions + clipboard JSON snapshot, "early signal" banner under 4 sessions / 36 answers; (7) **M4 session aggregations** (`_pfM4RenderSessionAggregations`) with handClass / heroHandRole / actionReason buckets; (8) **action_choice_m4** + **reason_choice_m4** entries in `_pfChoiceGuide`; (9) **TRAINING_MODES.postflop.actions** gains M4 entry with route `postflop:m4` + BETA badge + hint "72 turn-defense scenarios · Limited Beta"; (10) **`runTrainingModeAction`** case `'postflop:m4'` → beta-gate → `startPostflopDrill('pf_turn_barrel_oop_def', 12)`; (11) **`startPostflopDrill`** module dispatch extended with `pf_turn_barrel_oop_def` → 12-question default; (12) **`startPostflopConceptDrill`** + **`startPostflopWeakSpotReview`** extended with M4 routing + M4 empty-state toast "Play Module 4 sessions to unlock Turn Defense weak-spot review"; (13) **`renderPostflopQuestion`** + **`renderPostflopAnswer`** + **`renderPostflopComplete`** extended with `isM4` branches: "🎯 Module 4 · Facing Turn Barrel OOP · Limited Beta · Q n/N" context label, "Your Hand (BB)" hero hand title, drawCategory + showdownValue chips, 5-step spot-tag flow (BB OOP / BTN IP / SRP / BTN open 2.5x · BB call / BTN cbet small · BB call / BTN barrels turn), 4-card board with turn-card pill on index 3, M4 first-time explainer on Q1, M4 teaching feedback routing, M4 module label on session summary, M4 aggregations + leak hint, M4 "Drill again" route; (14) **12 M4 entries added to `_PF_CONCEPT_LIBRARY`** (turn_equity_shift / second_barrel_defense / turn_pot_odds / turn_bluff_catcher / turn_domination_fold / turn_board_change / turn_draw_completion / turn_check_raise_value / turn_check_raise_bluff / turn_blocker_pressure / turn_slowplay_call / turn_range_disadvantage; all module='m4', previewOnly=false, drillable); (15) **`_pfConceptLibraryHtml`** updated to render M4 group "Module 4 — Turn Defense OOP (12 · Limited Beta)" + library summary "(N concepts · 10 M1 + 5 M2 + 9 M3 + 12 M4)"; (16) **`_PF_CURRICULUM` M4 entry flipped** from "Future module" to Limited Beta status with masteryNote "Limited Beta · 72 scenarios · 12 actionReasons · 16 boards" + 8-line syllabus; (17) **`_pfModuleStatus('m4')`** now returns `'beta'`; (18) **`_pfAcademyHomeHtml`** mounts `m4MasteryHtml` + `m4BetaQAHtml`; (19) **NEW CSS** `.postflop-board-card.is-turn-card` (amber outline + `.pf-turn-pill`) — only NEW visual primitive in this sprint. **Programmatic grading QA verified:** 6/6 sample scenarios PASS all 4 invariants (best in choices / all entries in choices / critical ⊆ bad / partition disjoint); R44b reason_choice integrity 0 violations across 13 M4 reason_choice scenarios; simulated walkthrough on action_choice spot confirmed correct tier classification for all 5 actions (best/acceptable/bad/critical/bad). **Audits all green:** production 457/0/0 PASS unchanged; M4 polish 19/0/0 PASS unchanged; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged; M3 24/0/0 PASS clean unchanged; M2 24/0/8 PASS unchanged; R29/R71/R72/R44b 0 fires across 72 M4. **Cache bump:** appVersion 4.3.0D → 4.3.1; SW VERSION v4.3.0D → v4.3.1. **Production data byte-identical to v4.3.0D manifest** (verified via SHA256 + blob OID match). **M1/M2/M3 strategy fields byte-identical**, all v4.3.0/v4.3.0C/v4.3.0D builders byte-identical, all migration tools byte-identical, production auditor byte-identical, ranges/manifest/preflop/gamification all byte-identical. **Module 4 IS NOW PLAYABLE as Limited Beta** from TCC, Postflop Academy curriculum, and Concept Library. Honest copy preserved throughout: "Limited Beta — 72 scenarios", "more scenarios coming", "display only", "good for structured practice, not final certification". New doc `docs/specs/postflop-v4.3.1-module4-limited-beta-wire.md`. New snapshot `GPT AUDIT/v4.3.1/` with SHA256 manifest. Predecessors: `v4.3.0D-doc` (`9792758`); `v4.3.0D` (`023430a`); `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.1:** Mirrors v4.2.4's M3 Limited Beta wire pattern almost exactly — same architectural choices, same honest-copy discipline, same threshold philosophy. Three pattern lessons baked in: (1) **Module-agnostic helpers pay off** — `_pfBetaQAStatsForModule` (v4.2.6 module-agnostic helper) was directly reused for M4 with a one-liner wrapper `_pfM4BetaQAStats() { return _pfBetaQAStatsForModule('pf_turn_barrel_oop_def'); }`. The weak-spot scoring engine (`_pfCurrentSessionWeakProfile` / `_pfWeakScenarioScore` / `_pfBuildWeakSpotQueue`) is also fully module-agnostic and worked for M4 unchanged. The same generic-first design will pay off again for M5 / M6. (2) **Mastery thresholds match corpus quality** — M4's 9-of-12 actionReason coverage threshold (vs 9-of-9 for M3) is calibrated to corpus density: blocker_check_raise_turn ships at 3/72 and semi_bluff_check_raise_turn at 2/72, so requiring all 12 would be unfair to early Limited Beta sessions. The threshold should grow as the corpus grows; v4.3.2's coverage continuation should bump it to 10-of-12 or 11-of-12. (3) **Turn-card visual distinction is the single new visual primitive** — adding `.is-turn-card` + `.pf-turn-pill` is a 25-line CSS addition that does not require any new layout primitive. The existing `.postflop-board-card` flexbox layout already supports 4 cards in the row. The amber outline + "Turn" pill makes the M4 4th-card-as-turn-card relationship immediately visible to learners. **Module 4 IS PLAYABLE but stays preview-quality:** 72 scenarios is at the lower end of "stable runtime beta"; the recommendation continues to be either v4.3.2 data continuation (72 → 80-100) before declaring full beta, or M5 architecture sprint to mirror the v4.3.0 planning pattern. **No M4 strategy field was touched.** No data was migrated. The 72-scenario corpus authored across v4.3.0 / v4.3.0A / v4.3.0B / v4.3.0C / v4.3.0C1 / v4.3.0D is now learnable end-to-end without any further data changes. v4.3.1 is the runtime-only complement of that 72-scenario foundation.

- **Last committed + pushed (HISTORICAL, MODULE 4 COVERAGE POLISH 53 -> 72)**: `v4.3.0D` (`023430a`) — Module 4 Coverage Polish. **Production-data polish sprint on top of v4.3.0C1 (`896e0ef`).** Authored 19 NEW polish scenarios in canonical polish builder `tools/build-m4-polish-v4.3.0D.ps1` (separate from v4.3.0 baseline builder + v4.3.0C expansion builder for source-of-truth clarity). Migrated to production via canonical migration tool `tools/migrate-module4-v4.3.0D.ps1` (idempotent, no Invoke-Expression, no unsafe Remove-Item, two-phase staged approval review_pending → approved). **Production count 438 → 457** (251 M1 + 49 M2 + 85 M3 + **72 M4** = 24 baseline + 29 expansion + 19 polish). **Coverage gaps closed:** `blocker_check_raise_turn` 1 → 3 (R1 As9c on flush-complete + R2 As6c on A-overcard); `reason_choice` qtype 6 → 13 (~18% of M4); `mixed_indifference_turn` 3 → 4 (TT underpair on multi-FD turn); `consensus_gto` sourceConfidence 0 → 8 textbook spots promoted (TPTK+blocker bluff-catch, TPTK+blocker value-raise, top set value-raise, made-straight value-raise, naked-overcards-fold, top-set slowplay, etc.). **Critical-flag density recalibrated:** combined M4 from 81.1% (43/53) → 72.2% (52/72) — hit target 70-75% range purely via polish authoring (10 polish scenarios with critical, 9 without; no existing scenarios touched). **check_raise_big-best** stays at 2 (no v4.3.0D scenarios force this label per "ONLY if natural" discipline). **NEW board family F11 introduced:** `7s 5d 3h / 4c` (low BB-favored straight-complete; bottom-end one-card straight via any 6) -- distinct strategic dimension from F5 (mid 9-8-4-7 straight). **Polish seed auditor** (`tools/audit-postflop-module4-polish-v4.3.0D.ps1`) validates polish-only + cross-corpus duplicate-ID check vs production AND v4.3.0C expansion + content-drift check + R71 bidirectional nut_flush_draw WARN + R72 text-integrity HARD + NEW M4.R44b reason_choice answer-integrity HARD (mirrors production R04 strictness; catches action ids leaking into reason_choice answer arrays); PASS 19/0/0. **Strategic review:** 19/19 PROMOTE, 0 REVISE, 0 REJECT. 5 audit-driven revisions applied during build (AsQs board collision → AhQc; JhJs critical recalibrated; As9c drawCategory backdoor_only → nut_flush_draw per R71; TdTc reason_choice answer.bad de-leaked of action ids; 8h7d reason_choice answer.critical cleared). **R71 bidirectional nut_flush_draw WARN:** 0 fires across full 72-scenario M4 corpus. **R72 text-integrity:** 0 hits. **Concepts/taxonomy unchanged:** 51 concepts; M4 vocab from v4.3.0B unchanged; all 19 polish scenarios use existing vocabulary. **Production auditor:** R55-R72 from v4.3.0C1 unchanged; runtime-locked. **Two-phase migration:** Phase 1 review_pending → 457/0/0 PASS; Phase 2 -FlipApproved → 72 approved → 457/0/0 PASS. **Audit results:** production 457/0/0 PASS; M4 polish 19/0/0 PASS; M4 expansion 29/0/0 PASS unchanged; M4 original seed 24/0/0 PASS unchanged (planning JSON not touched); M3 24/0/0 PASS clean unchanged; M2 24/0/8 PASS unchanged; R29 = 0 warnings preserved. **Cache bump:** appVersion 4.3.0C1 → 4.3.0D; SW VERSION v4.3.0C1 → v4.3.0D (data invalidation only). **Runtime-lock preserved:** M4 NOT in TRAINING_MODES.postflop.actions, no postflop:m4 route, no M4 runtime UI, no concept-drill, no weak-spot, no mastery — Module 4 stays preview-locked. **Forbidden files all byte-identical:** tools/build-m4-seeds-v4.3.0.ps1, docs/specs/postflop-v4.3.0-module4-seed-scenarios.json, tools/build-m4-expansion-v4.3.0C.ps1, docs/specs/postflop-v4.3.0C-module4-expansion-seeds.json (v4.3.0C1-corrected JSON unchanged), tools/audit-postflop-module4-seed.ps1, tools/audit-postflop-module4-expansion-v4.3.0C.ps1, tools/audit-postflop-module2-seed.ps1, tools/audit-postflop-module3-seed.ps1, tools/migrate-module4-v4.3.0B.ps1, tools/migrate-module4-v4.3.0C.ps1, tools/hotfix-module4-v4.3.0C1.ps1, tools/audit-postflop-ps.ps1 (production auditor unchanged from v4.3.0C1; R72 + R55-R71 carry forward), ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json, postflop_taxonomy.json. New tool `tools/build-m4-polish-v4.3.0D.ps1` + `tools/audit-postflop-module4-polish-v4.3.0D.ps1` + `tools/migrate-module4-v4.3.0D.ps1`. New seed JSON `docs/specs/postflop-v4.3.0D-module4-polish-seeds.json`. New doc `docs/specs/postflop-v4.3.0D-module4-coverage-polish.md`. New snapshot `GPT AUDIT/v4.3.0D/` with SHA256 manifest. Predecessors: `v4.3.0C1-doc` (`c8d6ef7`); `v4.3.0C1` (`896e0ef`); `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.0D:** Quality-discipline coverage polish, not raw-count expansion. The 19 new scenarios were designed gap-first (blocker_check_raise +2, reason_choice +7, mixed +1, consensus_gto +8, critical-density recalibration) rather than family-first. Each new scenario carries a uniqueness lesson distinct from its closest existing M4 cousin. Three architectural decisions: (1) **NEW M4.R44b reason_choice answer-integrity HARD rule** added to polish auditor — catches the v4.3.0D sprint-time bug where `'fold'`/`'mixed'`/`'call'`/`'check_raise_big'` (action ids) leaked into reason_choice `answer.{bad, critical}` arrays which only accept reason ids. The production auditor R04 caught this post-Phase-1, but R44b would have caught it in the polish auditor pre-migration. This is the same lesson v4.3.0C1's R72 banked for prose: mechanical structural validation must encode semantic invariants. (2) **consensus_gto promotion is conservative** — 8 of 19 polish scenarios get the upgrade, all clear textbook spots (TPTK+blocker calls/raises, top-set value/slowplay, naked-overcards fold). 11 stay at expert_judgment because they involve mixed/marginal/blocker-bluff trade-offs where solver overconfidence would mislead. (3) **critical density landed at 72.2% via authoring discipline only** — no existing scenarios touched. The 9 polish scenarios with critical flags are the ones where the wrong action is genuinely a severe punt (folding TPTK on dry brick, calling no-pair-no-draw on dynamic turn, oversizing weak hands). The 10 with empty critical are blocker bluffs (advanced not punt), reason_choice (diagnostic miss not punt), genuine mixed spots, and slowplay-vs-value tradeoffs (both defensible). Pattern lessons: (a) the auditor must keep growing teeth — R44b, R72, R71 each came from a real sprint-time bug; the auditor that doesn't catch the next bug class lets it ship. (b) the consensus_gto bucket needs care — over-promotion teaches false certainty; under-promotion fails to flag the obvious spots learners should master first. (c) coverage polish > raw count expansion — the 19 polish scenarios are designed for diagnostic value, not corpus size. **Module 4 stays preview-locked.** Recommendation: either v4.3.0E coverage continuation (72 → 80-100 with critical-density recalibration of v4.3.0C 86% block) OR v4.3.1 runtime wire as Limited Beta with scaled mastery thresholds and honest "Limited Beta — 72 scenarios" copy. Project-owner decision still required.

- **Last committed + pushed (HISTORICAL, MODULE 4 EXPANSION RAW CONTENT HOTFIX)**: `v4.3.0C1` (`896e0ef`) — Module 4 Expansion Raw Content Hotfix. **Targeted content-hotfix sprint on top of v4.3.0C (`19569e6`).** No new scenarios authored; M4 stays at 53 / 53. Two scenario-level defects that survived v4.3.0C's mechanical 438/0/0 audit are corrected at the canonical builder source-of-truth and re-synced into production via a new idempotent hotfix tool: (1) **`pf_btn_v_bb_srp_100bb_turn_Ac7d2s_4h_m4_action_7s6h_v430C`** (Family 1 brick) — false `gutshot` claim removed (hero+board ranks 2/4/6/7/A leave a 2-card gap; no one-card straight exists). Reclassified `drawCategory: gutshot → none`, `heroHandRole: marginal_made_hand → bluff_catcher`, `actionReason: equity_realization_turn_call → bluff_catch_turn`. `conceptTags: turn_pot_odds, turn_bluff_catcher, second_barrel_defense → turn_bluff_catcher, second_barrel_defense, turn_range_disadvantage`. Explanation prose rewritten end-to-end as a clean middle-pair-no-draw bluff-catch lesson; "wait need 3+5; actually 4-5-6-7..." authoring artifact removed. recommendedAction stays `call`. (2) **`pf_btn_v_bb_srp_100bb_turn_Ts8s4d_7c_m4_action_9d6d_v430C`** (Family 8 draw-intensifier) — strategic verdict (T-high straight check-raise small for value) and `handClass=straight` were correct. Defect was prose-only: turnLogic contained `"Loses only to JT and J9 (J9 makes 7-8-9-T-J wait needs J actually impossible without J on board)"`. Cleaned to: hero has T-high straight 6-7-8-9-T; only higher straight is `7-8-9-T-J` requiring `J9` in villain (hero blocks one of the four 9s, leaving 9 J9 combos); JT removed as a higher-straight (JT on T-8-7-4 makes pair of tens, not a straight); all "wait..." / "actually impossible" prose removed. recommendedAction unchanged. (3) **Metadata fix:** expansion JSON `expansionStats.finalProductionTarget` corrected `"52 (24 baseline + 28 new)" → "53 (24 baseline + 29 new)"`; `version: v4.3.0C → v4.3.0C1`. **NEW R72 text-integrity guard** added to expansion auditor (`tools/audit-postflop-module4-expansion-v4.3.0C.ps1`) AND production auditor (`tools/audit-postflop-ps.ps1`, M4-scoped). R72 is a HARD error if any of `explanation.{short, turnLogic, rangeContext, handLogic, sizingLogic, commonMistake, takeaway}` or `blockerNote` contains the substrings ` wait `, ` wait,` ` wait.` ` wait;` ` wait:` ` wait?` ` wait!`, `wait needs`, `wait need `, `actually impossible`, `... wait`, `...wait` (case-insensitive). **CR.R03 refinement** in expansion auditor — replaces "expansion ID exists in production" pre-migration check with content-drift check: only fires when an expansion ID is in production AND a synced strategy/prose field disagrees with the expansion source (drift detection that's meaningful pre AND post-migration). **NEW hotfix tool** `tools/hotfix-module4-v4.3.0C1.ps1` (idempotent; no Invoke-Expression; no unsafe Remove-Item; UTF-8 NO-BOM atomic tmp+Move-Item write; pre-flight verifies production total=438 / M4=53 / all approved / target IDs present in both source+production; per-id field-level diff plan; post-write verifies field equality vs source + zero drift on 436 non-target scenarios + scenario-array order preservation). **Audits all green:** production 438/0/0 PASS; expansion 29/0/0 PASS; M4 original seed 24/0/0 PASS unchanged (planning JSON not touched); M3 24/0/0 PASS unchanged; M2 24/0/8 PASS unchanged; R72 text-scan 0 hits across 53 M4 production scenarios; R71 nut_flush_draw 0 fires across 53. **Cache bump only:** appVersion `4.3.0C → 4.3.0C1` (index.html line 33226); SW VERSION `v4.3.0C → v4.3.0C1` (service-worker.js line 1). **Runtime-lock preserved:** M4 NOT in TRAINING_MODES.postflop.actions, no postflop:m4 route, no M4 runtime UI, no concept-drill, no weak-spot, no mastery — Module 4 stays preview-locked. **Forbidden files all byte-identical:** tools/build-m4-seeds-v4.3.0.ps1, docs/specs/postflop-v4.3.0-module4-seed-scenarios.json, tools/audit-postflop-module4-seed.ps1, tools/audit-postflop-module2-seed.ps1, tools/audit-postflop-module3-seed.ps1, tools/migrate-module4-v4.3.0B.ps1, tools/migrate-module4-v4.3.0C.ps1, ranges.json, manifest.json, preflop, gamification, M1/M2/M3 strategy fields, postflop_concepts.json (51 concepts), postflop_taxonomy.json. New doc `docs/specs/postflop-v4.3.0C1-module4-content-hotfix.md`. New snapshot `GPT AUDIT/v4.3.0C1/` with SHA256 manifest. Predecessors: `v4.3.0C-doc` (`7399e6d`); `v4.3.0C` (`19569e6`); `v4.3.0B-doc` (`c65ce16`); `v4.3.0B` (`95fe02f`); `v4.3.0A-doc2` (`69fe211`); `v4.3.0A-doc` (`ab26aee`); `v4.3.0A` (`0ea2910`); `v4.3.0-preA-doc` (`5e74a18`); `v4.3.0-preA` (`9a05c30`); `v4.3.0` (`3816723`); `v4.2.6` (`86d21c0`); `v4.2.5-doc` (`8b80b37`); `v4.2.5` (`55fa676`); `v4.2.4-doc` (`cc5c209`); `v4.2.4` (`81d54c5`); `v4.2.3B` (`e72eef6`); `v4.2.3A` (`14fb380`); `v4.2.3-doc` (`b4b944f`); `v4.2.3` (`e718f07`); `v4.2.2G` (`8c150ab`).

- **Project owner notes for v4.3.0C1:** Mechanical-PASS-is-not-enough discipline reinforced. v4.3.0C had a clean 438/0/0 mechanical audit but two real raw-content defects survived — a false-gutshot claim that taught a poker fiction (no one-card straight is reachable on Ac-7d-2s-4h with 7s6h), and a "wait... actually impossible" authoring artifact in user-facing prose plus an incorrect higher-straight listing (JT on Ts-8s-4d-7c makes a pair, not a straight). R72 is the new mechanical rule that systematically catches the prose class of bug going forward. Three pattern lessons banked: (1) **content audits beyond schema validation matter** — R72 is narrow and literal but it would have caught both v4.3.0C defects pre-migration. Future audit hardening should consider similar "draft prose" guards (`TODO`, `FIXME`, `[citation needed]`, `wait`, `hmm`, `not sure`, etc.) plus stronger drawCategory-vs-rank-set invariants. (2) **CR.R03 drift-detection is reusable** — refining the cross-corpus check from "ID exists in prod" to "ID's content drifted between source and prod" turns it into a permanent guard that runs both pre-migration AND post-migration. This is the right shape for a long-lived production-of-record auditor. (3) **Idempotent narrow-scope hotfix discipline pays off** — the hotfix tool only touches 2 IDs, verifies pre/post invariants, and leaves all 436 other scenarios byte-identical. The tool is reusable for any future M4 content correction. **Module 4 stays preview-locked.** Recommendation unchanged from v4.3.0C: either v4.3.0D additional expansion (53 → 70+) for a wider corpus before runtime exposure, OR v4.3.1 runtime-wire as Limited Beta with scaled mastery thresholds and honest "Limited Beta — 53 scenarios" copy. Project-owner decision still required.
- **Last committed + pushed (HISTORICAL, MODULE 4 DATA EXPANSION 24 -> 53)**: `v4.3.0C` (`19569e6`) — Module 4 Data Expansion. **Production-data expansion sprint.** Authored 29 NEW expansion scenarios in canonical expansion builder `tools/build-m4-expansion-v4.3.0C.ps1` (separate from original v4.3.0 builder for source-of-truth clarity). Migrated to production via canonical migration tool `tools/migrate-module4-v4.3.0C.ps1` (idempotent, no Invoke-Expression, no unsafe Remove-Item, two-phase staged approval review_pending → approved). **Production count 409 → 438** (251 M1 + 49 M2 + 85 M3 + **53 M4** = 24 baseline + 29 expansion). **All 5 actions now represented**: fold 12 / call 21 / check_raise_small 15 / **check_raise_big 2** / **mixed 3** (achieved coverage goals). **All 12 actionReasons now have ≥1 representation** (only blocker_check_raise_turn thin at 1; semi_bluff_check_raise_turn at 2). **6 turn categories all expanded:** brick 4→10, overcard 4→10, draw_intensifier 4→10, board_pair 4→9, flush_complete 4→7, straight_complete 4→7. **10 NEW board families:** Ac 7d 2s 4h (brick), 8d 6c 3s Qh (BB-favored Q overcard), Kd 8c 4s Ah (Ace overcard), Qs 8s 4d 2s (different flush_complete), 9s 8d 4c 7h (BB-favored straight complete), Kd 8s 3c 8h (board-pair high), Qs 7d 3c 3h (board-pair low), Ts 8s 4d 7c (draw-intensifier), Ah 9d 4d 7h (multi-FD turn), Jd Td 5s 2c (polar brick after dynamic). **2 check_raise_big-best spots justified:** JT nut straight on 9-8-4-7 (BB range advantage) + KK top boat on Kd 8s 3c 8h. **3 mixed-action spots:** 99 underpair on A-7-2-4 brick / AA on 9-8-4-7 polar straight / 99 underpair on J-T-5-2 polar brick — all genuine indifference. **Expansion seed auditor** (`tools/audit-postflop-module4-expansion-v4.3.0C.ps1`) validates expansion-only + cross-corpus duplicate-ID check vs production; PASS 29/0/0. **Strategic review:** all 29 PROMOTE / 0 REVISE / 0 REJECT (4 critical-flag downgrades applied to call-with-real-equity scenarios where check_raise_big is suboptimal but not severe-punt; final critical density 25/29 = 86% expansion, 43/53 = 81% combined). **R71 bidirectional nut_flush_draw WARN:** 0 fires across full 53-scenario M4 corpus; rule stays at WARN level (not promoted to HARD due to limited corpus size). **Concepts/taxonomy unchanged:** 51 concepts, M4 vocab from v4.3.0B unchanged (no new vocab needed; all 29 new scenarios use existing M4 conceptTags / heroHandRole / actionReason). **Production auditor unchanged** (R55-R71 from v4.3.0B + R10/R13 scoping). **Two-phase migration:** Phase 1 review_pending → 438/0/0 PASS; Phase 2 -FlipApproved → 53 approved → 438/0/0 PASS. **Audit results:** production 438/0/0 PASS; M2 24/0/8 PASS unchanged; M3 24/0/0 PASS clean unchanged; M4 original seed audit 24/0/0 PASS unchanged (planning JSON untouched). R29 = 0 warnings preserved. **Cache bump:** appVersion 4.3.0B → 4.3.0C; SW VERSION v4.3.0B → v4.3.0C (data invalidation only). **Runtime-lock preserved:** M4 NOT in TRAINING_MODES.postflop.actions, no postflop:m4 route, no M4 runtime UI. **Forbidden files all byte-identical:** tools/build-m4-seeds-v4.3.0.ps1 (canonical for original 24), docs/specs/postflop-v4.3.0-module4-seed-scenarios.json, tools/audit-postflop-module4-seed.ps1, tools/audit-postflop-module2-seed.ps1, tools/audit-postflop-module3-seed.ps1, tools/migrate-module4-v4.3.0B.ps1, ranges, manifest, preflop, gamification, M1/M2/M3 strategy fields. **No Invoke-Expression in migration tool. No unsafe Remove-Item.** New doc `docs/specs/postflop-v4.3.0C-module4-data-expansion.md`. New snapshot `GPT AUDIT/v4.3.0C/` with SHA256 manifest.

- **Project owner notes for v4.3.0C:** Quality-discipline expansion. Targeted 26-36 new scenarios, delivered 29 with 100% PROMOTE rate after strategic review + 4 critical-flag recalibrations. Achieved every coverage goal except blocker_check_raise_turn (accepted limitation since adding more would risk filler). Most important architectural decisions: (1) **Separate expansion builder + JSON** prevents source-of-truth confusion -- original 24 seeds canonical at v4.3.0 builder; new 29 canonical at v4.3.0C builder; production combines both via two migration tools. (2) **Strict no-Invoke-Expression rule** for migration tool, learned from v4.3.0B accidental file delete; migration uses param block + atomic Write + Move-Item only. (3) **R71 stays WARN, not HARD:** 0 fires on 53 M4 corpus is encouraging but not enough confidence; legitimate edge cases (e.g., flush-draw-with-stronger-value classification) need larger corpus before HARD promotion. (4) **Critical-flag recalibration discipline:** 4 scenarios moved from critical=[check_raise_big] to critical=[] because hero has 8-out OESD or 4-out gutshot equity backing the action. critical reserved for true severe-punts (no equity, dominated calls, slowplay-in-error). (5) **53 scenarios is at the lower end of 50-60 target** -- defensible foundation but a v4.3.0D additional expansion (53 → 70+) is recommended before v4.3.1 runtime wire. Pattern lessons: (a) auditor's bidirectional invariants (R71) catch real classification bugs; expansion KdQd vs AdKd nut-vs-2nd-nut distinction caught at audit time. (b) coverage-driven design (gap → board family → 3 scenarios per family) is the right method for hitting reason/action distribution targets without filler. (c) migration tool's idempotent baseline-validation (24 baseline + 0 expansion-IDs in production) ensures clean re-runs. **Module 4 stays preview-locked.** Recommendation: v4.3.0D more expansion, OR v4.3.1 runtime-wire as Limited Beta with scaled mastery thresholds and honest "Limited Beta · 53 scenarios" copy.

- **Last committed + pushed (HISTORICAL, MODULE 4 PRODUCTION MIGRATION)**: `v4.3.0B` (`95fe02f`) — Module 4 Production Migration. **Production data + audit-extension sprint.** Migrated all 24 strategically-reviewed Module 4 planning seeds from `docs/specs/postflop-v4.3.0-module4-seed-scenarios.json` into production `postflop/postflop_scenarios.json`. **Production count 385 → 409** (251 M1 + 49 M2 + 85 M3 + **24 M4**). Production audit extended with M4-aware rules **R55-R71** (R55 module-id+street+game+schemaVersion lock; R56 spot block BB-vs-BTN turn-defense lock; R57 4-card board structure; R58 turnCategory enum; R59 heroHand+collision; R60 handClass/heroHandRole/drawCategory/showdownValue vocab; R61 action_choice schema + recommendedAction-best alignment; R62 action_choice prompt completeness anti-regression of v4.3.0 prompt bug; R63 reason_choice schema + actionReason vocab; R64 answer partition disjointness + critical subset of bad; R65 explanation 7-field including turnLogic; R66 conceptTags 1-4 from M4 vocab no duplicates; R67 auditStatus + sourceConfidence; R68 nut_flush_draw invariant via A-of-suit + 4-suit total; R69 nut-suit blocker invariant; R70 handClass=straight invariant; **R71 NEW bidirectional nut_flush_draw WARN: hero has A-of-suit + 4-of-suit total + handClass not made-flush-class → drawCategory should be nut_flush_draw**). R10/R13 scoped to non-M4 modules (M4 has different board structure). Concepts file extended **39 → 51** (+12 M4-native concepts under category=module4). Taxonomy file extended with `modules.pf_turn_barrel_oop_def` entry, `heroHandRole.module4` (13 values), `actionReason.module4` (12 values), 6 new top-level enums (turnCategory, boardChange, equityShift, drawCompletion, pairStatusChange, suitTextureTurn), and `actionMenuByModule.pf_turn_barrel_oop_def`. **Migration tool** `tools/migrate-module4-v4.3.0B.ps1` is idempotent + ASCII-clean + supports `-DryRun` + `-FlipApproved` two-phase staged approval. **Two-phase audit:** (1) migrated with auditStatus=review_pending; production audit 409/0/0 PASS; (2) flipped to auditStatus=approved; production audit 409/0/0 PASS. **Strategy-field survival check on 14 scenarios** (11 REVISE from v4.3.0A + 3 random PROMOTE): 0 mismatches across recommendedAction/actionReason/answer.best/answer.critical/handClass/heroHandRole/drawCategory/conceptTags/turnLogic/short. **Cache bump only:** appVersion `4.2.6` → `4.3.0B`; service-worker VERSION `v4.2.6` → `v4.3.0B`; runtime-lock M4 preserved (M4 NOT added to TRAINING_MODES.postflop.actions, no postflop:m4 route, no runtime UI). M2/M3/M4 seed audits unchanged. R29 = 0 warnings preserved. **Sprint-time bug discovered + recovered:** earlier helper-script `Invoke-Expression` reassigned `$path` in outer scope causing accidental delete of postflop_concepts.json + postflop_taxonomy.json from working tree; restored via `git restore` + extensions re-applied inline (no temp file). No deletion propagated to git HEAD or any user-facing artifact. **Forbidden files all byte-identical:** tools/build-m4-seeds-v4.3.0.ps1, docs/specs/postflop-v4.3.0-module4-seed-scenarios.json, tools/audit-postflop-module4-seed.ps1, tools/audit-postflop-module2-seed.ps1, tools/audit-postflop-module3-seed.ps1, ranges.json, manifest.json, all preflop / gamification / wardrobe / collection-book, M1/M2/M3 strategy fields, TRAINING_MODES.m4 (still absent). New tool `tools/migrate-module4-v4.3.0B.ps1`. New doc `docs/specs/postflop-v4.3.0B-module4-production-migration.md`. New snapshot `GPT AUDIT/v4.3.0B/` with SHA256 manifest exported from final HEAD.

- **Project owner notes for v4.3.0B:** Production-data sprint with strict runtime-lock discipline. The data + audit infrastructure is now M4-aware: a learner cannot yet PLAY Module 4, but the system structurally KNOWS about Module 4 (concepts library has the 12 M4 concepts; taxonomy module entry says `production_data_loaded_preview_only`; production auditor enforces R55-R71). This separation of concerns — load production data WITHOUT runtime wire — is the same pattern v4.2.3 used for M3 (migrated 24 seeds → production at v4.2.3, runtime-wired at v4.2.4). The R71 bidirectional nut_flush_draw invariant is a genuine new poker-sanity guard learned from the v4.3.0A strategic review (where 2 scenarios were missed by the unidirectional R52). It catches "hero has nut FD pattern but NOT classified as nut_flush_draw" — currently 0 violations in production, but the rule will catch this class of bug systematically going forward. The `-FlipApproved` two-phase migration design ensures the production rules can validate data structure independently of approval gate; this is reusable for v4.3.0C+ expansion. **24 M4 production scenarios is migration-ready, NOT runtime-ready.** v4.3.0C should expand to 50+ before v4.3.1 runtime wire. The 24-scenario floor is too thin for diversity across the 5-action menu × 12-actionReason × 6-turnCategory matrix; current action distribution is 10 call / 6 check_raise_small / 8 fold / 0 mixed / 0 check_raise_big — mixed and check_raise_big are entirely absent in M4 production, which is a real coverage gap to address before runtime exposure. Pattern lessons for future migrations: (a) general production rules tied to module-specific board fields (R10 rangeAdvantage, R13 suitTexture) need explicit module scoping when a new module has a different board shape; (b) helper scripts that share variable names with the outer scope must use isolated scriptblocks, not `Invoke-Expression`-on-files; (c) two-phase staged approval (review_pending → approved) is the right migration pattern because it lets the auditor validate structure before the approval gate flips.

- **Last committed + pushed (HISTORICAL, MODULE 4 SEED STRATEGIC REVIEW + REPAIR)**: `v4.3.0A` (`0ea2910`) — Module 4 Seed Strategic Review + Repair. **Strategic-review sprint on top of v4.3.0-preA-doc (`5e74a18`).** Reviewed all 24 Module 4 planning seeds against Poker NLH MTT chipEV / 100BB / BTN-vs-BB SRP context using the 14-point per-scenario checklist from the GPT review package. **Verdict counts:** 13 PROMOTE / **11 REVISE** / 0 REJECT (no seed flawed enough to discard). All 11 REVISE seeds repaired builder-first (`tools/build-m4-seeds-v4.3.0.ps1`); JSON regenerated; no JSON-only edits. **Repairs applied:** (#5 9c9s) duplicate conceptTag `turn_check_raise_value × 2` → `turn_board_change` (D-issue); (#7 9h7h) handClass `top_pair_weak_kicker` → `mid_pair` because after K-overcard turn the 9-pair is middle-pair below K; (#9 6s5s) handLogic removed false "small backdoor straight" claim (backdoor needs 2 streets, only river remains); (#10 AsKd) drawCategory `flush_draw` → `nut_flush_draw` (As + 3 board spades = 4 spades, As guarantees nut flush); (#11 AsJd) drawCategory `none` → `nut_flush_draw` + handLogic/blockerNote/uniquenessNote rewritten to acknowledge As provides BOTH nut-spade blocker AND nut-flush redraw (blocker still primary raise mechanism); (#12 Tc9c) handLogic + rangeContext cleaned of "wait there is no Q on board" mid-sentence + false "gutshot to J" claim; (#14 KhQh) drawCategory `gutshot` → `oesd` (T-J-Q-K = 4 consecutive in hero+board, 9 OR A completes = OESD with 8 outs, A makes Broadway/nut); (#15 Tc9d) handClass `top_pair_weak_kicker` → `mid_pair` (T-pair on Q-T-6-J board is below Q top and J); (#20 QhJh) critical `[check_raise_big]` → `[call, check_raise_big]` for cross-category consistency with #4 (JsTh brick) and #8 (AdJs overcard) which already mark dominated-overcards calls as critical; (#23 As6s) turnLogic removed false "gutshot to 7 (5-6-7-8-9)" claim (5-6-7-8-9 needs 7+8 = 2 cards, not a gutshot); (#24 KhQd) drawCategory `none` → `gutshot` (K-Q + T-9 + need J = 9-T-J-Q-K 4-out gutshot). **Critical-flag calibration:** 18 of 24 (75%) have critical, evenly distributed 3 of 4 per category. 0 reason_choice scenarios have critical (correct -- reasons are not severe punts). All retained critical entries fit user-defined criteria (calling dominated no-equity hand on bad turn / folding strong continue with price / check-raise bluffing with no equity-blockers / overplaying marginal made hand on bad range card). No critical-flag downgrades applied. **Anti-filler:** 24 distinct strategic lessons across 6 turn categories x 4. uniquenessNote present + substantive on all 24. **JSON now 180108 bytes (was 178993 in v4.3.0-preA, 177918 in v4.3.0).** **M4 seed audit:** 24 / 0 hard / 0 warnings PASS. R50-R54 all PASS. **Production audit unchanged 385/0/0** (no production data touched). M2 + M3 seed audits unchanged. R29 = 0 warnings preserved. **Forbidden files byte-identical:** index.html, service-worker.js, postflop/postflop_scenarios.json, postflop/postflop_concepts.json, postflop/postflop_taxonomy.json, tools/audit-postflop-ps.ps1, tools/audit-postflop-module2-seed.ps1, tools/audit-postflop-module3-seed.ps1, tools/audit-postflop-module4-seed.ps1 (NOT edited this sprint), ranges.json, manifest.json, all preflop / gamification / wardrobe / collection-book. **No appVersion bump, no SW VERSION bump, no runtime UI changes.** TRAINING_MODES.postflop.actions.m4 still preview/null. New doc `docs/specs/postflop-v4.3.0A-module4-strategic-review-results.md` (per-scenario verdict table for all 24, edit list, critical-flag calibration, anti-filler review, post-edit audit results, recommendation for v4.3.0B production migration). New snapshot folder `GPT AUDIT/v4.3.0A/` with SHA256 manifest exported from the v4.3.0A commit.

- **Project owner notes for v4.3.0A:** True strategic review of all 24 seeds against poker NLH MTT chipEV. The high REVISE rate (46%) reflects three lesson categories: (1) **handClass ranking shifts after the turn** -- multiple scenarios labeled flop-top-pair as `top_pair_weak_kicker` after an overcard turn that demoted the pair to middle. The convention is to label handClass at decision time (the turn), not flop time. Fixed [7] 9h7h and [15] Tc9d. (2) **drawCategory under-specification** -- two scenarios (#10 AsKd, #11 AsJd) had `flush_draw`/`none` despite hero holding As + 4 spades total = nut flush draw; one (#14 KhQh) had `gutshot` despite hero holding 4 consecutive ranks K-Q-T-J in hero+board = true OESD; one (#24 KhQd) had `none` despite a clear gutshot to J. R52 already enforced one direction (nut_flush_draw → A of 4-suited suit), but the converse (hero has nut-FD pattern → must classify as nut_flush_draw) was not yet a guard. Future audit hardening could add R55 to catch this. (3) **Prose nits** -- two scenarios had unfinished mid-sentence prose ("...wait there is no Q on board, so..." and "gutshot to 7 (5-6-7-8-9)" which needed 2 cards not 1). These read as authoring mistakes that survived v4.3.0-preA review but were caught in v4.3.0A. **Pattern lessons for v4.3.0B+ migration:** (a) handClass should be evaluated at decision-street, not flop; (b) drawCategory invariants should ideally be bidirectional auditor rules (R52 catches "claimed nut FD without A"; an R55 could catch "has nut FD but not classified"); (c) cross-scenario consistency for critical flags matters -- if pattern X (naked overcards no-draw fold on bad turn) marks `call` as critical in some scenarios, all scenarios with the same pattern should match; (d) the canonical-builder + regenerate-JSON discipline is now mature and reliable: 11 builder edits → JSON regenerated → audit PASS in one cycle. **Module 4 seeds are now strategically reviewed and ready for v4.3.0B production migration** (analogous to v4.2.3's M3 migration). 24 scenarios is acceptable for migration but TOO THIN for stable runtime exposure -- v4.3.0C should expand to 50+ scenarios before the v4.3.1 runtime wire.

- **Last committed + pushed (HISTORICAL, MODULE 4 SEED SANITY REPAIR)**: `v4.3.0-preA` (`9a05c30`) — Module 4 Seed Sanity Repair before Strategic Review. **Corrective sprint on top of v4.3.0 (`3816723`).** Mechanical M4 audit was 24/0/0 PASS on v4.3.0 but raw seed review caught 4 content/poker-logic bugs the original auditor did not detect: (1) all 18 action_choice prompts shipped without hero hands -- root cause was PowerShell parsing `$hero?` as variable name `hero?` (the `?` is a valid variable-name character) which expanded to empty; (2) Tc7c on 9d 8c 6h Kc was classified as oesd/semi_bluff but ALREADY makes the nut straight 6-7-8-9-T on the flop; (3) AhJs on Ks 8s 3d 2s claimed nut-spade blocker but Ah is the heart Ace, not the spade Ace; (4) AdJs on 9d 8c 6h Kc claimed gutshot but no straight draw exists for AdJs on this runout. **Fixes applied in canonical builder (`tools/build-m4-seeds-v4.3.0.ps1`) and JSON regenerated:** Q-Action helper changed from `$hero?` to `${hero}?` (all 18 prompts now contain hero hand); Tc7c reclassified to straight/nutted_value/value_check_raise_turn with explanation rewritten to reflect made nut straight + Kx domination; AhJs replaced with AsJd (true nut spade blocker, no flush draw, pure blocker bluff); AdJs drawCategory changed gutshot→none + explanation prose rewritten as naked-overcards-no-draw fold lesson. **Auditor strengthened with 5 new rules** (`tools/audit-postflop-module4-seed.ps1`): M4.R50 HARD action_choice prompt must not end with 'with '; M4.R51 HARD action_choice prompt must contain both hero cards; M4.R52 HARD drawCategory='nut_flush_draw' requires hero to hold A of a 4-suited suit (1 card needed for nut flush); M4.R53 HARD heroHandRole='blocker_bluff' AND blockerNote claiming 'nut-<spade|heart|club|diamond>' requires hero to hold A of that suit; M4.R54 HARD handClass='straight' requires 5 consecutive ranks (or A-2-3-4-5) in hero+board. **Negative-test verified:** all 5 new rules fire correctly when injected with the original v4.3.0 bugs. **Critical-flag sanity pass on the 3 repaired scenarios:** Tc7c (reason_choice value raise on nut hand) -- no critical, correct; AsJd (reason_choice advanced blocker bluff) -- no critical, correct; AdJs (action_choice naked-overcards fold) -- critical=[call,check_raise_big] kept (both severe punts not close mixes). 0 reason_choice scenarios have critical (correct -- close mixes shouldn't be critical). **Builder is canonical:** all fixes applied to builder first, JSON regenerated -- future regenerations preserve fixes; no JSON-only edits. **Audit-plan doc updated** with new section 11.5 documenting M4.R50..M4.R54 origins. **Post-fix audits:** M4 seed audit 24/0/0 PASS; production audit unchanged 385/0/0 (R29=0 preserved); M2 seed audit unchanged 24/0/8 PASS; M3 seed audit unchanged 24/0/0 PASS clean. **Forbidden files byte-identical:** index.html, service-worker.js, postflop/postflop_scenarios.json, postflop/postflop_concepts.json, postflop/postflop_taxonomy.json, tools/audit-postflop-ps.ps1, tools/audit-postflop-module2-seed.ps1, tools/audit-postflop-module3-seed.ps1, ranges.json, manifest.json, all preflop / gamification / wardrobe / collection-book. **No appVersion bump, no SW VERSION bump.** TRAINING_MODES.m4 still preview/null. JSON now 178993 bytes (was 177918), 0 mojibake, 0 ASCII issues, 0 card collisions. New net diff vs `3816723`: builder ~+30 lines, auditor +93 lines, audit-plan +56 lines (R50-R54 documentation), seed JSON regenerated. GPT AUDIT/v4.3.0/ snapshot refreshed.

- **Project owner notes for v4.3.0-preA:** Mechanical-audit-PASS does NOT equal strategic-correctness. v4.3.0 had 24/0/0 mechanical PASS but four real bugs: a 100%-prevalence prompt-rendering bug (every action_choice prompt was missing hero hand), and three poker-logic bugs that mechanical schema-validation cannot catch (a hand classified as a draw when it was actually a made nut straight; a hand classified as nut-suit-blocker when it held the wrong suit Ace; a hand classified as a draw when no draw existed on that runout). The v4.3.0-preA corrective sprint adds five mechanical guards (R50-R54) that catch THIS class of bug systematically -- the auditor is now poker-sanity-aware in the narrow but high-value cases of prompt-completeness, nut-FD A-of-suit invariant, nut-blocker A-of-suit invariant, and made-straight 5-rank-window invariant. Negative-tested all 5 rules. Pattern lesson #1: **the canonical builder is single source of truth** -- when the builder has a bug (like `$hero?`), fix the builder, regenerate the JSON, do not patch the JSON in place; this is how v4.3.0-preA preserves consistency. Pattern lesson #2: **PowerShell string interpolation rules** -- always use `${var}` braces in non-trivial contexts because `?`, `.`, `:` are valid variable-name characters that silently truncate the variable boundary. Pattern lesson #3: **mechanical guards should encode poker invariants** -- "nut FD = A of 4-suited suit" and "made straight = 5 consecutive ranks" are simple, deterministic, and catch real bugs that pure schema validation never will. v4.3.0A strategic review is now safe to start: the seeds are mechanically clean AND poker-sanity-clean for the most error-prone fields. The reviewer will still need to validate hand realism, range realism, recommendedAction correctness, and uniqueness -- but the 4 v4.3.0 bugs are now eliminated.

- **Last committed + pushed (HISTORICAL, MODULE 4 TURN DEFENSE OOP ARCHITECTURE + SEED PLAN)**: `v4.3.0` (`3816723`) — Module 4 (Facing Turn Barrel OOP) Architecture + Seed Plan. **Planning-only sprint. NO production data, NO runtime wiring, NO version bump in index.html / service-worker.js.** Mirrors the v4.2.0 Module 3 planning pattern. Locks Module 4 scope to the **BB Turn Defense OOP** spot (BB facing BTN turn barrel after flop check-call): BTN open 2.5x → BB call → BTN c-bet small flop → BB call → BTN barrels turn → BB OOP turn decision. NLH MTT 100BB SRP only. **Out of scope:** BTN IP turn barreling, donk-leading, multiway, 3-bet pots, ICM, river. **6 planning artifacts created, all under `docs/specs/` and `tools/`:** (1) `postflop-v4.3.0-module4-turn-defense-oop-architecture.md` (16-section module purpose + spot lock + 10-item learner mastery goals + 8 out-of-scope items with future-module promises + 12 strategic concepts + 12 turn-card categories + 5-action menu + answer partition philosophy + critical mistake philosophy + future migration path v4.3.0A → v4.3.3+); (2) `postflop-v4.3.0-module4-schema-taxonomy.md` (extends M3's schema 1.1.0 → 1.2.0, adds 4-card board structure: `flopCards` + `turnCard` + `cards`; new turn-specific fields `turnCategory`, `boardChange`, `equityShift`, `drawCompletion`, `pairStatusChange`; M4-native heroHandRole `slowplay_trap`; 12 actionReason `_turn` values; 12 M4-native conceptTags; explanation field adds `turnLogic` as defining required field parallel to M3's `defenseLogic`); (3) `postflop-v4.3.0-module4-seed-scenarios.json` (24 planning seeds across 6 turn categories x 4 each: brick / overcard / flush_complete / straight_complete / board_pair / draw_intensifier; 18 action_choice + 6 reason_choice; 6 distinct boards: As 8d 3h 2c, 9d 8c 6h Kc, Ks 8s 3d 2s, Qs Ts 6d Jc, 8c 8d 3s 3h, Ts 9s 5d 6h; ALL 24 carry `auditStatus=planning_only`, `reviewStatus=v4.3.0_seed_candidate`, schemaVersion `1.2.0`, module `pf_turn_barrel_oop_def`, plus uniquenessNote field for anti-filler discipline; 18 of 24 have at least one `critical` action highlighted as a teaching spot); (4) `postflop-v4.3.0-module4-audit-plan.md` (15-section audit plan defining 49 mechanical rules `M4.R01..M4.R49` covering top-level envelope, identity housekeeping, BB-vs-BTN turn-defense lock, board structure, hero hand+role, question + answer partition semantics, explanation+conceptTag, anti-filler distribution; rules numbered M4.R to avoid collision with production `R29/R30`; partitions semantic clarified: `best/acceptable/bad` are disjoint and exhaustive over the 5-action menu, `critical` is a highlight subset of `bad`); (5) `postflop-v4.3.0-module4-gpt-review-package.md` (10-section reviewer prompt with 14-question per-scenario checklist + 8 strategic-quality flags + per-scenario template + 5 aggregate review questions for a future GPT/coach strategic review); (6) `tools/audit-postflop-module4-seed.ps1` (PS 5.1-compatible mechanical auditor implementing all M4.R01..M4.R49; UTF-8 NO-BOM read; ASCII-only). **Builder script** (`tools/build-m4-seeds-v4.3.0.ps1`, 600+ lines, ASCII-clean) authors the 24 seeds with explicit `turnLogic` per scenario; output JSON 177918 bytes, 0 mojibake. **M4 seed audit verified PASS:** 24 scenarios / 0 hard errors / 0 warnings via `tools/audit-postflop-module4-seed.ps1`. **Forbidden files all byte-identical:** index.html, service-worker.js, manifest.json, ranges.json, postflop/postflop_scenarios.json, postflop/postflop_concepts.json, postflop/postflop_taxonomy.json, tools/audit-postflop-ps.ps1, tools/audit-postflop-module2-seed.ps1, tools/audit-postflop-module3-seed.ps1, all preflop / gamification / wardrobe / collection-book files. **Production audit unchanged 385/0/0** (no data file edits anywhere). M2 + M3 seed audits unchanged. R29 = 0 warnings preserved. **No appVersion bump, no SW VERSION bump, no runtime UI changes.** TRAINING_MODES.postflop.actions.m4 still preview/null (locked until v4.3.0A+ runtime wire sprint). New doc batch under `docs/specs/postflop-v4.3.0-*.md` + `docs/specs/postflop-v4.3.0-module4-seed-scenarios.json`. New snapshot `GPT AUDIT/v4.3.0/`.

- **Project owner notes for v4.3.0:** Planning-foundation discipline maintained. The v4.3.0 sprint replicates the successful v4.2.0 planning pattern that preceded M3's six-step productionization (v4.2.1 → v4.2.5 + v4.2.6 dashboard). Six lessons baked in from M3 sprint history: (1) **Schema versioning is forward-looking**: 1.2.0 extends 1.1.0 additively (new turn fields, new heroHandRole `slowplay_trap`, new `turnLogic` explanation field) so M3 normalization helpers `_pfNormalizePostflopChoices` + `_pfNormalizePostflopAnswer` work unchanged for M4. (2) **Anti-filler discipline encoded mechanically**: every seed carries `uniquenessNote` (>= 20 chars) explaining why it's non-redundant relative to its 3 category-mates. The auditor enforces presence; the GPT review package enforces substance. (3) **Critical-flag calibration carried forward**: from v4.2.5's "30.6% landing point" lesson — `critical` is reserved for genuinely harmful mistakes, not close mixed spots; the audit plan documents this as a strategic-quality rule (S.Q04). (4) **No solver overconfidence**: explanations use `expert_judgment` confidence by default; "the solver says X" claims require `solver_aligned`. (5) **Module-agnostic infrastructure works**: v4.2.6's `_pfBetaQAStatsForModule(moduleId)` is reusable for M4 with a one-liner wrapper, no rewrite. (6) **Version-discipline lock**: this sprint touches NO production data, NO runtime files, NO version numbers — the v4.3.0 commit's diff is 100% additive (5 new docs + 1 new JSON + 1 new builder script + 1 new auditor script). Pattern lessons for future modules: (a) write the architecture doc first to lock scope; (b) write the schema/taxonomy doc second to fix the data shape; (c) write the audit plan third to define mechanical rules BEFORE writing the seeds (catches schema gaps); (d) build the seeds via canonical PowerShell builder (PSCustomObject definitions, UTF-8 NO-BOM, ASCII-only) — NEVER hand-edit JSON; (e) the auditor and seed JSON co-evolve — the first auditor run will reveal vocabulary mismatches that should resolve via aligning the audit plan to the builder's actual output (richer enums beat narrower ones), not by trimming the builder. Future v4.3.0A sprint will run the GPT review package, apply edits, and re-run the M4 mechanical auditor; v4.3.0B/C will migrate seeds → production data + extend production auditor with M4-aware rules; v4.3.1 will runtime-wire M4 as Limited Beta with scaled mastery thresholds.

- **Last committed + pushed (HISTORICAL, POSTFLOP BETA FEEDBACK LOOP + M3 RUNTIME QA DASHBOARD)**: `v4.2.6` (`86d21c0`) — Postflop Academy Beta Feedback Loop. **Runtime-only sprint. ZERO data changes.** Adds an internal-facing M3 Beta QA Dashboard mounted inside the Postflop Academy panel (collapsible `<details>` element, default closed) with: 6-metric grid (sessions / answers / latest pct / avg pct / critical count / crit rate); 🔍 Weak-spot review would target top 3 reasons (lifetime miss count + candidate scenario count per actionReason); ⚡ Critical-mistake monitor (totalCritical, crbHabitCount, by-selected-action, by-scenario-actionReason); 4 by-axis breakdowns (actionReason / recommendedAction / heroHandRole / handClass); 🕘 Last 3 sessions micro-summary; 📋 Copy M3 Beta QA Snapshot button (clipboard-only JSON with privacy `_privacy` field, navigator.clipboard with execCommand fallback). Sample-size honesty banner appears below 3 sessions / 30 answers ("⚠ Early signal — N sessions, M answers. Needs more sessions for confident readings"). Empty-state renders cleanly when no M3 history ("Play Module 3 sessions to generate beta feedback"). **Module-agnostic helper architecture:** `_pfBetaQAStatsForModule(moduleId)` accepts any postflop module id; `_pfM3BetaQAStats()` is a thin wrapper. Reusable for Module 4+ (`_pfM4BetaQAStats()` etc.). New helpers: `_pfBetaQAStatsForModule`, `_pfM3BetaQAStats`, `_pfM3BetaQAWeakSpotPreview`, `_pfM3BetaQACriticalMonitor`, `_pfM3BetaQADashboardHtml`, `_pfM3BetaQACopySnapshotClick`, `_pfM3BetaSessionLeakHtml`. **Session summary polish:** new "🔥 Biggest leak this session" block in M3 session summary (uses `_pfM3BetaSessionLeakHtml`) — surfaces the top weak actionReason + "Best next review: [actionReason] — open Module 3 Weak-Spot Review to drill it." Honest copy: "(early signal — small sample)" appended when answers < 5. **Privacy/local-only confirmed:** zero `fetch`/XHR/network calls anywhere in new helpers; all data sourced from `_pfHistoryLoad()` (localStorage `rmtt_postflop_history`); copy snapshot uses clipboard only; privacy footer + JSON `_privacy` field both render the same disclaimer. **Browser QA mobile 375 + 360 + desktop 1280:** dashboard renders all 7 blocks + 6 metrics, 6-col grid wraps to 3 cols at ≤480px and 2 cols at ≤360px, no horizontal overflow at any tested viewport. **Programmatic QA verified:** simulated 3-session/15-answer/1-critical history → dashboard shows correct sessionCount=3, latestPctScore=90%, averagePctScore=80%, criticalCount=1 (6.7% rate), crb-habit=1, weakReviewUsed=true, weak-spot preview shows blocker_raise (2 misses, 4 candidates). Copy snapshot → 2,445-char valid JSON with all 14 expected fields populated. Button confirms "✓ Copied to clipboard" for 1.8s. M1/M2/M3 grading regression byte-identical (passthrough preserves their shape). **Production audit unchanged 385/0/0** (zero data edits). R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. **Forbidden files untouched:** postflop_scenarios.json / postflop_concepts.json / postflop_taxonomy.json / tools/audit-postflop-ps.ps1 / tools/audit-postflop-module3-seed.ps1 / tools/audit-postflop-module2-seed.ps1 / ranges.json / manifest.json / preflop / gamification all byte-identical. M1/M2/M3 scenario strategy fields byte-identical. **Net diff:** index.html +500 lines (QA helpers + CSS + Academy mount + session summary leak hint), service-worker.js 1-line VERSION bump, 1 new doc. appVersion + SW v4.2.5 → 4.2.6 (cache invalidation only). 10-step first-user manual QA script documented in v4.2.6 doc for real-device beta testing. New doc `docs/specs/postflop-v4.2.6-beta-feedback-loop.md`. New snapshot `GPT AUDIT/v4.2.6/`.

- **Project owner notes for v4.2.6:** Build-once-reuse-many discipline win. The QA dashboard is the first piece of Postflop Academy infrastructure that's been intentionally designed for reuse from day one — `_pfBetaQAStatsForModule(moduleId)` accepts any postflop module id, so M4 (Turn Strategy) and M5 (River) will inherit the same observability layer with a one-liner wrapper. The honest sample-size copy is critical: the brief said "do not imply solver-certified precision" and "(early signal — small sample)" + "needs more sessions for confident readings" + 6.7% crit-rate readings instead of fake-confident percentages all reinforce that Limited Beta means "we're observing, not certifying." The export snapshot pattern ($.kind, $.schema, $._privacy) is a clean foundation for any future cross-device QA tooling — even though v4.2.6 keeps it local-only, the schema is portable enough that v5.x.x could optionally enable opt-in cloud sync without re-designing the format. Pattern lessons for future modules: (1) module-agnostic helpers > module-specific hacks; (2) collapsed `<details>` is the right UX for diagnostic panels — not intrusive but discoverable; (3) sample-size warnings should appear automatically, not be a manual flag; (4) every observability surface should have a corresponding export so the project owner can review offline.

- **Last committed + pushed (V4.2.5 RECONCILIATION)**: `v4.2.5-doc` (`8b80b37`) — state reconciliation. Last committed + pushed (UX POLISH): `v4.2.5` (`55fa676`). **Targeted data + runtime sprint.** Three axes: (1) **Strategic critical-flag rebalance** — `check_raise_big` critical rate brought from **47/85 = 55.3% → 26/85 = 30.6%** via 21 surgically-justified downgrades. Each downgrade documented with poker rationale (hand has real equity backup like BDFD/1-card FD/gutshot/OESD/A-blocker, made-hand component like TPGK/TPTK/TPWK, sizing error vs severe punt, oversizing strong value). 26 KEEPs are uniquely-worst-bad cases: 5 naked-pair calls (mid pair / underpair, no draw OOP), 4 slowplay teachings (raise nut hand folds villain's wide air bucket = severe value punt), 17 BEST=fold naked-trash spots (no pair / no draw / no blocker = uniquely-worst). **Production audit unchanged 385/0/0.** R29 = 0 warnings preserved. M2 + M3 seed audits unchanged. **Zero changes to:** answer.best, answer.acceptable, answer.bad, recommendedAction, actionReason, conceptTags, explanation text, board.cards, heroHand, M1/M2 scenarios, audit rules. Only `answer.critical` arrays edited on 21 M3 scenarios. (2) **ActionReason-keyed weak-spot review** — `_pfCurrentSessionWeakProfile` extended with `targetActionReasons` + `targetHeroHandRoles` (looked up from live scenario pool because history.answers don't store these fields). `_pfWeakScenarioScore` extended with +50 boost for matching actionReason and +35 for matching heroHandRole. Verified with simulated 2-bad-blocker_raise session: top 4 candidates all `blocker_raise` scenarios. M3 weak-spot empty-state explicit toast: "Play Module 3 sessions to unlock BB Defense weak-spot review." (3) **UX polish:** mobile chip wrapping at 320/360/380px micro-mobile breakpoints (no horizontal overflow at any tested viewport); first-time M3 explainer (`_pfM3IsFirstTime` + `_pfM3FirstTimeExplainerHtml` — collapsible "About Module 3" block on Q1 of first M3 session, hides forever after); M3 Concept Library module-aware review signals (`_pfConceptReviewSignal` extended with module-match check + 8 alias-concept actionReason mappings — value_raise/protection_raise/semi_bluff_raise/blocker_raise/slowplay_call/domination_fold/bluff_catchers/range_disadvantage); Limited Beta progression hint after 3rd M3 session ("📈 Beta progress unlocked — now review your weakest BB Defense reasons" — honest copy, not "mastery"). **Browser QA mobile 320×568 + 360×740 + 375×812 + desktop:** no horizontal overflow at any width, first-time explainer renders + collapses cleanly, 4-axis chip row wraps gracefully, M3 grading correct, 0 console errors. **Programmatic QA:** actionReason weak-spot scoring verified (top 4 = all blocker_raise scenarios), M3 weak-spot empty-state toast fires, M1 + M2 regression-tested PASS via normalizer passthrough. **5-scenario poker spot-check post-edit:** Th8h-call-downgraded ✓, AsKh-blocker_raise-reason ✓, QdJh-domination_fold (kept critical) ✓, 8h7h-trips-slowplay (kept critical) ✓, TcTd-protection_raise (kept) ✓. All 5 grade 1.0 BEST. **Forbidden files untouched:** postflop_concepts.json / postflop_taxonomy.json / tools/audit-postflop-ps.ps1 / tools/audit-postflop-module3-seed.ps1 / ranges.json / manifest.json / preflop / gamification all byte-identical. M1/M2 scenario strategy fields byte-identical. **Net diff:** index.html +210/-8 (UX polish + helpers + CSS), service-worker.js 1-line VERSION bump, postflop_scenarios.json 21 critical-array edits, 1 new tool (downgrade-crb-critical-v4.2.5.ps1), 1 new doc. appVersion + SW v4.2.4 → 4.2.5. **Module 3 still playable as Limited Beta** — the rebalance + polish improves the learner experience without weakening any strategic truth. New doc `docs/specs/postflop-v4.2.5-module3-limited-beta-ux-polish.md`. New snapshot `GPT AUDIT/v4.2.5/`.

- **Project owner notes for v4.2.5:** Discipline win — the brief said "do not weaken critical flags merely to improve a metric" and the 21 downgrades all carry concrete poker reasoning (real equity backup, made-hand component, sizing error vs severe punt). The 26 KEEPs are uniquely-worst-bad cases where big-raising IS the conceptual punt. The 30.6% landing point isn't the 25% the brief targeted, but it's the strategically-justified bottom — going further would invalidate slowplay teachings or under-punish naked trash. The actionReason-keyed weak-spot review is the most architecturally important addition: it gives M3 a per-reason learning signal that the v4.2.4 generic engine couldn't provide. Score weights tuned (+50 actionReason vs +40 conceptTag vs +35 heroHandRole vs +60 family vs +100 scenarioId) so weak buckets cluster correctly without drowning out the broader concept space. The first-time explainer is intentionally compact (~100 words) and dismissible — no onboarding-flow over-engineering. Pattern lessons for future modules: (1) per-module review signals require module-aware filtering OR alias-concept mappings — both implemented for M3; (2) weak-spot review benefits from actionReason / heroHandRole axes when the module has structured decision categories; (3) mobile chip wrapping needs micro-mobile breakpoints below 380px even when 480px breakpoint exists; (4) the canonical PowerShell builder + critical-rebalance script is more maintainable than hand-edited JSON.

- **Last committed + pushed (V4.2.4 RECONCILIATION)**: `v4.2.4-doc` (`cc5c209`) — state reconciliation. Last committed + pushed (RUNTIME WIRE): `v4.2.4` (`81d54c5`). **Runtime-only sprint. NO data changes.** Wired Module 3 (Facing C-bet OOP) into the runtime as **"Limited Beta · 85 scenarios"** with the full learning loop. **Production audit unchanged 385/0/0** (no data file edits). R29 preserved at 0 warnings. M2 + M3 seed audits unchanged. **TRAINING_MODES.postflop.actions.m3 flipped:** `kind: 'preview'` / `route: null` / `icon: '🔒'` / hint "Coming in v4.2.4 beta" → `kind: 'secondary'` / `route: 'postflop:m3'` / `icon: '🛡️'` / `badge: 'BETA'` / hint "85 OOP defense scenarios · Limited Beta". **New `postflop:m3` route** in `runTrainingModeAction` calling `startPostflopDrill('pf_flop_cbet_oop_def', 10)` (default Limited Beta queue length 10). **Schema normalization (the architectural keystone of v4.2.4):** new `_pfNormalizePostflopChoices(scenario)` + `_pfNormalizePostflopAnswer(answer)` helpers handle M3's string-form `question.choices` (e.g. `["fold","call",...]`) and string-form `answer.best` (e.g. `"call"`) uniformly with M1/M2's object/array forms. `classifyPostflopAnswer` + `_findChoiceLabel` route through normalizers — M1/M2 grading is byte-identical because passthrough preserves their shapes. **`_PF_M3_ACTION_LABELS` (5 actions)** + **`_PF_M3_REASON_LABELS` (9 reasons)** provide pretty labels. **New `getModule3Scenarios()` helper** filters to `auditStatus === 'approved'` (Limited Beta cleanliness). **M3 question rendering:** dedicated BB-defending prompt framing ("BB has X on board... BTN c-bets ~33% pot. What is BB's best action?"); 4-axis chip row (handClass / heroHandRole / drawCategory / showdownValue); BB-OOP spot tag row distinct from M1/M2 BTN-IP framing; M3-flavored choice guide (`action_choice_m3` + `reason_choice_m3`) with vocabulary explicitly distinct from M2's IP-c-bet vocabulary. **NEW `_pfM3TeachingFeedbackBlocksHtml`** — Recommended Action (BB OOP) + **🎯 Defense Logic** (M3-defining field, prominent) + Hand Logic + Sizing Logic + 🃏 Blocker note + Range Context (collapsed) + 💡 Takeaway + ⚠️ Common Mistake. Hides empty fields. **Concept Library extended +10 entries:** 7 M3-native + 3 M3-alias concepts, all `module: 'm3'`, all `previewOnly: false`. New 3rd group header "Module 3 — BB Defense OOP (10 · Limited Beta)" in `_pfConceptLibraryHtml`. **M3 concept drill** wired through `startPostflopConceptDrill` (filters M3 pool, queue 10). **M3 weak-spot review** wired through `startPostflopWeakSpotReview` (routes by previously-played module; M1/M2/M3 pools never cross-contaminate; M3 weak queue 10). **NEW `_pfM3RenderSessionAggregations`** — handClass / heroHandRole / actionReason breakdowns (parallel to M2's pattern). **NEW `_pfM3MasteryStats` + `_pfM3MasteryProgressHtml`** with **Limited Beta thresholds** (3 sessions / 75% in 2 / all 9 actionReasons / weak-review used / no critical) — scaled vs M1/M2's full-module thresholds. Title explicitly "Module 3 Limited Beta progress (display only)" — honest copy. **`_PF_CURRICULUM` m3 entry** updated: status beta, 85 scenarios, 8-line syllabus, "▶ Start Module 3 Limited Beta" primary button. **`_pfModuleStatus('m3')` → 'beta'** (was 'locked'). **Concept Library hint** "Browse 15 concept drills" → "Browse 25 concept drills" to reflect 10 M1 + 5 M2 + 10 M3. **Browser QA mobile 375×812 + desktop:** TCC M3 tile renders with BETA badge + 🛡️ icon + "85 OOP defense scenarios · Limited Beta" hint; M3 drill starts; question screen shows BB-OOP framing with hero hand row + 4-axis chips; 5 string-form choices grade correctly; feedback panel shows defenseLogic prominently with all M3 teaching blocks; concept library shows M3 group; M3 mastery checklist renders with Limited Beta thresholds; curriculum card shows Beta status + Start button; M1 + M2 regression-tested PASS (normalizer passthrough preserves their shapes); 0 console errors. **Poker learning-product QA on 10 M3 scenarios + 1 M1 + 1 M2:** all 12 PASS — every M3 best graded 1.00 BEST, every non-best graded bad/critical correctly, all 10 had defenseLogic + takeaway populated. **CRITICAL — no data file edits:** postflop_scenarios.json / postflop_concepts.json / postflop_taxonomy.json / tools/audit-postflop-ps.ps1 / tools/audit-postflop-module3-seed.ps1 all byte-identical. ranges.json / manifest.json / preflop / gamification all byte-identical. M1 / M2 scenario strategy fields all byte-identical. **Net file diff: index.html +798/-61 lines (M3 wiring across 6 batches), service-worker.js 1-line VERSION bump.** appVersion + SW bumped 4.2.3B → 4.2.4 (cache invalidation). Limited Beta is ON: M3 is now playable from the Training Command Center, Postflop Academy curriculum card, and Concept Library. New doc `docs/specs/postflop-v4.2.4-module3-limited-beta-wire.md`. New snapshot `GPT AUDIT/v4.2.4/`.

- **Project owner notes for v4.2.4:** Module 3 ships as Limited Beta with the full learning loop wired. The architectural keystone was the schema normalization layer — M3's string-form choices/best are converted to M1/M2's object/array shape at every consumer boundary, so the rest of the runtime stays module-agnostic. This pattern will be reusable for any future Postflop module that uses a different schema (Module 4 turn play, Module 5 river polarization, etc.). The honest-labeling discipline ("Limited Beta," "85 scenarios," scaled mastery thresholds, "display only") is critical: the user explicitly said don't overclaim stability, and v4.2.4 doesn't. The mastery checklist title is "Module 3 Limited Beta progress (display only)" not "mastery"; the curriculum syllabus closes with "Limited Beta · 85 approved scenarios"; the TCC tile says "85 OOP defense scenarios · Limited Beta" — every surface is consistent. Pattern lessons for future runtime sprints: (1) normalize schema at the boundary, never in the renderer; (2) extend existing renderers via `isM3` flags rather than creating parallel duplicate renderers — keeps drift low; (3) the canonical `handlePostflopChoiceById` works programmatically AND through the delegated DOM listener — both paths grade identically; (4) postflop history doesn't need M3-specific schema — sessions are keyed by `module` so M3 sessions are filterable from the same `_pfHistoryLoad()` source as M1/M2.

- **TCC `kind` enum clarification (added during v4.2.4 reconciliation):** the TRAINING_MODES `kind` field uses three values in the codebase: `'primary'`, `'secondary'`, `'preview'`. There is **NO `'available'` value** — that term appears in some sprint briefs colloquially to mean "clickable + non-preview," but the actual enum is `'secondary'`. Runtime treatment: `_tccBuildActionTileHtml` adds `is-primary` class for primary actions, `is-preview` class for preview actions (and renders a SOON badge + omits the click handler), and treats anything else as a clickable secondary CTA. `runTrainingModeAction` early-returns with a "coming soon" toast iff `action.kind === 'preview' || !action.route`. So `kind: 'secondary'` + `route: 'postflop:m3'` makes the M3 tile fully clickable with the BETA badge, distinguished visually from the M1 primary tile (which has the lead-CTA accent). This is **intentional and correct** — do NOT change M3 to `kind: 'available'` (the value doesn't exist in the codebase) or `kind: 'primary'` (would visually compete with M1 as the postflop lead CTA). Future modules should use `kind: 'secondary'` + a real `route` to flip from preview → live, mirroring v4.2.4's M3 pattern.

- **Last committed + pushed (MODULE 3 DATA POLISH)**: `v4.2.3B` (`e72eef6`) — Module 3 Data Polish + Thin-Bucket Completion. **Quality + sourceConfidence sprint. NO runtime wiring.** Polished Module 3 from 62 → **85 production scenarios** (+23 across 5 new board families + 1 added to existing 8c8d3s). **Production audit raised 362/0/0 → 385/0/0** (251 M1 + 49 M2 + 85 M3). R29 card-notation guard preserved at 0 warnings. M2 + M3 seed audits unchanged. **5 new boards:** Kh Qh 4s (K-high two-tone broadway dynamic), Kh Jh 4h (K-high monotone), Qd 7d 2c (Q-high two-tone dry), Ac Ad 7s (A-high paired), Ts 9s 5d (mid-connected two-tone dynamic). M3 distinct boards: 14 → **19**. **ALL 5 thin-bucket targets met:** blocker_raise 1→**4** ✓; domination_fold 2→**5** ✓; nut_flush_draw drawCategory 1→**3** ✓; slowplay_call 3→**5** ✓; protection_raise 3→**6** ✓. **15 textbook scenarios promoted from `expert_judgment` → `consensus_gto`** with documented promotion criteria (set raises on dry boards, naked overcards no equity, backdoor-only fold on broadway, no-spade overcards on monotone, nut flush on monotone raise, AA overpair value-raise on rag). M3 sourceConfidence: 70 expert + 15 consensus_gto. Difficulty spread improved (8/59/15/3 across 2/3/4/5 vs v4.2.3A's 7/45/9/1). **Anti-suit-swap discipline:** filler scan = 0 cross-bucket / 0 suit-only dupes / 0 missing uniquenessNotes / 0 card collisions. Multiple suit-similar scenarios (e.g., 4 blocker_raise on different textures monotone vs two-tone vs dry vs broadway, 3 nut_flush_draw on different boards) JUSTIFIED BY UNIQUE STRATEGIC DIMENSIONS documented per scenario. **Strategic 12-scenario poker spot-check: all 12 PASS** (blocker_raise positive + distinction, domination_fold weak Qx, nut FD call/raise, slowplay trips paired-A + overpair paired-low, protection_raise top set wet, pot odds marginal call, range disadv fold no blocker, reason_choice + consensus_gto promotion). New canonical authoring tool `tools/build-polish-v4.2.3B.ps1` (PSCustomObject definitions, ASCII-only) + migration tool `tools/migrate-polish-v4.2.3B.ps1`. **CRITICAL — Module 3 still NOT playable, NOT routable, NOT runtime-wired:** TRAINING_MODES.postflop.actions.m3 still `kind: 'preview'`, `route: null`. Runtime helpers byte-identical (only appVersion + SW VERSION lines changed in index.html / service-worker.js — 1 line each). Net runtime effect: `App.state.postflop.scenarios` 362 → 385 in memory but no UI surface routes the new 23 (or any of the 85 M3 scenarios). appVersion + SW bumped 4.2.3A → 4.2.3B (cache invalidation only). **Limited Beta readiness assessment: M3 IS NOW READY for v4.2.4 runtime wire.** All thin buckets filled; 19 distinct boards; 15 consensus_gto anchors; 9 actionReasons all ≥4. v4.2.4 should label as "Limited Beta · 85 scenarios" with scaled mastery thresholds (e.g., 3 sessions / 75%+ in 2 / all 9 reasons seen). New doc `docs/specs/postflop-v4.2.3B-module3-data-polish.md`. New planning seed file `docs/specs/postflop-v4.2.3B-module3-polish-seeds.json` (23 scenarios with uniquenessNotes). New snapshot `GPT AUDIT/v4.2.3B/`. No ranges.json / preflop / gamification / wardrobe / TRAINING_MODES logic changes.

- **Project owner notes for v4.2.3B:** Quality > count discipline maintained. The thin-bucket completion was the headline target — all 5 buckets met or exceeded. The most subtle lesson added is the BLOCKER_RAISE distinction: 4 scenarios across 4 different textures (low monotone, K-high two-tone, K-high monotone, Q-high dry two-tone) testing that hero correctly identifies the actionReason as "blocker" (not "semi-bluff") when the EV comes from removing villain combos rather than from outs. This is the kind of nuance that distinguishes a beginner module from an intermediate one and now has enough volume to support concept drills + weak-spot review reliably. The sourceConfidence promotion (15 → consensus_gto) gives the module credible authority for its most textbook lessons while keeping the close-mix and solver-uncertain spots honestly at expert_judgment. v4.2.4 runtime wire is now defensible — Limited Beta labeling is the recommended path forward. Pattern lessons: (1) the canonical PowerShell builder + migration script pattern (now 3 versions deep: v4.2.3, v4.2.3A, v4.2.3B) is the right pattern for any future expansion sprint; (2) ASCII-only authoring scripts continue to be required to avoid CP874 mojibake; (3) sourceConfidence promotion criteria should be documented per-spot, not blanket-applied.

- **Last committed + pushed (MODULE 3 DATA EXPANSION)**: `v4.2.3A` (`14fb380`) — Module 3 Data Expansion. **Data expansion sprint. NO runtime wiring.** Expanded Module 3 from 24 → **62 production scenarios** (+38 new scenarios across 8 new board families). **Production audit raised 324/0/0 → 362/0/0** (251 M1 + 49 M2 + 62 M3). R29 card-notation guard preserved at 0 warnings. M2 + M3 seed audits unchanged. **8 new board families:** As9s4d (A-high two-tone dry), Ks8s3d (K-high two-tone dry), QsTs6d (Q-high two-tone dynamic), 7s5s3s (low monotone), 8c8d3s (paired low rainbow), 9d8c6h (low semi-connected rainbow), TcTh6s (paired T rainbow), 6c3d2h (very dry rag rainbow). M3 distinct boards: 6 → **14**. **Coverage gap fixes:** `pot_odds_defense` 0→5 primary tags (was missing entirely); `blocker_raise` actionReason 0→1 (introduced via AsKh reason_choice on monotone — first M3 blocker_raise lesson); `slowplay_call` 1→3 (paired-low trips, paired-T trips); `protection_raise` 1→3 (top set on connected, TPGK on K-high two-tone, two-pair on connected); `domination_fold` 1→2 (QJ on K-high two-tone added); `bluff_catch` 2→7 (3.5x); `nut_flush_draw` drawCategory 0→1 (AsQs on Ks8s3d). **Difficulty spread improved:** all 24 v4.2.3 were diff 3; now 7 diff 2 + 45 diff 3 + 9 diff 4 + 1 diff 5. **Filler discipline:** anti-suit-swap rule enforced via filler scan (0 cross-bucket boards, 0 suit-only dupes, 0 missing uniquenessNotes — every new scenario carries a uniquenessNote explaining new strategic dimension). **Strategic spot-check on 12 scenarios across major themes (pot odds call/fold, bluff-catcher call, dominated fold, semi-bluff raise, protection raise, range disadv fold, slowplay, paired bluff-catch, monotone call/fold, blocker_raise reason): all 12 PASS.** Migration via NEW `tools/build-expansion-v4.2.3A.ps1` (canonical authoring script with PSCustomObject scenario definitions) + `tools/migrate-expansion-v4.2.3A.ps1` (idempotent, UTF-8 NO-BOM). Mid-sprint text-integrity caught: 1 Thai-mojibake byte (≈ symbol round-tripped through CP874 to U+0E42); fixed inline by replacing with ASCII `~`. Final: 0 mojibake. **CRITICAL — Module 3 still NOT playable, NOT routable, NOT runtime-wired:** TRAINING_MODES.postflop.actions.m3 still `kind: 'preview'`, `route: null`. Runtime helpers byte-identical (only appVersion + SW VERSION lines changed in index.html / service-worker.js). Net runtime effect: `App.state.postflop.scenarios` 324 → 362 in memory but no UI surface routes the new 38 (or any of the 62 M3 scenarios). appVersion + SW bumped 4.2.3 → 4.2.3A (cache invalidation only). **Training volume assessment:** 62 scenarios is in the 50-80 target range; 8 of 9 actionReasons have ≥3 representation; only `blocker_raise` (1) is still thin. v4.2.3B optional (focus on blocker_raise depth + sourceConfidence promotion); v4.2.4 (runtime wire as Limited Beta · 62 scenarios) is acceptable now if labeled honestly with scaled mastery thresholds. New doc `docs/specs/postflop-v4.2.3A-module3-data-expansion.md`. New planning seed file `docs/specs/postflop-v4.2.3A-module3-expansion-seeds.json` (38 scenarios with uniquenessNotes). New snapshot `GPT AUDIT/v4.2.3A/`. No ranges.json / preflop / gamification / wardrobe / TRAINING_MODES logic changes.

- **Project owner notes for v4.2.3A:** Discipline-led expansion. The anti-suit-swap rule was the key gate — every new scenario had to add a strategic dimension beyond cosmetic suit changes. The 38 scenarios were authored as PSCustomObjects in a canonical PowerShell builder, then migrated through the same enrichment pipeline as v4.2.3, ensuring schema parity. The biggest coverage win was `pot_odds_defense` (0→5 primary), which now anchors the threshold-defense lessons that make M3 trustworthy. The `blocker_raise` reason_choice on AsKh/7s5s3s monotone is the most advanced new lesson (difficulty 5) — it tests recognition that some OOP raises are reason="blocker" not "semi-bluff," which is the kind of subtle distinction that separates a beginner module from an intermediate one. With 62 scenarios and 14 distinct boards, M3 is now content-strong enough for v4.2.4 runtime wire IF the project owner is willing to label it as "Limited Beta · 62 scenarios" with scaled mastery (vs M1's mature 251). v4.2.3B is the more conservative path (push to ~80 first); both are defensible. Pattern lessons: (1) the canonical PowerShell builder (build-expansion-v4.2.3A.ps1) is much more maintainable than hand-edited JSON for 38 scenarios — future expansions should copy this pattern; (2) the filler scan must be done BEFORE migration, not after — caught 0 dupes here but the scan is the hard gate; (3) authoring tools must use ASCII-only chars (no `≈`) to avoid CP874 mojibake on Windows PowerShell write paths.

- **Last committed + pushed (MODULE 3 MIGRATION TO PRODUCTION)**: `v4.2.3` (`e718f07`) — Module 3 Migration to Production Data. **Data + audit hardening sprint. NO runtime wiring.** Migrated all 24 finalized v4.2.0 Module 3 (Facing C-bet OOP) planning seeds into production `postflop/postflop_scenarios.json`. **Production audit raised 300/0/0 → 324/0/0** (251 M1 + 49 M2 + 24 M3). **R29 card-notation guard preserved at 0 warnings.** M2 + M3 seed audits unchanged. Migration enriches each M3 seed with production-only fields: `version`, `game="NLH_MTT"`, `street="flop"` (top-level), `actionHistory=[]`, `scoring={best:1,acc:0.5,bad:0,crit:0}`, `difficulty=3`, plus board enrichment (`connectedness`, `pairedStatus`, `dynamicLevel`, `rangeAdvantage`, `nutAdvantage`) per the 6 board families. Strips planning-only `reviewStatus`. Migration auditStatus path: `planning_only → review_pending → approved` (via the migration script then a flip pass once production audit passed clean). **Concepts file extended +10:** 7 M3-native (`oop_defense_threshold`, `check_raise_value`, `check_raise_bluff`, `bluff_catchers`, `equity_realization_oop`, `range_disadvantage`, `pot_odds_defense`) + 3 M3-alias (`value_raise`, `protection_raise`, `semi_bluff_raise`) bringing total 15 → 25. **Taxonomy file extended:** `heroHandRole.module2[]` + `heroHandRole.module3[]` (incl. `bluff_catcher`, `dominated_marginal`); `actionReason.module2[]` + `actionReason.module3[]` (incl. `slowplay_call`); `auditStatusValues[]` += `review_pending`, `planning_only`; `textureTags[]` += `static`; new `modules.pf_flop_cbet_oop_def` entry. **Production auditor (`tools/audit-postflop-ps.ps1`) extended with R30–R41** covering M3 spot/action/reason/vocabulary/answer-consistency/explanation/conceptTags/sourceConfidence/villainAction/villainSizing/heroHand-collision/moduleId rules — applied only when `module === 'pf_flop_cbet_oop_def'`. R04/R05 generalized to handle M3's string-form `question.choices` and string-form `answer.best` (different from M1/M2 which use object-form choices and array-form best). Audit-plan doc renumbered R29-R40 → R30-R41 because R29 is owned by the v4.2.2D/E card-notation guard. **Migration tool (NEW):** `tools/migrate-module3-v4.2.3.ps1` — idempotent one-shot script (UTF-8 NO-BOM via `[System.IO.File]::WriteAllText`). **Strategic spot-check on 8 critical M3 scenarios verified by reviewer** — all 8 strategic verdicts match expert-judgment GTO intuition for BB-vs-BTN SRP 100BB OOP defense. **CRITICAL — Module 3 still NOT playable, NOT routable, NOT runtime-wired:** TRAINING_MODES.postflop.actions.m3 still `kind: 'preview'`, `route: null`, hint "Coming in v4.2.4 beta". `runHomeCommandCenterMount`, `runTrainingModeAction`, `startPostflopDrill`, postflop loaders all byte-identical. Net runtime effect: `App.state.postflop.scenarios` 300 → 324 in memory but no UI surface routes the new 24. appVersion + SW bumped 4.2.2G → 4.2.3 (cache invalidation only). **Training volume caveat documented:** 24 scenarios is acceptable for migration but too thin for a playable beta — v4.2.4/v4.2.5 must expand to ~80–120 scenarios before flipping the M3 tile from preview to available. New doc `docs/specs/postflop-v4.2.3-module3-migration.md`. New snapshot `GPT AUDIT/v4.2.3/`. No ranges.json change. No preflop / gamification / boss / mission / wardrobe / collection-book / answer-fx / field-fx / aura touched.

- **Project owner notes for v4.2.3:** Migration sprint succeeded with discipline — the 24 seeds went production with zero strategic content changes (only structural enrichment for production schema parity). The R30–R41 audit extension is reusable for any future M3 seed expansion in v4.2.3A / v4.2.4 / v4.2.5 — they enforce schema integrity without touching strategic correctness (which is the seed audit's job). The 3 alias concepts (value_raise, protection_raise, semi_bluff_raise) parallel M2's value_betting / protection_betting / semi_bluff_with_equity but in OOP-raise framing — keeping them as separate concepts (rather than tagging M3 seeds with the M2 concept names) preserves the curriculum's mode-specific framing for the eventual Concept Library M3 tab. Two architectural lessons: (1) M3's string-form choices vs M1/M2's object-form choices required generalizing R04/R05 — future modules should pick one schema and stick with it; (2) the v4.2.0 audit-plan's R29 numbering was already consumed by the v4.2.2D/E text-integrity guard, so the migration had to renumber to R30+. Future audit plans should avoid claiming hard rule numbers without checking the current guard registry.

- **Pending push (STAGED, COMMAND CENTER POLISH)**: `v4.2.2G` — (NOTE: now historical; committed at `8c150ab`.) Command Center Polish + Routing Honesty Pass. **Focused UX polish on top of v4.2.2F. No architecture change. No data/audit/preflop/gamification touched.** Six v4.2.2F preview-review findings addressed: (1) **Header copy sharper** — title `"Choose your training world"` → `"Choose Your Training Path"`; helper trimmed 22 → 12 words to `"Build ranges preflop. Sharpen decisions postflop. Pick one focus for this session."` (2) **Preflop status-pill asymmetry fixed** — added `metaPills` field to TRAINING_MODES registry; Preflop now shows `["Drills · Exams · Boss", "Ranks · Browse"]` always + dynamic rank/answer pills when available (matches Postflop's M1/M2 status pattern). (3) **Boss Tests · Missions routing honesty** — renamed to `"Training Setup"` with hint `"Choose drill, boss, or exam mode"` + icon ⚙ (was ⚔); route still `preflop:drillsetup` (Drill setup screen) — label now matches destination. (4) **Concept Library hint precision** — `"15 concept drills"` → `"Browse 15 concept drills"` (honest about scroll-to-library navigation, doesn't imply auto-start). Postflop Progress hint similarly: `"Mastery + history"` → `"View mastery + history"`. (5) **Premium visual polish** — selected-card corner indicator dot via `::after` (8×8px, accent-colored, glow); panel top-edge accent line via `::before` (gradient line, mode-colored, opacity 0.45); stronger primary CTA gradient + box-shadow + brighter hover; unselected card opacity 0.78 → 0.72 for stronger contrast. (6) **Icon consistency** — kept ♠ + 🎯 (each carries semantic meaning); CSS containers already provide visual consistency. **Architecture 100% preserved:** TRAINING_MODES registry preserved (only additive `metaPills` field), all 4 helpers unchanged, M3 still `kind: 'preview'`, `route: null`, no `onclick` attribute on M3 tile (verified via DOM inspect). **Browser QA via Preview MCP at mobile 375×812:** TCC shell 343px (no overflow), mode buttons 156.5px each (2-up), Preflop selected dot blue + Postflop selected dot orange visible at top-right corners, both panels show subtle accent line at top edge, primary CTAs visibly more saturated, status pills wrap cleanly, 0 console errors. appVersion + SW bumped 4.2.2F → 4.2.2G. Production audit unchanged 300/0/0 (R29 = 0). M2 + M3 seed audits unchanged. New doc `docs/specs/postflop-v4.2.2G-command-center-polish.md`. UI captures saved to `GPT AUDIT/screenshots/v4.2.2G/`. **Module 3 NOT productionized, NOT runtime-wired, NOT playable. v4.2.3 still paused.**

- **Project owner notes for v4.2.2G:** This sprint demonstrates the "polish without rewrite" pattern. v4.2.2F's B+ architecture was correct but had 6 honest UX gaps. All 6 fixed via additive changes (new `metaPills` field, CSS-only visual polish, label/hint rewrites) — zero breaking changes to the 4 helpers or the 12 routes. The discipline of routing honesty ("Boss Tests · Missions" → "Training Setup" because the route doesn't directly land on Boss) reinforces the §7.5 5-property rule: visual promise must match actual behavior. Future sprints adding more modes / more actions should follow the same pattern: extend the registry, don't rewrite the engine.

- **Pending push (STAGED, PRODUCT MODE SYSTEM FOUNDATION)**: `v4.2.2F` — (NOTE: now historical; committed at `159dd6f`. Detail kept for reference.) — Product Mode System Foundation + Premium Home Command Center. **UX architecture sprint.** Replaces v4.1.8 misleading home tabs (which were a fair user complaint — Preflop tile just `switchTab('drill')`, Postflop tile just smooth-scrolled) with a real foundation: (1) **`TRAINING_MODES` registry** = central metadata for both modes (id, title, accent, subtitle, description, 6 actions each with id/label/hint/icon/kind/route/badge), (2) **4 helper functions** (`getTrainingMode`, `setTrainingMode`, `getTrainingModeMeta`, `runTrainingModeAction`), (3) **Premium Home Command Center** with `.tcc-shell` (eyebrow + title + helper) + `.tcc-selector` (2-card segmented selector with strong accent-colored selected state) + `.tcc-panel` (mode-aware command panel with hero + status pills + 6-action grid), (4) **safe CTA routing through `runTrainingModeAction(mode, id)`** — no scattered onclick strings; every visible CTA is registry-driven. **Preflop panel:** Quick Drill (primary) / Deep Drill / Weakness Review / Marginal Spots / Browse Ranges / Boss Tests · Missions — all real existing routes (sets `App.state.drill.mode` then `switchTab('drill')` + `startDrill()` for the 4 mode-based drills, `switchTab('browse')` for ranges, `switchTab('drill')` to Drill setup for Boss/Mission/Exam pickers). **Postflop panel:** Module 1 · Board Texture (primary, 251 scenarios) / Module 2 · Flop C-bet IP (BETA badge, 49 scenarios) / Concept Library / Weak Spot Review / Postflop Progress / **Module 3 · BB Defense OOP (preview, SOON badge, route=null)** — M3 explicitly preview-only, no real destination, click shows "coming soon" toast. Mode persists via existing `App.state.settings.trainingMode` (saved to `localStorage.rmtt_settings`). **Mobile 375×812 verified:** TCC shell 343px (no overflow), mode buttons 2-up at 156.5px each, action grid 2-col mobile / 3-col tablet, primary CTA spans full width, bottom nav not overlapped, install-banner safe-area-inset still working from v4.2.2C. **Permanent UX principle documented** (5 mandatory properties for any future mode/tab/world UI: visual promise, actual behavior, user mental model, routing destination, fallback if route not ready). **Lessons from v4.1.8 captured as anti-patterns** to prevent recurrence (tile-as-shortcut, scroll-only navigation, collapsing complex area into one default, missing fallback for unimplemented routes, UI without state foundation). **Bottom-nav rewrite intentionally deferred** to v4.3.x once M3 + future modules validate the foundation. **No data/audit/preflop/gamification files touched.** Production audit unchanged 300/0/0 (R29 = 0 warnings). M2 + M3 seed audits unchanged. appVersion + SW v4.2.2F. New doc `docs/specs/postflop-v4.2.2F-product-mode-system-foundation.md` (~430 lines). **Module 3 NOT productionized, NOT runtime-wired, NOT playable.** v4.2.3 migration still paused.

- **Project owner notes for v4.2.2F:** This sprint is the explicit fix for a fair user criticism — the v4.1.8 home tabs looked like mode navigation but behaved like shortcut buttons. The criticism became a permanent design rule (the 5-property requirement) rather than just a one-off fix. Every future mode/tab UI must answer the 5 questions before code review. **Architecturally, the foundation built here is reusable:** when M3 productionizes in v4.2.3 + v4.2.4, the only change is `m3.kind: 'preview' → 'primary'` and `m3.route: null → 'postflop:m3'` in the registry, plus one route case in `runTrainingModeAction`. The visual / mode-button / panel-swap behavior is zero-effort. Same pattern scales to M4+ and any future training context (turn play, river polarization, multi-way, ICM, tournament-stage modes, etc.).

- **Pending push (STAGED, FINAL TEXT INTEGRITY + R29 HARDENING)**: `v4.2.2E` — (NOTE: now historical; committed at `3cc3704`. Detail kept for reference.) — Final Postflop Text Integrity Repair + Poker-Semantic Guard Hardening. **Production text-integrity correction. No strategic content changes.** v4.2.2D's Pattern 4 (`[R] — [R] —` → `[R][R]s` for combo lists) over-triggered on board references that escaped the `On [R] — [R] — [R] — ` board-rebuild — producing fake suited-hand strings like `KTs 2` (originally `Kh Td 2s`). Three other patterns also leaked through v4.2.2D: `A — X` / `K — X` flush combo residue, and `5 — /7 — /8` rank-x list residue. **14 scenarios / 18 text-field edits.** Repairs use `scenario.board.cards` to rebuild the genuine board (e.g., "on KTs 2" → "on Kh Td 2s"); flush prose `A — X` → `A-X` (canonical); rank-x list `5 — /7 — /8-holdings` → `5x / 7x / 8x holdings`. **R29 guard hardened with 3 new patterns** (`board_collapse`, `rank_dash_X`, `rank_dash_slash`) bringing total to 6 patterns. **R29 verified against 13 test cases: 6/6 positive flagged correctly, 7/7 negative not flagged** (e.g., `KTs is in BTN range` is not flagged, but `on KTs 2` is flagged). **15-field strategic integrity check: 0 changes** to id/module/auditStatus/sourceConfidence/recommendedAction/actionReason/answer.*/conceptTags/handClass/heroHandRole/drawCategory/showdownValue. **Manual poker-semantic spot-check on 5 critical scenarios:** Kh Td 2s nutadv (rebuilt board, sets/two-pair/TPGK identification preserved), Kh Td 2s set scenario TcTh (set logic preserved), Kh Td 2s OE QcJc (semi-bluff logic preserved), Th 8h 3h monotone (A-X nut flush prose clean), 8s 7s 5s low monotone (5x/7x/8x readable, BB caller-crush logic preserved) — all PASS. **Browser QA via Preview MCP at mobile 375x812: 0 console errors, all 6 defective pattern categories = 0 in runtime DOM, runtime data + sample renders all clean.** appVersion + SW bumped 4.2.2D → 4.2.2E. Production audit unchanged 300/0/0 (R29 fires 0 warnings — true negative now). M2 seed audit unchanged 24/0/8. M3 seed audit unchanged 24/0/0. Scenario count unchanged 300 = 251 M1 + 49 M2 + 0 M3. New doc `docs/specs/postflop-v4.2.2E-final-text-integrity-repair.md`. **No Module 3 productionization. No strategic content / answer-key changes. No ranges.json change. No runtime UI logic change beyond version bump.**

- **Project owner notes for v4.2.2E:** Third-order correction of the v4.2.2C cleanup chain. Lesson: when the v4.2.2D R29 guard reported 0 warnings, that was a FALSE NEGATIVE — the regex set was too narrow. v4.2.2E adds the missing pattern detectors (board_collapse, rank_dash_X, rank_dash_slash) and verifies via positive/negative test suite that the guard now matches what it claims to. Future text repair sprints should ALWAYS include positive-case tests against the guard rules — a 0-warnings result is meaningful only if the guard is verified to fire on the bug class it claims to cover. R29 now has 6 patterns and 13 verified test cases.

- **Pending push (STAGED, CARD/SUIT NOTATION REPAIR)**: `v4.2.2D` — (NOTE: now historical; committed at `f53b425`. Detail kept for reference.) — Postflop Card / Suit Notation Semantic Repair. **Production text-integrity repair after v4.2.2C. No strategic content changes.** v4.2.2C eliminated visible CP874 mojibake but blanket-replaced all mojibake-character runs with em-dashes — including the runs that were originally suit symbols inside card notation prose. This left semantically broken text like "BTN with K — K — on 6 — 5 — 4 —" (was "BTN with KhKs on 6c 5c 4s") and "A — -x" (was "Ah-x"). v4.2.2D applies a context-aware repair using `scenario.board.cards` + `scenario.heroHand` arrays to reconstruct meaning. **Two-pass repair: 263 scenarios touched, 525 text-field edits.** Pass 1 replaced 6 specific patterns (rank-dash-dash-x, BTN-with-pair, On-board, suited-combo lists, sentence residuals); Pass 2 cleaned 251 trailing em-dash residuals after the rebuilt card prompts. **All 4 suspicious card-notation patterns now 0 in the data.** 186 legitimate em-dashes preserved as sentence punctuation (e.g., "K9s+, KJo+ — many cards", "They don't — sets of 5 are rare"). **15-field strategic integrity check: 0 changes** to id/module/auditStatus/sourceConfidence/recommendedAction/actionReason/answer.best/answer.acceptable/answer.bad/answer.critical/conceptTags/handClass/heroHandRole/drawCategory/showdownValue. Added new **R29 audit guard rule** to `tools/audit-postflop-ps.ps1` (warning-only) detecting the three suspicious card-notation patterns so any future regression surfaces immediately during the regular production audit. **Browser QA via Claude Preview MCP at mobile 375x812: M1 prompt now reads "On Ah Kd 5c (BTN open vs BB call, 100BB SRP), who has range advantage?", M2 prompt reads "With AhKh on As 8d 3h ...", explanation prose preserves legitimate em-dashes ("They don't — sets of 5...").** appVersion + SW bumped 4.2.2C → 4.2.2D for cache invalidation. Production audit unchanged **300/0/0** (R29 fires 0 warnings — data is clean). M2 seed audit unchanged 24/0/8. M3 seed audit unchanged 24/0/0. Scenario count unchanged (300 = 251 M1 + 49 M2 + 0 M3). New doc `docs/specs/postflop-v4.2.2D-card-suit-notation-repair.md`. **No Module 3 productionization. No strategic content / answer-key changes. ranges.json untouched (already clean from v4.2.2C).**

- **Project owner notes for v4.2.2D:** This sprint is the second-order correction of the v4.2.2C over-aggressive cleanup. Lesson learned for future data hotfixes: when replacing mojibake, distinguish between mojibake-from-em-dash (replace with em-dash) and mojibake-from-suit-symbols (replace using structured `board.cards` / `heroHand` reconstruction). The R29 audit guard now prevents this specific class of regression silently slipping into production. Also: even legitimate punctuation em-dashes can survive — the heuristic "em-dash between lowercase letters/words/digits = keep; em-dash between/after uppercase ranks A K Q J T = repair" works well for the postflop English copy. Strategy fields verified untouched via 15-field integrity check; this should be the project-owner template for any future text-only data sprint.

- **Pending push (STAGED, RUNTIME TEXT HOTFIX)**: `v4.2.2C` — (NOTE: now historical; committed at `8896504`. Detail kept for reference.) — Runtime Text Encoding / Mojibake Hotfix. **Production UX hotfix. No strategic content changes.** Cleaned 292 of 300 production scenarios + 8 ranges.json position-description lines of CP874 mojibake (em-dash `—` U+2014 round-tripped through CP874 became long Thai-character runs like `เน€เธโฌเน€เธยเธขย...`). Source data fix is durable. Added `_pfFixMojibake` runtime safety net to the M1 expandable explanation sections (Range Logic / Nut Logic / Hand Logic / Sizing Logic / Common Mistake at index.html line ~35792-35805) — defense-in-depth for any future authoring slips. **Caught and fixed a regression** introduced by the data clean: the v4.0.10 `_pfFixMojibake` was stripping isolated clean em-dashes (em-dash maps to CP874 byte 0x97 which is invalid as a leading UTF-8 byte, so decode failed and fallback emitted U+0097 C1 control); fixed by tracking original chars in parallel to the byte buffer and restoring originals on decode failure. Verified `_pfFixMojibake('Hi — there')` now round-trips identically. Fixed PWA install banner CSS to use `margin: calc(env(safe-area-inset-top, 0px) + 8px)` so it no longer crowds the iOS status bar / clock area. **appVersion + service-worker VERSION bumped 4.1.9 → 4.2.2C** for cache invalidation. **Browser QA via Claude Preview MCP at 375x812 mobile viewport: 0 console errors; 0 mojibake / 0 replacement chars in runtime DOM across 300 scenarios; 292 scenarios contain valid em-dashes; M1 explanation sections render cleanly ("They don't — sets of 5 are rare..."); install banner sits with proper inset.** Production audit unchanged **300/0/0**. M2 seed audit unchanged **24/0/8**. M3 seed audit unchanged **24/0/0**. New doc `docs/specs/postflop-v4.2.2C-runtime-text-encoding-hotfix.md`. **No Module 3 productionization. No strategic content changes. No answer keys altered.**

- **Project owner notes for v4.2.2C:** This was a user-trust hotfix that took priority over v4.2.3 migration. Three sub-defects layered: (a) source data corruption from v4.0.0 baseline authoring, (b) v4.0.10 reverser was incomplete (M1 expandable sections never wrapped), (c) reverser regression that broke clean em-dashes after the source data fix. All three resolved. The fix preserves the v4.0.10 reverser as defense-in-depth (real mojibake sequences still get reversed) while no longer corrupting clean text. v4.2.3 migration can now resume on a clean runtime.

- **Pending push (STAGED, MODULE 3 POST-COMMIT VERIFICATION)**: `v4.2.2B` — (NOTE: now historical; committed at `1ee6cc1`. Detail kept for reference.) — Module 3 Post-Commit Raw Verification + Migration Readiness Gate. **Verification + repair sprint. No production data, no runtime wiring, no version bumps.** Independently verified all v4.2.2 claims against the raw seed JSON and against architecture/schema/audit-plan/auditor cross-refs. **Found and fixed 3 defects in-place:** (1) F6.2 conceptTags `[check_raise_value, value_raise]` were stale from v4.2.0 (best was raise) and didn't match the v4.2.2 slowplay flip — fixed to `[bluff_catchers, pot_control, value_raise]` (slowplay lesson primary; value_raise tertiary for the acceptable raise option); (2) F3.3 acceptable list had `protection_raise` which doesn't fit Td9d (a pure draw with no made hand to protect) — moved to `bad`; (3) **Top-level summary metadata block was stale** (reflected v4.2.0 distribution, never updated through v4.2.1's F6.1 reason flip or v4.2.2's F6.2 + slowplay_call addition + 24 reviewStatus flips) — full recompute applied + added `metadataRecomputedAt: 2026-05-06_v4.2.2B` freshness marker. **All v4.2.2 strategic claims verified correct after repair:** F1.3 critical=[semi_bluff_raise] justified by sizingLogic; F5.3 critical=[semi_bluff_raise] correct (semi_bluff is wrong reason for *calling*, blocker_raise is the raise reason); F6.2 best=call/reason=slowplay_call/explanation reframed; F5.4 range_disadvantage_fold correct (no RIO needed); F6.4 fold-with-call-acceptable correct; slowplay_call vocab consistent across 6 locations (auditor + 4 docs + JSON). **Training Volume Gate assessment:** 24 scenarios is ACCEPTABLE for planning lock and ACCEPTABLE for production migration (as planning data) but NOT ACCEPTABLE for stable runtime exposure. Per-concept primary-tag depth: only `equity_realization_oop` (8) at threshold; 6 of 7 native M3 concepts below 8-12 healthy target. Action reasons severely underrepresented: slowplay_call (1), protection_raise (1), domination_fold (1), bluff_catch (2). **Migration readiness decision: Path A — proceed with v4.2.3 migration** (24 → production data + 7 concepts to concepts.json + 2 heroHandRole values to taxonomy.json + R29-R40 to production auditor + appVersion/SW bump). **Recommended insertion: v4.2.3A data expansion sprint** between v4.2.3 (migration) and v4.2.4 (runtime wire) to bring M3 from 24 → 40+ scenarios. If v4.2.3A is skipped, v4.2.4 must label M3 as "Limited Beta · 24 scenarios" with scaled mastery. Final M3 seed audit (post-v4.2.2B): **24 / 0 hard / 0 warnings PASS clean**. Production audit unchanged **300/0/0**. M2 seed audit unchanged **24/0/8**. New doc `docs/specs/postflop-v4.2.2B-module3-postcommit-verification.md` documents per-claim verification + 3 defects + Training Volume gate + Path A migration decision.

- **Project owner notes for v4.2.2B:** The verification sprint was the right call — it caught a real "stale metadata" defect that would have propagated into v4.2.3 migration documentation. The summary block's `byActionReason` was missing `slowplay_call` entirely; if v4.2.3 had keyed off this stale summary for migration counts, the migration would have miscounted reasons. Conceptually, this sprint demonstrates: **don't trust your own previous-sprint reports against the raw data; always recompute.** F6.2 conceptTags fix is also a meaningful UX win — the runtime concept-library would have shown F6.2 under "check_raise_value" filter (wrong) instead of "bluff_catchers / pot_control" (right). The summary now carries a `metadataRecomputedAt` freshness marker so future sprints can see at-a-glance whether it's current.

- **Pending push (STAGED, MODULE 3 FINAL LOCK)**: `v4.2.2` — Module 3 Final Strategic Review + Planning Commit Lock. (NOTE: this entry is now historical; v4.2.2 was committed at `14bbd82`. Detail kept for reference.) **Planning-only sprint. No production data, no runtime wiring, no version bumps.** Second strategic pass over the 24 v4.2.1-reviewed M3 seeds. **Result: 22 FINAL_PASS + 2 FINAL_WARN + 0 BLOCKED.** All 24 seeds flipped from `reviewStatus: v4.2.0_seed_reviewed → v4.2.0_final`. **3 second-pass changes:** (1) F1.3 critical=[semi_bluff_raise] re-added (UN-soften — solver mix to raise is ~3-5%, near-zero, critical was justified); (2) F5.3 critical=[semi_bluff_raise] re-added (UN-soften — picking semi_bluff_raise as the reason for *calling* is genuinely confused; the raise reason here is blocker_raise); (3) F6.2 FLIPPED best=call (was check_raise_small) with reason=slowplay_call (re-introduced from pruned vocab). F6.2 flip teaches the paired-board exception: 5 of 6 board families had raise as best for nutted-value, creating an "always raise nuts OOP" anti-pattern; F6.2 now teaches "slowplay disguised nuts on paired boards because villain's c-bet range is air-heavy and raising folds out the bluffs." **Vocabulary expansion:** `slowplay_call` re-introduced to the M3 reason set (8 → 9 reasons). Updated `tools/audit-postflop-module3-seed.ps1` $validReasons; updated `postflop-v4.2.0-module3-architecture.md` §6, `postflop-v4.2.0-module3-schema-taxonomy.md` §5, and `postflop-v4.2.0-module3-audit-plan.md` rules M3-R19/M3-R20 to include slowplay_call. **6 follow-up notes from v4.2.1 resolved:** Note 1 (F5.4 RIO reason) REJECTED — 65o on monotone has no equity to be RIO against; Note 2 (F6.2 best-action) APPLIED NOW (see above); Notes 3+4+6 DEFERRED to v4.2.3/v4.2.4 migration; Note 5 (RIO reintroduction) REJECTED — duplicate of Note 1. **Re-evaluated 7 v4.2.1 refinements** with UN-soften lens: 5 held up, 2 un-softened (F1.3, F5.3 above). Other 5 (F2.3, F3.3, F5.2, F6.1, F6.4) confirmed correct. **Training Quality + Volume Principle assessment:** Accuracy strong (92% FINAL_PASS), but learning coverage BELOW depth target (6 of 7 native concepts <8 primary-tag matches; pot_odds_defense has zero seeds), question count BELOW playable-beta minimum (24 < 40-60), and expansion verdict is **C. major data sprint** required — suggested v4.3.x roadmap to bring M3 to 100+ scenarios. **Honest disclosure planning:** v4.2.4 must label M3 as "BETA · 24 scenarios" and avoid promising mastery at this depth. New doc `docs/specs/postflop-v4.2.2-module3-final-review.md` documents per-scenario sign-off + 6 follow-up resolutions + UN-soften reasoning + Training Quality assessment + v4.3.x expansion roadmap. **No production files touched.** Production audit unchanged **300/0/0**. M2 seed audit unchanged **24/0/8**. M3 seed audit (post-v4.2.2) **24/0/0 PASS clean**.

- **Project owner notes for v4.2.2:** Discipline win — UN-softened 2 of the 7 v4.2.1 refinements rather than blanket-accepting them. The seed auditor's M3-R31 `critical ⊆ bad` rule was a real find in v4.2.1; this sprint's UN-soften decisions show the author was willing to be self-critical. Vocabulary expansion (slowplay_call) was the right call for F6.2 because forcing the F6.2 lesson into bluff_catch or value_raise would have created semantic confusion. The Training Quality assessment is honest: 24 scenarios is a SEED set, not a stable module, and the v4.3.x expansion plan is concrete (path to 100+ scenarios over ~5-8 sprints).

- **Pending push (STAGED, MODULE 3 SEED AUDITOR + STRATEGIC REVIEW)**: `v4.2.1` — Module 3 Seed Auditor + Initial Strategic Review. **Planning-only sprint. No production data, no runtime wiring, no version bumps.** Implemented `tools/audit-postflop-module3-seed.ps1` (Option A: new script, ~440 LOC) instead of extending M2 auditor — M3's vocabulary differs in 4+ places (villainAction/villainSizing fields, 5-action decision set, 8-reason set, 2 new heroHandRole values). M3 seed auditor enforces all 38 hard rules + 7 soft warnings from `postflop-v4.2.0-module3-audit-plan.md`. **First run: 13 hard errors caught** — 13 scenarios had `answer.critical` values not in `answer.bad`, violating M3-R31 (`critical ⊆ bad`). The v4.2.0 author had treated `bad` and `critical` as disjoint sets; the schema rule requires critical to be a subset of bad. **Fix:** added each critical value to bad (13 batch updates). Re-audit clean. **Strategic review of 24/24 scenarios via the per-scenario prompts in `postflop-v4.2.0-module3-gpt-review-package.md`: 17 PASS / 7 WARN / 0 FAIL.** All 7 WARNs addressed in-place: F1.3 + F2.3 + F5.3 critical [`semi_bluff_raise`] removed (mixed strategy possible); F3.3 acceptable += equity_realization_call; F5.2 acceptable += call (slowplay nut flush); F6.1 actionReason equity_realization_call → bluff_catch (more specific); F6.4 acceptable += call (BDFD + overcards may have call frequency). All 24 scenarios flipped from `reviewStatus: v4.2.0_seed_candidate → v4.2.0_seed_reviewed`. Final M3 seed audit: **24 / 0 hard / 0 warnings = PASS clean**. New doc `docs/specs/postflop-v4.2.1-module3-seed-review.md` documents per-scenario verdict + applied fixes + 6 follow-up notes for v4.2.2 / v4.2.3. **No production files touched.** `postflop/postflop_scenarios.json`, `postflop/postflop_concepts.json`, `postflop/postflop_taxonomy.json`, `postflop/postflop_audit_rules.js`, `postflop/postflop_audit.html`, `tools/audit-postflop-ps.ps1`, `tools/audit-postflop-module2-seed.ps1`, `index.html`, `service-worker.js`, `manifest.json`, `ranges.json`, all preflop and gamification systems all untouched. Production audit unchanged **300/0/0**. M2 seed audit unchanged **24/0/8**.

- **Project owner notes for v4.2.1:** Mechanical defect was a real find — the v4.2.0 seeds had a partition-vs-flag confusion that the auditor caught immediately. Strategic refinements stayed disciplined: only downgraded `critical` flags where the v4.2.0 author had explicitly pre-flagged solver-mix concerns. Did not blanket-soften `critical` flags on the "stubborn call" folds (F1.4 JTo on dry A-high, F3.4 AKo on low connected, F5.4 65o on monotone) because the over-bluff leak there is real. Kept the auditor focused on schema-and-vocabulary validation rather than re-implementing strategic correctness checks (which is what the strategic review pass is for).

- **Pending push (STAGED, MODULE 3 PLANNING-ONLY)**: `v4.2.0` — Module 3 (BB Defense vs BTN C-bet OOP) Architecture + Seed Plan. (NOTE: this entry is now historical; v4.2.0 was committed at `515a3c1`. Detail kept for reference.) **Planning-only sprint. No production data, no runtime wiring, no version bumps.** Five new docs in `docs/specs/postflop-v4.2.0-module3-*`: (1) `architecture.md` — module purpose, curriculum position, spot assumptions (NLH MTT, 100BB, BTN open 2.5x, BB call, flop, BTN c-bets ~33% pot), player roles, 5-action decision set (fold / call / check_raise_small / check_raise_big / mixed), 8-reason set (trimmed from 11 candidates to those actually used by 24 seeds), relationship to M1/M2, runtime implications for v4.2.4, 8 known risks, open decisions; (2) `schema-taxonomy.md` — full JSON schema (mirrors M2 v4.1.2 with 3 additions: villainAction, villainSizing, optional defenseLogic), required fields, action+reason vocabularies, reused handClass/drawCategory/showdownValue from M2, 7 planned M3 concept tags (planned only — NOT added to postflop_concepts.json in v4.2.0); (3) `seed-scenarios.json` — exactly 24 planning-only scenarios across 6 board families (Dry A-high As 8d 3h, Dry K-high Kh 9c 4s, Low connected 8s 7d 5h, Two-tone broadway Qh Jh 6c, Monotone Jh 8h 4h, Paired Kc Kd 7s) × 4 hands each, 18 action_choice + 6 reason_choice, distribution: 6 calls + 5 check_raise_small + 1 check_raise_big + 6 folds + 6 reason_choice answers. All seeds use auditStatus=planning_only, reviewStatus=v4.2.0_seed_candidate, sourceConfidence=expert_judgment. JSON parses cleanly; counts validated; no card collisions; (4) `audit-plan.md` — 38 hard rules + 7 soft warnings across 12 categories (mechanical, collision, texture, spot, action, reason, vocabulary, answer-consistency, explanation, concept tags, sourceConfidence honesty, coverage), planned for v4.2.1 implementation as either `tools/audit-postflop-module3-seed.ps1` or extension of M2 seed auditor with `-Module 3` switch; production rules R29-R40 forward-planned for v4.2.3 migration; (5) `gpt-review-package.md` — per-scenario review entries (24) with 6-dimension review prompt template and risk flags (~12 PASS expected, ~10 WARN expected, 0 FAIL expected). **No production files touched.** No scenarios added to `postflop/postflop_scenarios.json`. No runtime helpers added to `index.html`. No service-worker bump. No appVersion bump. Production audit still **300/0/0**. M2 seed audit still **24/0/8**.

- **Project owner notes for v4.2.0:** One scope decision made and documented in architecture §3 — villain c-bet sizing for v4.2.0 seeds = `bet_small` only (~33% pot). Rationale: matches M2's most common c-bet size, foundational defense skill (~25% equity / MDF ~67%), `bet_big` defense reserved for v4.3.x. Reason set was trimmed from the brief's 11 candidates to 8 actually used by the 24 seeds (omitted: pot_odds_call, reverse_implied_odds_fold, slowplay_call) — explicitly reversible if v4.2.4 player data shows they are needed.

- **Service worker `VERSION`**: `'v4.2.6'` (bumped in v4.2.6 for cache invalidation after Postflop Beta Feedback Loop / M3 Runtime QA Dashboard).
- **App backup `appVersion`**: `'4.2.6'`.

- **Previous v4.1.6 detail (kept)**: Concept Library Module 2 Bridge (Path A preview-only).
- **Previous v4.1.7 detail (kept)**: Module 2 Curriculum Playable Beta.
- **Previous v4.1.8 detail (kept)**: Home Mode Tabs + Module 2 Mastery + Concept-Pool Depth Audit.
- **Previous v4.1.9 detail (kept)**: Module 2 Data Expansion (35 → 49) + Tester Pass. 14 new M2 scenarios. Per-concept primary-tag depth target hit (8/8/8/8/11). Production audit raised 286/0/0 → 300/0/0. Browser QA 30/30 PASS. appVersion + SW bumped to v4.1.9. Adds prominent **Home mode tabs** (Preflop/Postflop entry tiles) at the top of Home — Preflop tile calls `switchTab('drill')`, Postflop tile smooth-scrolls to the Postflop Beta Lab section (or routes to Settings if Postflop Beta is off). Adds **Module 2 mastery checklist** parallel to M1 (5 criteria evaluated against M2-only sessions: 5 sessions / 80%+ in 3 / no critical in latest / weak-review used / all 5 M2 concepts seen) inside the Postflop Academy panel below M1 mastery. Adds **Module 2 session summary aggregation** (`_pfM2RenderSessionAggregations`) that groups answers by `handClass` and `actionReason` to surface "weak hand classes", "weak action reasons", and an "action reason coverage" overview — injected between the existing learning summary and concept rows in `renderPostflopComplete` (M1 sessions get empty string, no regression). Documented **M2 concept-pool depth audit** showing primary-tag matches per concept (value_betting=5, pot_control=6, blocker_pressure=4, give_up_strategy=6, range_advantage_stab=5) with v4.1.9 expansion recommendation (+14 scenarios across 5 concepts). New CSS namespaced under `.home-mode-tabs*`, `.pf-mastery-section-m2`, `.pf-m2-aggr-section`. Mobile 1-column stack for tabs at ≤480px. Browser QA: 32/32 PASS (Home tabs render + click correctly, M2 mastery renders 5 items, M2 aggregation renders 3 blocks, M1/M2 normal+concept drills regression-clean, preflop unaffected, mobile 375px no overflow, console 0 errors). Production audit unchanged 286/0/0. M2 seed audit unchanged 24/0/8. **appVersion bumped 4.1.7 → 4.1.8, service-worker VERSION bumped v4.1.7 → v4.1.8.** No scenario data changes. New `docs/specs/postflop-v4.1.8-home-tabs-m2-mastery-depth.md` documents implementation, UX rationale, depth audit, v4.1.9 next step.

- **Service worker `VERSION`**: `'v4.1.8'`.
- **App backup `appVersion`**: `'4.1.8'`.

- **Previous v4.1.6 detail (kept)**: Concept Library Module 2 Bridge (Path A preview-only).
- **Previous v4.1.7 detail (kept)**: Module 2 Curriculum Playable Beta. Players can: (1) Start Module 2 from Curriculum Map (new "▶ Start Module 2 Beta" button calling `startPostflopDrill('pf_flop_cbet_ip', 12)`); (2) Drill any of the 5 M2 concepts from Concept Library (`previewOnly: true → false`); (3) Answer `action_choice` + `reason_choice` questions with hand-aware rendering (hero hand card row + handClass / heroHandRole chips, hand-aware question prompts); (4) See M2-specific feedback via new `_pfM2TeachingFeedbackBlocksHtml` (Recommended Action chip with action label + reason, Hand Logic / Sizing Logic / Action Logic / Takeaway / Common Mistake); (5) Use M2 weak-spot review (routed to M2 pool only, no M1 contamination); (6) Finish M2 sessions with M2-aware summary (`🎯 Module 2 · Flop C-bet IP · Complete` + `Module 2 Session Summary`). Final strategic seed review: 24/24 PASS (`postflop-v4.1.7-final-gpt-review-of-seeds.md`); 24 v4.1.2 seeds flipped from `auditStatus: review_pending → approved`; runtime now loads all 286 scenarios (was 262). New `getModule2Scenarios()` helper. `startPostflopDrill` routes pool by moduleId (M1=15-q default, M2=12-q default). `_pfChoiceGuide` extended for action_choice + reason_choice with all enum values. `_pfBuildQuestionPrompt` hand-aware for M2 qtypes. `renderPostflopQuestion` adds hero hand row + chips when scenario.module=='pf_flop_cbet_ip'; suppresses board-only checklist for M2. `renderPostflopAnswer` routes M2 to new teaching layer. `renderPostflopComplete` M2 module label + Drill again routes to same module. `startPostflopConceptDrill` routes pool by concept's module. `startPostflopWeakSpotReview` routes pool by previously-played module. `_PF_CURRICULUM` m2 entry: scenarioCount 11→35 + updated syllabus. New CSS (~110 lines): `.postflop-hero-card`, `.pf-m2-hand-chips`, `.pf-m2-handrole-chip`, `.pf-fb-m2-action`, `.pf-status-pill.is-beta`, `.pf-module-action-btn.is-secondary`. **Browser QA: 35/35 PASS** (M2 from curriculum + M2 concept drill + M2 weak-spot + M2 summary + M1 normal drill + M1 concept drill + preflop unaffected + mobile 375px clean + console 0 errors). Production audit unchanged 286/0/0. M2 seed audit unchanged 24/0/8. **appVersion 4.1.6 → 4.1.7, SW VERSION v4.1.6 → v4.1.7.** Two new docs: `postflop-v4.1.7-module2-playable-beta.md` (35-check QA + implementation detail), `postflop-v4.1.7-final-gpt-review-of-seeds.md` (24 PASS / 0 WARN / 0 FAIL strategic re-review).
- **Service worker `VERSION`**: `'v4.1.7'`.
- **App backup `appVersion`**: `'4.1.7'`.

- **Previous v4.1.6 detail (kept)**: Concept Library Module 2 Bridge. **Path A (preview-only) chosen.** Runtime UI patch only. `_PF_CONCEPT_LIBRARY` extended with 5 Module 2 concepts (`value_betting`, `pot_control`, `blocker_pressure`, `give_up_strategy`, `range_advantage_stab`) each carrying `module: 'm2'` + `previewOnly: true`. `_pfConceptLibraryHtml` rewritten to render Module 1 / Module 2 grouped sections with bottom-bordered headers. Module 2 cards display orange-tinted "trained in Module 2" tag + "🔒 Coming in Module 2 Beta" lock badge instead of drill button. Defense-in-depth: `startPostflopConceptDrill` now refuses any `previewOnly` concept key with a toast. New CSS namespaced under `.pf-concept-group-header*`, `.pf-concept-card-locked`, `.pf-concept-tag-m2`, `.pf-concept-locked-badge`. **Module 2 still NOT playable from curriculum** — `startPostflopDrill('pf_flop_cbet_ip', ...)` not called anywhere; M2 curriculum card stays "📖 Preview syllabus". 24 v4.1.2 seeds in production JSON remain `auditStatus: review_pending` (filtered out by runtime loader; 262 scenarios actually load = 251 M1 + 11 migrated baseline). Browser QA: 25/25 PASS — M1 concept drill works (queue=12, all M1), M2 concept drill refused, preflop drill works (queue=15), curriculum unchanged, mobile 375px clean (no overflow, locked badge 247px, M2 card 269px), console 0 errors. Production audit unchanged at 286/0/0. Module 2 seed audit unchanged at 24/0 hard errors / 8 warnings. **appVersion bumped 4.1.1 → 4.1.6, service-worker VERSION bumped v4.1.1 → v4.1.6.** No scenario data changes. New `docs/specs/postflop-v4.1.6-concept-library-module2-bridge.md` documents Path A choice, UI behavior, runtime limitations, v4.1.7 next step.

- **Service worker `VERSION`**: `'v4.1.6'`.
- **App backup `appVersion`**: `'4.1.6'`.

- **Previous v4.1.5 detail (kept)**: Production audit gate raised from 262/0/0 to 286/0/0. Six file changes: (1) `docs/specs/postflop-v4.1.2-module2-seed-scenarios.json` — 4 cleanup edits applied (3 `backdoor_only` relabels on #4/#8/#18 + #20 question prompt reworded for trap-value clarity); (2) `docs/specs/postflop-v4.1.2-module2-schema-taxonomy.md` — extended `underpair` definition with paired-board exception; documented optional `actionLogic` field for reason_choice; (3) `postflop/postflop_concepts.json` — added 5 new Module 2 concept entries: `value_betting`, `pot_control`, `blocker_pressure`, `give_up_strategy`, `range_advantage_stab` (category `module2`); (4) `postflop/postflop_scenarios.json` — 11 baseline scenarios migrated to v4.1.2 schema (renamed `bet_33→bet_small`, `bet_75→bet_big`, populated `heroHandRole`/`drawCategory`/`showdownValue`/`recommendedAction`/`actionReason`/`blockerNote`/`explanation.takeaway`) + 24 v4.1.2 seeds appended (with version bumped to 1.0.0). One real card-collision bug discovered + fixed (`_action_AhKc` → `_action_AsKc` because original baseline had `Ah` on board AND in heroHand). Plus 2 mechanical handClass corrections caught during migration: `_action_76s` (no_pair_no_draw → backdoor_only) and `_action_QJs` (gutshot → oesd). Total Module 2 production scenarios: **35** (11 migrated + 24 seeds). Distinct boards: 12 (no overlap). (5) `tools/audit-postflop-ps.ps1` — extended with R18-R28 enforcing v4.1.2 Module 2 rules on `module === 'pf_flop_cbet_ip'` scenarios (required fields, spot assumption, hero card collision, choice id sets, vocabulary, recommendedAction/actionReason consistency, explanation completeness, suit-count discipline, sourceConfidence honesty). Soft warnings deliberately excluded from production auditor (kept in seed auditor only). (6) `docs/specs/postflop-v4.1.5-baseline-migration-review.md` — NEW, documents 11 PASS / 0 WARN / 0 FAIL on migrated baseline + the 3 fixes. Plus appended **v4.1.5 Migration Addendum** in `postflop-v4.1.2-module2-gpt-review-package.md`. **Module 2 STILL NOT PLAYABLE** — runtime not wired. `index.html`, `service-worker.js`, `manifest.json`, `ranges.json`, all preflop systems, gamification systems all untouched. No appVersion bump. No service-worker VERSION bump. Module 2 seed audit reduced from 11 warnings to 8 after seed cleanup. Production audit: 286 / 0 / 0 (251 Module 1 + 35 Module 2).
- **(Pre-v4.1.8 reference, superseded above)** Pre-v4.1.6: SW `VERSION` was `'v4.1.1'`, `appVersion` was `'4.1.1'`. v4.1.6 bumped both to v4.1.6. v4.1.7 bumped to v4.1.7. v4.1.8 bumps to v4.1.8.

---

## 2. Current App Summary

The shipped app (`v3.8.2`) is a single-file PWA poker training tool focused on **MTT preflop** decisions. Capabilities:

- **Preflop drill engine** — quick / deep / weakness / challenge / overall_exam / marginal modes
- **Boss tests** + **Overall exams** + **Mission system** + **Challenges**
- **XP / Chips / Levels / Rank progression**
- **Wardrobe** cosmetic system (trainer character outfit)
- **Boss / Achievement / Rank / Source rewards** with reveal ceremonies
- **Collection Book** (long-term cosmetic progression with milestone rewards — v3.7.0)
- **Answer FX** + **Field FX** (per-pack themed atmosphere across drill flow — v3.8.0–v3.8.2)
- **Aura system** (cosmetic effect rendered around trainer character — v3.6.0)
- **SRS** (spaced repetition per hand) + **stats breakdown**
- **Settings** (FX intensity, reduced motion, exports/imports)

**Not yet built**:
- ❌ Post-flop training (in planning — see Active Epic).
- ❌ ICM-aware ranges.
- ❌ Multi-language UI.
- ❌ Cloud sync / accounts.

---

## 3. Current Active Epic

**v4.0.0 — Post-flop GTO Foundation Architecture**

A new training domain (sibling to preflop) for No Limit Hold'em MTT post-flop decisions. The first round is a **planning / data / audit package only** — no production integration in v4.0.0.

The epic is tracked in `TASK_BOARD.md`.

---

## 4. Current Execution Gate

**Round 1 (v4.0.0 planning) — what was authorized:**

1. Architecture proposal
2. Strict scenario schema
3. Board / suit / dynamic / advantage / sizing taxonomy
4. Concept taxonomy (with definitions + cross-refs)
5. 20–40 hand-authored sample scenarios
6. Audit script (17 rules) + browser audit viewer
7. Human-readable audit report
8. Risks & mitigations register

**Round 1 — what is NOT authorized:**

- ❌ Full drill engine integration in `index.html`
- ❌ Postflop boss/mission/challenge/reward integration
- ❌ Cosmetic rewards for postflop modules
- ❌ FX / Aura / Collection extensions for postflop
- ❌ Service worker version bump
- ❌ Modifying preflop ranges, scoring, SRS, or any existing reward logic

The gate stays closed until human review approves the planning package.

---

## 5. Latest Completed Work

### v4.1.0 Postflop Academy Foundation — STAGED

Evolves Postflop from a single quiz module into a structured learning academy. Per human direction: "Lay the foundation like a school. Make it robust and grow gradually."

**What was added**:
1. **Postflop Academy panel** replaces the simple Beta Lab entry card. Header with title + subtitle.
2. **Progress snapshot** — sessions completed, latest score %, latest quality pill (reuses `_pfSessionLearningLabel`), weak families from current drill state. Empty-state copy: "Start Module 1 to build your academy profile."
3. **Recommendation engine** (`_pfAcademyRecommendation`) — 6 rule-driven messages: no-history → start; latest critical → review weak; many bad → repeat Learn Mode; <5 sessions → build foundation; all 5 mastery met → Module 2 preview ready; recent strong but not all met → close to ready; default → continue.
4. **Curriculum map** (6 modules with status pills): Module 1 Active (Continue + optional Review Weak Spots buttons); Module 2 Preview (Preview syllabus toggles inline `<details>`); Module 3 Locked; Module 4-6 Future. Locked/Future cards dimmed (opacity 0.65/0.50) but still visible — "a school should show the path ahead."
5. **Module 1 mastery checklist** (5 display-only criteria): 5 sessions, 80%+ in 3 sessions, no critical leaks latest, weak-spot review engaged, foundational concepts covered. Each row shows met/not-met icon + detail string. No enforcement.
6. **Concept Library drawer** — 10 concepts (Range Advantage, Nut Advantage, Board Texture, Static vs Dynamic, C-bet Frequency, Sizing Family, Monotone, Paired, Low Connected, Two-tone). Each card has short definition + "trained in Module 1" tag.
7. **"Progress is saved locally on this device."** note pinned to the bottom. Honest copy — no implication of cloud sync.

**Helpers added** (10 + 2 declarative arrays, all `_pf*` namespaced):
`_PF_CURRICULUM`, `_PF_CONCEPT_LIBRARY`, `_pfAcademyStats`, `_pfModuleStatus`, `_pfMasteryProgress`, `_pfAcademyRecommendation`, `_pfMasteryProgressHtml`, `_pfAcademySnapshotHtml`, `_pfAcademyRecommendationHtml`, `_pfModuleCardHtml`, `_pfCurriculumMapHtml`, `_pfConceptLibraryHtml`, `_pfAcademyHomeHtml`.

All defensive against: missing localStorage, missing `App.postflop.scenarios`, malformed history JSON, missing concept tags. All output goes through `_pfEscape`.

**Live QA result** (25/25 checks pass):
- Helpers loaded; recommendation engine returns correct messages across no-history / critical / poor / mastery-met / strong-recent / default cases (6/6 paths verified)
- Beta off hides Academy; beta on shows full Academy (title, snapshot, recommendation, 6 module cards, mastery, 10 concepts, local-only note)
- Module 1 "Continue Board Texture" button starts the existing drill
- Mastery checklist updates correctly with no-history vs sample-strong-history
- Mobile 375px: academy 317px wide, no overflow, action buttons 273px tappable
- Console: 0 errors throughout
- Tab regression + preflop drill + boss UI: all working

**Files modified**: `index.html` (CSS block ~280 lines + 10 new helpers + 2 declarative arrays + edit in `renderPostflopHomeCardMount` to call new helper + appVersion 4.0.12 → 4.1.0), `service-worker.js` (VERSION v4.0.12 → v4.1.0), `PROJECT_STATE.md`, `TASK_BOARD.md`, `docs/specs/brief-v4.1.0-postflop-academy-foundation.md` (NEW).

**Untouched**: all postflop data files, audit infrastructure, generator scripts, ranges, manifest, preflop systems, scoring, cosmetics. service-worker diff is solely VERSION bump.

### v4.0.12 Postflop Drill Weak Spots Button — COMMITTED + PUSHED (`79cfc2a`)

After v4.0.11 added a "Recommended next move: replay weak spots first" line, the only available action was the existing "Drill again" button which built a fresh queue ignoring the player's mistakes. v4.0.12 closes the loop with an actual weak-spot replay flow.

**What was added**:
1. **"🎯 Drill Weak Spots" button** on the completion summary, only shown when the just-completed session has at least one bad or critical answer (`hardMisses > 0`). Amber-styled to differentiate from the standard "Drill again" button.
2. **"No weak spots detected this session."** italic note shown when no bad/critical answers — positive feedback rather than a disabled-button anti-pattern.
3. **"🎯 Review Mode · Weak Spots" badge** on the question screen during a weak-spot review session. Subcopy: "Focused on concepts and board families missed last session."
4. **Dynamic completion-screen header** when in weak-spot mode: "Board Texture Trainer · Review session complete" + "REVIEW SESSION SUMMARY".

**Helpers added** (4 pure functions, all `_pf*` namespaced):
- `_pfCurrentSessionWeakProfile(answers, scenarios)` → null OR `{mode, sourceSessionId, hardMisses, targetScenarioIds, targetConceptTags, targetFamilyKeys}`. Soft fallback when hardMisses < 2 includes acceptable answers as weak signals (weight 0.5).
- `_pfWeakScenarioScore(scenario, weakProfile, lastSessionIds)` → numeric score (+100 exact missed / +60 weak family / +40 weak concept / −30 recent repeat / +0..10 random).
- `_pfBuildWeakSpotQueue(weakProfile, allScenarios, targetLen=12)` → up to 12 scenarios, weak-prioritized then filled with general scenarios if pool too small. Always returns an array, no duplicates.
- `startPostflopWeakSpotReview()` — entry point wired to the button. Falls back to `startPostflopDrill('pf_board_texture')` when no weakness or pool empty.

**Defensive behavior**: returns null on perfect session; soft-fallback works when hardMisses=0 but acceptable answers exist; missing `conceptTags` / cleared localStorage / null `App.postflop.scenarios` all handled without crash; weak button hidden when no hard misses.

**Live QA result** (23/23 checks pass):
- Helpers loaded; weak profile derives correctly from POOR (10 hard misses → 9 target scenarios, 11 concept tags, 6 family keys), ONE-BAD (1 hard miss → 1 target + 11 fillers), ALL-ACCEPTABLE (soft fallback: 15 scenarios + 17 concepts as 0.5-weight signals)
- Queue properties verified: 12 unique scenarios; 100% family coverage on 10-bad session; 9/9 missed scenarios surfaced first
- Perfect session: weak button hidden + "No weak spots detected" note shown
- One-critical session: weak button visible
- Click weak button → `mode === 'weak_spots'`, badge appears, 12-question queue starts
- Answer → feedback → next still works; all 5 feedback blocks render in review mode
- Review summary header changes to "Review session complete" + "REVIEW SESSION SUMMARY"
- Mobile 375px: weak button 343×49px tappable; badge 343px wide, no overflow
- Console: 0 errors throughout
- Tab regression + preflop drill + boss UI + beta toggle: all working

**Files modified**: `index.html` (CSS block ~50 lines + 4 new helpers ~210 lines + Review Mode badge wire-in to `renderPostflopQuestion` + 4 edits to `renderPostflopComplete` for dynamic header/headline + weak button + empty note + appVersion 4.0.11 → 4.0.12), `service-worker.js` (VERSION v4.0.11 → v4.0.12), `PROJECT_STATE.md`, `TASK_BOARD.md`, `docs/specs/brief-v4.0.12-postflop-drill-weak-spots.md` (NEW).

**Untouched**: all postflop data files, audit infrastructure, generator scripts, ranges, manifest, preflop systems, scoring, cosmetics.

### v4.0.11 Postflop Session Learning Summary — COMMITTED + PUSHED (`a2e4fae`)

After a Module 1 session, the player should understand what they learned and what to focus on next. Prior summary screen showed score + tier counts + a flat concept-mastery list. v4.0.11 adds a learning-focused block stack between the score card and the existing details.

**New sections** (all derived from existing session data; no schema changes; no data file edits):

1. **Dynamic quality label** replaces the static "✅ Drill Complete" subtitle. Picks one of: "Clean read" / "Good pattern recognition" / "Mixed session" / "Needs review" / "High-risk leaks found" — colour-coded pill (green / blue / amber / red).
2. **Strongest concepts** green block — top 3 conceptTags by score % where seen ≥ 2 AND pct ≥ 80.
3. **Review signals** amber block — worst 3 conceptTags (lowest pct, then most bad+critical). Shows green-empty-state ("No major weak concept detected this session.") when clean.
4. **Board family pattern notes** red block — surfaces up to 3 families where misses cluster (missCount ≥ 2 OR critical ≥ 1). Each row shows the family label + miss count + one-sentence coaching lesson from an 18-entry family map (e.g., "low connected — missed 2 of 3. BB has more suited connectors and straight density.")
5. **Recommended next move** blue block — single coaching action: "Replay weak spots first..." (if critical), "Run another Learn Mode session..." (if 4+ bad/crit), "Focus on turning acceptable into best..." (if too many half-credit), "Good session..." (if 80%+ best), or "Keep going..." (default).

The existing concept-mastery details + critical-leaks details are preserved below (collapsed by default for power users).

**Helpers added** (8 pure functions, all `_pf*` namespaced):
`_pfBoardFamilyKey(board)`, `_pfBoardFamilyDisplayLabel(key)`, `_pfBoardFamilyLesson(key)`, `_pfLearnPrettyConcept(tag)`, `_pfSessionConceptSummary(answers)`, `_pfSessionBoardFamilySummary(answers, scenarios)`, `_pfSessionLearningLabel(counts, total)`, `_pfSessionNextMove(counts, total)`, `_pfRenderLearningSummary(...)`.

**Defensive fix**: legacy `conceptTally` line in `renderPostflopComplete` was crashing if an answer had no `conceptTags` (legacy localStorage). Added `(a && a.conceptTags) || []` guard. Per brief requirement #12.

**Live QA result** (20/20):
- Helpers loaded; perfect / poor / mixed session profiles all produce sensible labels + concepts + family clusters
- Mobile 375px: no horizontal overflow; summary card 343px wide
- Edge cases pass: missing conceptTags, cleared localStorage, empty answers, null `App.postflop.scenarios`
- Console: 0 errors throughout
- All 5 tabs render; preflop drill works; beta toggle hides/shows postflop

**Files modified**: `index.html` (CSS block + 8 new helper functions + 2 edits in `renderPostflopComplete` + appVersion 4.0.10 → 4.0.11), `service-worker.js` (VERSION v4.0.10 → v4.0.11), `PROJECT_STATE.md`, `TASK_BOARD.md`, `docs/specs/brief-v4.0.11-postflop-session-learning-summary.md` (NEW).

**Untouched**: all postflop data files, audit infrastructure, generator scripts, ranges, manifest, preflop systems, scoring, cosmetics.

### v4.0.10 Postflop Card Text Encoding Hotfix — COMMITTED + PUSHED (`53eae80`)

Tester reported that Postflop question text shows broken suit symbols (`Aโฅ Kโฆ 5โฃ`) while card graphics render correctly (`A♥ K♦ 5♣`). Root-cause investigation found **CP874 (Thai Windows) → UTF-8 mojibake** in the v4.0.0 baseline scenarios: original UTF-8 bytes were at some point read as CP874 then re-encoded as UTF-8, splitting each multi-byte char into 2-3 separate Thai/Latin codepoints. Affects `question.prompt` and `explanation.*` (rangeLogic / nutLogic / sizingLogic / commonMistake / short) on ~31 baseline scenarios. The 220 v4.0.7-generated scenarios are clean. `board.cards` is clean ASCII on all scenarios — that's why card graphics work.

**Fix at render time** (per instruction "do NOT change postflop data"):
1. `_pfCardText(card)` and `_pfBoardText(cards)` build clean board text from clean ASCII `board.cards` using existing `_pfSuitChar`.
2. `_pfBuildQuestionPrompt(scenario)` reconstructs the question sentence per `question.type` using `_pfBoardText`, ignoring the corrupted `question.prompt` field.
3. `_pfFixMojibake(text)` walks input char by char, mapping CP874-mojibake codepoints back to their original bytes (via reverse maps `_pfCp874ToByte` and `_pfThaiCpToByte`), then decoding accumulated bytes as UTF-8 via `TextDecoder('utf-8', {fatal:true})`. Falls back to original chars if a chunk fails to decode (so real Thai/accented text passes through unchanged).
4. `renderPostflopQuestion` now uses `_pfBuildQuestionPrompt(scenario)` instead of `scenario.question.prompt`.
5. `_pfTeachingFeedbackBlocksHtml` and `renderPostflopAnswer` wrap explanation text with `_pfFixMojibake` before `_pfEscape` (5 sites total).

**Live verification** (corrupted baseline `AhKd5c_rangeadv_001`):
- Raw data prompt: `On Aโฅ Kโฆ 5โฃ ...` (codepoints U+0E42 U+0099 U+0E05 etc.)
- Rendered prompt: `On A♥ K♦ 5♣ ...` (codepoints U+2665 U+2666 U+2663 — clean)
- Raw rangeLogic snippet: `KJo+ โ€" many cards`
- Rendered rangeLogic: `KJo+ — many cards` (em-dash U+2014)
- Card graphics still render correctly (verified `boardCardSuits = [♥, ♦, ♣]`)
- All 5 feedback blocks render
- Zero mojibake codepoints in any rendered text
- Console: 0 errors

**Audit**: 262/0/0 (data file unchanged).

**Files modified**:
- `index.html` (~80 lines new helpers + 6 render-site rewires + appVersion 4.0.9 → 4.0.10)
- `service-worker.js` (VERSION v4.0.9 → v4.0.10)
- `PROJECT_STATE.md`, `TASK_BOARD.md` (status update)
- `docs/specs/brief-v4.0.10-postflop-card-text-encoding-hotfix.md` (NEW)

**Untouched**: all postflop data files, audit infrastructure, generator scripts, ranges, manifest, preflop systems, scoring, cosmetics.

A future v4.1 cleanup pass could optionally rewrite the data file with clean UTF-8, making the render-time fix a no-op. For now the helper protects against the corruption permanently — even if the file is ever round-tripped through CP874 again.

### v4.0.9 Postflop Teaching Polish — COMMITTED + PUSHED (`c38aafc`)

Targeted polish of the v4.0.8 teaching layer based on the v4.0.8 Extended QA report (`docs/specs/postflop-v4.0.8-teaching-layer-qa-report.md`).

**Five fixes implemented**:

1. **Fix M1** (`_pfHintForBoard`): Q/J/T-high non-paired non-monotone disconnected scenarios were falling to a generic fallback hint. Added Q-high specific branch ("Ask: does BTN have more Q-x, K-x, and overpairs than BB has...") and J/T-high merged branch ("Ask: does BB have suited connectors and middle pocket pairs..."). Affects ~30 scenarios.

2. **Fix M3** (`_pfTakeawayForBoard`): Same Q/J/T-high boards had a generic takeaway. Added Q-high branch ("Q-high boards still favor BTN but the edge is smaller than A/K-high") and J/T-high branch ("J/T-high disconnected boards are closer to neutral; mixed small/check often beats range-betting"). Affects ~30-40 scenarios.

3. **Fix M4** (`_pfTakeawayForBoard`): `low_dry_two_tone` boards (e.g., 9h6h2c, 7h3h2s) were getting the rainbow takeaway "Low disconnected boards still favor BTN... small high-frequency c-bet works" — but the actual `sizing_family` answer for these boards is `mixed_small_check`. Added new branch BEFORE the rainbow rule: "Low disconnected two-tone boards can still retain BTN overpair advantage, but the flush draw makes pure range-small less automatic; mixed small/check is usually safer." Affects ~6 scenarios.

4. **Fix L1** (`_pfPatternLabel`): (a) Two-tone scenarios with `dynamicLevel >= 3` now return "X two-tone medium board" instead of misleading "X two-tone semi-dry". (b) Final fallback for rainbow non-paired non-monotone disconnected with `dyn >= 3` now returns "X semi-wet board" or "X semi-connected board" instead of the empty "X board".

5. **Fix M2 (optional, implemented)**: `_pfTeachingFeedbackBlocksHtml` rewritten to use new `_pfPickPrimaryLogic(qtype, explanation)` helper. Core Reason now shows ONLY the question-type-relevant logic strand (rangeLogic for range_advantage, nutLogic for nut_advantage, sizingLogic for frequency_strategy / sizing_family / dynamic_level). Other strands collapse into a "More logic strands" `<details>` block. Heavy multi-strand scenario went from ~200 words inline to ~80 words primary + collapsed remainder. Cuts mobile reading load roughly in half on heavy scenarios.

**Live verification results** (9 test cases):
- Q-high rainbow disconnected: now gets specific hint + specific takeaway ✓
- J-high rainbow dyn=3: pattern is "J-high semi-wet board" (was "J-high board") ✓
- T-high two-tone dyn=3: pattern is "T-high two-tone medium board" ✓
- Low two-tone disconnected: takeaway acknowledges flush draws + mixed sizing ✓
- frequency_strategy with all 3 strands: Core Reason starts "Sizing logic:" with "More logic strands" collapsed below ✓
- range_advantage scenarios: Core Reason starts "Range logic:" only ✓
- nut_advantage scenarios: Core Reason starts "Nut logic:" only ✓
- A-high dry, low connected two-tone, paired_mid: all unchanged regression ✓

**Files modified**:
- `index.html` (CSS untouched; JS edits in `_pfPatternLabel`, `_pfHintForBoard`, `_pfTakeawayForBoard`, `_pfTeachingFeedbackBlocksHtml`; new helper `_pfPickPrimaryLogic`; appVersion 4.0.8 → 4.0.9)
- `service-worker.js` (VERSION v4.0.8 → v4.0.9)
- `PROJECT_STATE.md`, `TASK_BOARD.md` (this section + status row)
- `docs/specs/brief-v4.0.9-postflop-teaching-polish.md` (planning + implementation log)
- `docs/specs/postflop-v4.0.8-teaching-layer-qa-report.md` (QA report from prior session, still untracked → staged with this commit)

**Untouched** (verified): `ranges.json`, `manifest.json`, all postflop data files, audit infrastructure, generator scripts, preflop systems, scoring, cosmetics.

**Audit**: 262 / 0 errors / 0 warnings.

### v4.0.8 Postflop Teaching Layer — COMMITTED + PUSHED (`479b775`)

Module 1 (Board Texture Trainer) UI patch. After v4.0.7 expanded the scenario pool to 251, human tester reported: *"I can play it now, but I do not understand the principles behind the answers."* The app was asking questions but not teaching the underlying board-reading framework.

**Five teaching components added** (all in `index.html`, additive in a fenced v4.0.8 CSS + JS block):

1. **Pattern Label** — short header above each board (e.g. "🎯 J-high two-tone semi-dry") with meta-line ("two_tone · disconnected · semi-static"). Derived from `board.highCardClass`, `board.suitTexture`, `board.textureTags`, `board.pairedStatus`, `board.dynamicLevel` via `_pfPatternLabel(board)`. Reads field values only — no schema changes.
2. **Board Reading Checklist** — collapsible 7-item educational framework (high card / texture / connectivity / suit / range adv / nut adv / sizing implication). Same for every scenario; teaches the reading PROCESS rather than the answer.
3. **Pre-answer Hint** — "💭 Need a hint?" button reveals a non-spoiler thinking-prompt based on board family (e.g. "Ask: does BB have BOTH straight density AND flush-draw density?"). Verified to never directly state the answer.
4. **5-block Feedback Layout** — `renderPostflopAnswer` restructured into Result / Board Pattern / Core Reason / 💡 Takeaway / ⚠️ Common Mistake. Replaces the previous separate `<details>` per logic strand — same content surfaced more readably without extra clicks.
5. **Takeaway Generator** — `_pfTakeawayForBoard(board)` produces a one-sentence generalizable lesson per board family (e.g. "Low connected two-tone boards are dangerous for BTN: BB has straight density AND flush-draw density combined.").

Plus a small "LEARN MODE · EXPLANATIONS ENABLED" mode tag pill. Full Test Mode (toggle to hide hints/explanations) deferred to v4.0.9 if needed.

**Helpers added** (8 pure functions, all `_pf*` namespaced):
`_pfPatternLabel`, `_pfBoardMetaLine`, `_pfHintForBoard`, `_pfTakeawayForBoard`, `_pfBoardChecklistHtml`, `_pfPatternLabelHtml`, `_pfHintRowHtml`, `_pfTeachingToggleHint`, `_pfTeachingFeedbackBlocksHtml`.

**Live QA result**:
- ✅ Postflop audit 262/0/0 (data unchanged)
- ✅ Module 1 loads 251 scenarios; all 8 helpers resolve to functions
- ✅ Pattern + hint + takeaway match correctly across 5 sample board families (A-high dry, low connected two-tone, low monotone, paired mid, broadway connected)
- ✅ Beta OFF: postflop screen hidden, no Beta Lab section
- ✅ Beta ON: question screen shows pattern label, checklist, hint button, mode tag
- ✅ Hint toggle open/close; verified no answer leak
- ✅ Choice click → 5-block feedback renders with correct content per scenario
- ✅ Mobile 375px: no horizontal overflow; all teaching elements render cleanly
- ✅ All 5 tabs render
- ✅ Console: 0 errors

**Files modified**: `index.html` (CSS + JS additive block + 2 render-fn modifications + appVersion 4.0.6→4.0.8), `service-worker.js` (VERSION v4.0.7→v4.0.8), `PROJECT_STATE.md`, `TASK_BOARD.md`, `docs/specs/brief-v4.0.8-postflop-teaching-layer.md` (NEW).

**Untouched**: `ranges.json`, `manifest.json`, all postflop data files, audit infrastructure, generator scripts, preflop systems, scoring, cosmetics.

### v4.0.7 Module 1 Scenario Expansion — COMMITTED + PUSHED (`1f5fe99`)

Third pass on the largest data sprint to date. Corrects GPT-flagged template overgeneralization in the generic `two_tone` family.

**Template-correction summary**:
- Split `two_tone` family into 5 sub-families based on rank-class + connectedness (`high_two_tone_dry`, `mid_two_tone_dry`, `broadway_two_tone_connected`, `low_dry_two_tone`, `low_connected_two_tone`)
- Each board re-classified into the right sub-family by `ClassifyTwoTone()` in the generator
- Fixed `paired_mid` wording: "set combos for the paired rank" → "trips combos with the paired rank" (technically correct since you can't have a "set" of an already-paired board card)
- All 9 GPT-named samples re-verified (5 fixed answers, 1 wording fix only, 3 kept with documented reasoning)
- Module 1 grew from 243 → **251 scenarios** as a side-effect of bumping plan to use more two-tone boards
- **Micro-fix pass (final)**: monotone_low nut wording corrected ("essentially zero nut combos" → acknowledges Axs nut-flush combos); paired_mid + monotone_low + similar solver-sensitive families now have `preflop_raiser`/`caller` opposite as `bad` not `critical` for nut_advantage; `neutral` added to acceptable for nut_advantage on those families
- See `docs/specs/postflop-v4.0.7-template-correction-report.md` for full reasoning

**Final canonical counts (template-correction + micro-fix)**:
- Module 1 scenarios: **251**
- Module 2 scenarios (unchanged): **11**
- Total postflop scenarios: **262**
- Audit: **0 errors / 0 warnings**
- sourceConfidence: 133 `consensus_gto` / 118 `expert_judgment` / 0 `solver_verified` / 0 `needs_review`
- suitTexture: 140 rainbow (55.8%) / 96 two_tone (38.2%) / 15 monotone (6%)
- difficulty: 30/100/43/55/23 across diff 1–5
- qtype: ra=58, na=57, fs=48, sf=39, dl=49

**Hardening pass summary** (second pass, intermediate — superseded by template-correction; numbers below are historical for the hardening pass only):

**Module 1 pool**: 20 → **243 scenarios** (+223 net). All 14 board family/suit combinations and all 5 question types covered.

**Hardening corrections** (vs initial v4.0.7 staging):

1. **sourceConfidence rebalanced.** Was 239 `consensus_gto` / 4 `expert_judgment` (98% overclaim). Now 97 `consensus_gto` (39.9%) / 146 `expert_judgment` (60.1%) / 0 `solver_verified` / 0 `needs_review` — all in target ranges. Per-family per-qtype confidence rules encoded in the generator: only universally-agreed reads (A/K-high dry rangeAdv, low-connected check-heavy, etc.) keep `consensus_gto`; everything else (sizing, monotone, two-tone, paired_mid, J/T_medium) honestly tagged `expert_judgment`.
2. **suitTexture rebalanced.** Was 200 rainbow (82%) / 25 two_tone (10%) / 18 monotone (7%) — too rainbow-heavy. Now 130 rainbow (53.5%) / 98 two_tone (40.3%) / 15 monotone (6.2%) — all in target ranges. ~75 new two-tone boards added across all families (genuine new boards, not trivial card-swap duplicates of rainbow). Rainbow plan trimmed to keep total Module 1 ~240.
3. **Generator tooling tracked.** Was `.gen-postflop.ps1` + `.audit-postflop.ps1` at repo root (gitignored). Now `tools/generate-postflop-module1.ps1` + `tools/audit-postflop-ps.ps1` (tracked, documented, deterministic, idempotent — re-runs strip + replace `*_v407` ids; baseline preserved).
4. **GPT review package expanded.** Was 20 samples, no risk breakdown. Now 30 samples (5 easy + 10 medium + 10 hard + 5 highest-risk) with coverage requirements (≥5 rainbow, ≥10 two-tone, ≥5 monotone, ≥5 paired, ≥5 very-wet/connected) and dedicated "Scenarios most likely to be disputed by strong players" section calling out 5 named risk categories.

**Audit result**: 0 errors / 0 warnings across all 254 total postflop scenarios. Zero board-card duplicates. Zero (board, qtype) duplicates.

**[HARDENING PASS HISTORICAL]** Distribution at end of hardening pass (Module 1 = 243; superseded by template-correction final = 251):
- qtype: ra=49, na=47, fs=53, sf=49, dl=45 (all 45-53)
- diff: 27/84/59/58/15 across 1-5
- suitTexture: rainbow 130 (53.5%) / two_tone 98 (40.3%) / monotone 15 (6.2%)
- sourceConfidence: consensus_gto 97 / expert_judgment 146 / solver_verified 0 / needs_review 0
- All `auditStatus="approved"`

**[HARDENING PASS HISTORICAL] Files modified at end of hardening pass** (final stage list above is canonical):
- `postflop/postflop_scenarios.json` (+223 scenarios at end of hardening; 31 → 254)
- `service-worker.js` (`v4.0.6` → `v4.0.7`)
- `tools/generate-postflop-module1.ps1` (NEW, tracked)
- `tools/audit-postflop-ps.ps1` (NEW, tracked)
- `docs/specs/postflop-v4.0.7-scenario-expansion-report.md` (NEW, hardened)
- `docs/specs/postflop-v4.0.7-gpt-review-package.md` (NEW, 30 samples)
- `PROJECT_STATE.md`, `TASK_BOARD.md` (this section + status row)

**Untouched** (verified): `index.html`, `ranges.json`, `manifest.json`, `postflop/postflop_audit_rules.js`, `postflop/postflop_audit.html`, `tools/audit-postflop.js`.

**Discovered work (tracked in TASK_BOARD)**:
- `polar_big_strategy` concept tag has only 1 scenario (concept naturally surfaces on turn/river → reserved for v4.1)
- Difficulty diff-5 only 15 scenarios (target 20) — bumping more would inflate honesty
- 5 highest-risk categories enumerated for GPT review attention

### v4.0.6 Postflop Repeat Control + Local Session History — STAGED

Tester reported postflop questions felt repetitive after ~3 sessions. Module 1 has 20 scenarios; sessions use 15. Pure Fisher-Yates random meant ~75% overlap between back-to-back sessions on average.

**Implementation** (all in `index.html`, additive in a new v4.0.6 fenced block + 4 in-place edits to v4.0.2 functions):

1. **Local history schema** in `localStorage.rmtt_postflop_history`:
   - `scenarios[scenarioId]` — attempts, per-tier counts, totalScore, lastTier, lastScore, lastSeenAt, lastSessionId
   - `concepts[conceptTag]` — same shape, aggregated per tag
   - `sessions[]` — capped to most recent 50 sessions (id, date, module, scenarioIds, score, tier counts)
   - Defensive load with shape recovery; defensive save with try/catch
2. **`recordPostflopAnswer` hook** — calls `_pfHistoryRecordAnswer(scenario, cls)` after the in-memory record. Best-effort; never throws.
3. **`renderPostflopComplete` hook** — calls `_pfHistoryRecordSession({module, queue, answers, totalScore, counts})` to save the compact session summary.
4. **History-aware `buildPostflopQueue`**: scores each candidate (`+100` if never seen, `+30` if not in last session, `+10` if attempts<3, `-50` if in last session, `-attempts*3`, `+random*20`); sorts and slices. Result: back-to-back overlap reduced to **theoretical minimum** (10/15 with pool=20, sessionLength=15).
5. **`App.postflopHistorySummary()`** — dev console helper returns scenarios tracked, concepts tracked, sessions count, top 5 most-seen, last session ids.
6. **"Progress is saved locally on this device."** — honest copy on the home card; no overclaim about sync/cloud/account.

**QA verified** (live local server):
- ✅ Audit 31/0/0
- ✅ Within-session: zero duplicates (`new Set(queue.ids).size === 15`)
- ✅ Back-to-back overlap: 10/15 (67%) = theoretical minimum, ~12% better than pure-random expected ~11.25
- ✅ History persists in localStorage
- ✅ History tracking: 15 scenarios + 17 concepts + 1 session after a single completed run
- ✅ Beta toggle off→hides UI, on→UI back
- ✅ All 5 tabs render
- ✅ Preflop drill 5 hands all classified correctly + preflop SRS storage grew (independent of postflop history)
- ✅ Postflop history + preflop progress in separate localStorage keys (no collision)
- ✅ Console clean

**Files modified**: `index.html` (+218 / -10), `service-worker.js` (VERSION bump only). Postflop data files + audit infrastructure all 0-diff.

### v4.0.5 GTO Data Validation Pass — committed (`87c741e`) + pushed

Walked all 20 Module 1 Board Texture Trainer scenarios with GTO scrutiny. Findings published in `docs/specs/postflop-v4.0.5-gto-validation-report.md` and `docs/specs/postflop-v4.0.5-data-patch-plan.md`.

**Verdict tally**:
- 17 KEEP (no change)
- 2 KEEP-with-caveat (#11 Th8h3h_nutadv monotone, #20 7d7s3c_rangeadv paired-low — both already at expert_judgment + difficulty 3-4; honest hedging acceptable)
- 1 DOWNGRADE (#14 Qd9c4h_rangeadv — proposed sourceConfidence: consensus_gto → expert_judgment)
- 0 REVISE answer keys
- 0 HOLD from production

**Net production-ready**: 20 / 20 scenarios remain shippable. The proposed edit is a metadata honesty tag with zero player-visible impact.

**Audit re-confirmed**: 31 scenarios · 0 errors · 0 warnings (no data files touched in this pass).

### v4.0.4 Critical Hotfix — committed (`519df53`) + pushed

Real-play feedback after v4.0.3: tester reported answer buttons unresponsive on BOTH desktop and mobile, AND Choice Guide invisible on mobile.

**Root cause** (introduced in v4.0.2, not caught in v4.0.2 or v4.0.3 QA): the inline `onclick="handlePostflopChoice(' + JSON.stringify(ch.id) + ')"` had a quote conflict — `JSON.stringify("range_small")` returns `"range_small"` with double quotes, embedded into double-quoted onclick attribute, breaking HTML parsing. The browser truncated `onclick` to `handlePostflopChoice(` (just the partial stub) and parsed the rest as garbage attributes. **Real clicks never fired the handler.** My v4.0.2 and v4.0.3 QA passed because I called `handlePostflopChoice(bestId)` directly via JS, bypassing the broken onclick path. The bug was invisible to my automated tests and only surfaced in human real-play.

**Fixes applied**:
1. **Delegated event listener** on `#postflopScreen` (one handler for all clicks; reads `dataset.choiceId` from nearest `.postflop-choice-btn` ancestor of click target). Survives re-renders since it's attached to the persistent screen element. Idempotent re-installation via `_pfInstallDelegatedClickListener()` called on boot AND on every `showPostflopScreen()`.
2. **Renamed handler** to `handlePostflopChoiceById(choiceId, btn)` — accepts optional button reference for instant pressed state. Backward-compat alias `handlePostflopChoice(choiceId)` retained.
3. **Choice Guide v2** — always-visible 1-line summary block (`.postflop-choice-guide-v2 .pf-guide-summary-line`) above choice buttons; expandable `<details>` for per-choice breakdown below. Replaces v4.0.3's collapsed-by-default `<details>` that mobile users didn't see.
4. **Touch reliability**: `touch-action: manipulation` (eliminates iOS 300ms tap delay), `position: relative; z-index: 1` on `.postflop-choices` and `.postflop-choice-btn` (prevents decorative canvas layers from intercepting taps), `-webkit-tap-highlight-color`, `user-select: none`.
5. **Reinforced FX suppression**: `body[data-postflop-active="true"]` now hides `.field-fx-canvas`, `.answer-fx-canvas`, `.aura-canvas` AND sets `pointer-events: none` on them.
6. **Fail-safe error fallback**: try/catch around classify+record+render. If render fails, `_pfShowAnswerError()` shows inline red banner ("Could not process answer. Tap again."), re-enables buttons, restores `phase = 'question'` so user can retry.

**Files modified**: `index.html` (+225 lines / -37, all inside existing v4.0.2/v4.0.3 fenced blocks), `service-worker.js` (VERSION bump).

**Audit re-confirmed**: 31/0/0 (no data files touched).

### v4.0.3 Implementation — committed (`25fb45e`) + pushed

Real-play hotfix per human tester feedback. Fixes 4 issues:

| # | Issue | Fix |
|---|---|---|
| 1 | Loading feels slow | Loader (`loadPostflopData`) now re-renders Home if user is on it AND beta is on (success and error paths). Card has 3 states: loading (spinner) / ready / error (with reload button). |
| 2 | Choice meanings unclear | New `_pfChoiceGuide(qType)` helper renders an expandable "What are we choosing?" panel above choice buttons. 5 question types × per-type explanations. |
| 3 | Buttons feel unresponsive | `handlePostflopChoice` now disables all buttons synchronously + adds `postflop-choice-pressed` class to tapped button BEFORE classify/render. Uses `requestAnimationFrame` so the pressed visual paints before the heavy innerHTML swap. New phase `'answering'` blocks rapid re-entry. |
| 4 | Home placement too low | `renderPostflopHomeCardMount` now uses `insertAdjacentHTML('afterbegin', ...)` to prepend at TOP of Home; wraps card in `.postflop-betalab-section` with "🧪 BETA LAB" header for clear beta status. |

**Files modified**: `index.html` (~250 lines added/modified across 6 surgical edits + 1 CSS block), `service-worker.js` (VERSION bump).

**Audit re-confirmed**: 31 scenarios · 0 errors · 0 warnings (no data files touched).

### v4.0.2 Postflop Module 1 (Board Texture Trainer) UI — committed (`5d21128`) + pushed

First visible postflop UI. Beta-gated via `App.state.settings.postflopBeta` (default `false`). Implementation per `docs/specs/brief-v4.0.2-implementation-ready.md`.

**Changes**:
- `index.html` (~960 lines added in single fenced v4.0.2 block + 2 one-liner appends to renderMastery/renderSettings + `appVersion` bump):
  - New `#postflopScreen` container (sibling to `#drillScreen` inside tab-drill panel)
  - CSS block for all `.postflop-*` classes (~290 lines)
  - JS block: `getPostflopReady`, `getModule1Scenarios`, `getConceptByKey`, `App.state.postflopDrill` state, `buildPostflopQueue`, `startPostflopDrill`, `classifyPostflopAnswer` (multi-tier), `recordPostflopAnswer`, `handlePostflopChoice`, `advancePostflopDrill`, `showPostflopScreen`, `exitPostflopScreen`, `confirmExitPostflop`, `renderPostflopHomeCardMount`, `renderPostflopBetaToggleMount`, `togglePostflopBeta`, `renderPostflopQuestion`, `renderPostflopAnswer`, `renderPostflopComplete`
  - Field FX suppression: `body[data-postflop-active="true"] .field-fx-canvas { display: none !important; }`
  - Wiring: 1-line defensive append in `renderMastery` wrapper (line 29279) + 1-line defensive append in `renderSettings` (line 31484)
- `service-worker.js`: VERSION `'v4.0.1'` → `'v4.0.2'`

**Live browser QA result** (29-item subset of 52-item matrix):
- ✅ Loader: ready=true, scenarios=31, schema=1.0.0, getModule1Count=20, all functions exist
- ✅ Beta default off: no postflop UI when off
- ✅ Toggle on: home card appears + Settings shows beta section
- ✅ Drill flow: question→feedback→advance→summary all render
- ✅ All 4 scoring tiers verified (best=1.0/best, acceptable=0.5/acceptable, critical=0/critical+flag, bad path also exercised)
- ✅ Multi-section feedback renders all 4 expandable sections + short explanation + concept tag pills
- ✅ Summary screen: score banner + per-tier counts + 17-row concept mastery + critical leaks list
- ✅ Preflop drill regression: 5 hands played, all classified correctly, progress key created, App.postflop untouched
- ✅ All 5 tabs render after postflop session
- ✅ Settings panel: existing FX/Aura/etc. controls intact + beta toggle appended
- ✅ Console clean: only the expected `[postflop] loaded 31/31 scenarios (schema 1.0.0)` from v4.0.1 loader; zero new errors/warnings
- ✅ Field FX suppression rule present in CSS (verified via stylesheet inspection)

**Implementation note**: One bug surfaced and was fixed in-flight — `#postflopScreen` lives inside `tab-drill` panel which gets hidden when other tabs become active. Fix: `showPostflopScreen()` now activates `tab-drill` panel + hides all OTHER drill sub-screens; `exitPostflopScreen()` returns to Home tab cleanly.

### v4.0.2-data Postflop Seed Fix — committed (`473ce9a`) + pushed

Pre-implementation data hygiene pass per `postflop-v4.0.2-scenario-review.md` findings + `brief-v4.0.2-implementation-ready.md` § 16. Three fix categories applied to `postflop/postflop_scenarios.json` only:

1. **Scenario #20** (`pf_btn_v_bb_srp_100bb_flop_7d7s3c_rangeadv_001`) — replaced the leftover authoring artifact `"Trips-7 even (both have 77 — wait, 77 impossible; ..."` in `nutLogic` with a clean GTO-facing explanation covering trips-7 distribution, impossible 77, full-house combinatorics, and overpair density.
2. **Choice label hint stripping** — removed all 14 rationale parentheticals from Module 1 answer-choice labels (e.g., `"Preflop raiser (BTN) — overpairs dominate"` → `"Preflop raiser (BTN)"`). Choices now have neutral labels; reasoning belongs in `explanation` fields.
3. **#10 `sourceConfidence` downgrade** — `Qh9d6s_freq_001` changed from `consensus_gto` → `expert_judgment` (the answer depends on solver-mix interpretation; confidence overclaim risk per scenario review B1/E3). #11 (`Th8h3h_nutadv_001`) was already `expert_judgment` — no change needed.

**Audit result**: 31 scenarios · 0 errors · 0 warnings. All 16 fixes applied; verified spot-checks confirm targets corrected.

**Files modified**: `postflop/postflop_scenarios.json` only. No other surface touched.

### v4.0.2 Planning Sprint — committed (`377c844`) + pushed

### v4.0.1 Postflop Schema Loader + Audit Gate — committed (`2593e5c`) + pushed

| Change | File | Diff |
|---|---|---|
| Loader block (POSTFLOP_SCHEMA_VERSION + App.postflop init + loadPostflopData + boot setTimeout) | `index.html` | +63 lines (one fenced v4.0.1 block) |
| `postflopBeta: false` in settings defaults (App.state) + confirmReset | `index.html` | +4 lines / -2 modified |
| `appVersion: '3.8.2'` → `'4.0.1'` | `index.html` | 1 string |
| `VERSION` bump + 3 postflop paths in STATIC_ASSETS | `service-worker.js` | +5 lines / -1 modified |

**Total**: 2 files modified; 71 insertions / 3 deletions in `index.html`; 6 insertions / 2 deletions in `service-worker.js`. All within the v4.0.1 brief scope.

**QA result**: audit re-confirmed clean (31/0/0); loader logic simulated successfully (`[postflop] loaded 31/31 scenarios (schema 1.0.0)`); production data files unchanged; postflop_audit_rules.js unchanged; preflop code paths untouched.

**Not yet done**: actual browser load + console verification of `App.postflop.ready === true` (requires a human or QA Agent with browser access).

### v4.0.0 Postflop Planning Package — committed (`7849741`) + pushed

| File | Purpose | Status |
|---|---|---|
| `postflop/ARCHITECTURE.md` | Full architecture proposal + module plan + integration map | ✅ Done |
| `postflop/postflop_schema.md` | Strict schema spec + scoring + UI plan | ✅ Done |
| `postflop/postflop_taxonomy.json` | Board / suit / dynamic / advantage / sizing enums | ✅ Done |
| `postflop/postflop_concepts.json` | 24 concepts with short + long defs + cross-refs | ✅ Done |
| `postflop/postflop_scenarios.json` | 31 audited seed scenarios (20 Module 1 + 11 Module 2) | ✅ Done |
| `postflop/postflop_audit_rules.js` | 17 audit rules as pure JS functions | ✅ Done |
| `postflop/postflop_audit.html` | Self-contained browser audit viewer | ✅ Done |
| `postflop/audit-report-sample.md` | Example audit output | ✅ Done |
| `postflop/RISKS.md` | 13 risks rated by severity + mitigations | ✅ Done |

**Audit result on the seed dataset**: `31 scenarios · 0 errors · 0 warnings · 31 approved` (after fixing 2 misuses of textureTags as conceptTags during the run).

### Recent shipped versions (latest 5)

- `v3.8.2` — Viewport-Dominant Field FX (canvas with 5 animated layers)
- `v3.8.1` — Anime Battle Field FX (intensity surge + page shake)
- `v3.8.0` — Field FX pivot + lifecycle bug fix
- `v3.7.4` — Aura Identity + Premium Hierarchy + 3 new auras
- `v3.7.3` — Anime FX + Premium Hierarchy

---

## 6. Hard Guardrails

The following surfaces require explicit per-task approval to modify. **Subagents cannot touch them on their own initiative.**

| Surface | File / Concept | Why locked |
|---|---|---|
| Preflop ranges | `ranges.json` | Source of truth for the entire preflop trainer; any change cascades through SRS and stats |
| Scoring formula | `classifyAnswer()` in `index.html` (line ~11219) | Touches every drill answer; regression risk to thousands of stored SRS entries |
| SRS state | `getSRSKey()` + `updateSRS()` in `index.html` (line ~12584) | Player-progress data; backward compat critical |
| Cooldowns | Boss-fail cooldowns in `index.html` (line ~28405) | Anti-grind rule that gates progression |
| Rank progression | Rank/Level XP curves | Player progression curve already tuned |
| Chips formula | Chip grant logic in `index.html` (~line 12267 area) | Economy balance |
| Existing reward grant | `_grantCosmeticByKey()` and surrounding hooks | Cosmetic ownership integrity |
| Cosmetic ownership | `App.state.profile.owned*` arrays | Player inventory; corruption is irreversible |
| Production UI shell | `index.html`, `service-worker.js` | Single-file PWA; one bad edit breaks the live deploy |
| Manifest / PWA | `manifest.json`, icon files | PWA install behavior |

**Rule**: any task touching the above must explicitly cite the surface in its scope and pass through Orchestrator before a subagent edits it.

---

## 7. File Ownership Rules

- Every subagent has an explicit **allowed file pattern** (declared in `AGENTS.md`).
- A subagent that needs to edit a file outside its pattern must **stop and request Orchestrator escalation**.
- **No subagent except DEV Integration Agent may edit `index.html` or `service-worker.js`**, and only when Orchestrator has assigned a controlled implementation task with explicit scope.
- Multiple subagents may edit different files in parallel as long as their patterns don't overlap.
- Orchestrator is the only role that may edit `PROJECT_STATE.md`, `AGENTS.md`, `TASK_BOARD.md`.

---

## 7.5. GPT AUDIT Folder Convention (PERMANENT — added 2026-05-06 v4.2.2F, expanded)

**Two parallel artifact families** under `GPT AUDIT/` (folder is git-ignored — review artifacts, not source):

### A. Versioned data + audit snapshot — `GPT AUDIT/v{VERSION}/`

**Rule:** Every committed version that ships a sprint should have a snapshot folder under `GPT AUDIT/v{VERSION}/` containing the canonical 10 source files + per-sprint docs + audit outputs.

**Canonical 10 files** (always copied, with directory structure preserved to mirror repo layout):

1. `PROJECT_STATE.md`
2. `TASK_BOARD.md`
3. `index.html`
4. `service-worker.js`
5. `postflop/postflop_scenarios.json`
6. `postflop/postflop_concepts.json`
7. `postflop/postflop_taxonomy.json`
8. `tools/audit-postflop-ps.ps1`
9. `tools/audit-postflop-module2-seed.ps1`
10. `tools/audit-postflop-module3-seed.ps1`

**Plus per-version:**
- `docs/specs/postflop-v{VERSION}-*.md` — the sprint doc(s) from the most recent sprint covered by this snapshot
- `AUDIT_OUTPUT_production.txt` — full output of `tools/audit-postflop-ps.ps1`
- `AUDIT_OUTPUT_M2_seed.txt` — full output of `tools/audit-postflop-module2-seed.ps1`
- `AUDIT_OUTPUT_M3_seed.txt` — full output of `tools/audit-postflop-module3-seed.ps1`
- `AUDIT_HEADLINES.txt` — top-line summary of the three audits

**Snapshot folder structure:**

```
GPT AUDIT/v{VERSION}/
├── PROJECT_STATE.md
├── TASK_BOARD.md
├── index.html
├── service-worker.js
├── postflop/
│   ├── postflop_scenarios.json
│   ├── postflop_concepts.json
│   └── postflop_taxonomy.json
├── tools/
│   ├── audit-postflop-ps.ps1
│   ├── audit-postflop-module2-seed.ps1
│   └── audit-postflop-module3-seed.ps1
├── docs/specs/
│   └── postflop-v{VERSION}-*.md
├── AUDIT_HEADLINES.txt
├── AUDIT_OUTPUT_production.txt
├── AUDIT_OUTPUT_M2_seed.txt
└── AUDIT_OUTPUT_M3_seed.txt
```

The refresh PowerShell script is in `GPT AUDIT/README.md` ("How to refresh this snapshot at a later version" section).

### B. UI screenshot per UI sprint — `GPT AUDIT/screenshots/v{VERSION}/`

**Rule:** Any sprint that touches UI/UX (CSS, layout, navigation, mode rendering, panels, tiles, modals, screens, etc.) **MUST** also save preview captures to `GPT AUDIT/screenshots/v{VERSION}/` before commit so an external GPT reviewer can see what shipped visually.

**Per-sprint subfolder structure:**

```
GPT AUDIT/screenshots/v{VERSION}/
├── README.md                      describe what was captured + how to review
├── 01-{name}-{viewport}.html      self-contained DOM+CSS snapshot
├── 02-{name}-{viewport}.html      ...one per significant view state
├── 03-state-snapshot.json         runtime state + relevant registry summary
└── (optional) {name}.png          PNG only if a non-technical reviewer needs it
```

**Naming rules:**
- Numeric prefix `01-`, `02-`, `03-` for predictable sort order
- `{name}` describes the view: e.g., `preflop-mode`, `postflop-mode`, `drill-question`, `summary-screen`
- `{viewport}` is mobile-first: `mobile-375`, `tablet-768`, `desktop-1280`

**HTML snapshots are the default** (over PNG) because:
- Claude Preview MCP returns inline screenshots but doesn't expose `save_to_disk` — HTML capture via `preview_eval` + base64-encoded `outerHTML` + inlined computed CSS is the reliable persistence path
- ~10KB per snapshot (vs 50–100KB PNG), diff-friendly across versions
- GPT can read structure semantically (button labels, classes, ARIA attributes), not just pixels
- Renders faithfully when opened in any browser

**When to skip the screenshots folder:**
- Pure data sprints (text fixes, audit tweaks, version bumps with no UI change)
- Internal helper additions that don't change visible UI
- Document the skip explicitly in the sprint doc to avoid confusion

### General rules

- **Both folder families are git-ignored** (entry in `.gitignore`) — never commit them.
- **Reference implementation:** `GPT AUDIT/v4.2.2F/` (data + audit snapshot) and `GPT AUDIT/screenshots/v4.2.2F/` (UI captures) show the canonical patterns.
- **Top-level convention rules** live in `GPT AUDIT/README.md` — keep that file in sync with this section.

---

## 8. Open Questions (carried forward, awaiting human input)

Tagged from `postflop/postflop_schema.md` "Open questions for review":

1. **Acceptable-score granularity** — locked to `{0.25, 0.5, 0.75}`, or allow any value in `[0, 1]`?
2. **Critical-flag UI** — flag-only in stats, or block progression / force review?
3. **ICM in v4.0** — confirm out-of-scope (chipEV-only foundation)?
4. **Hand-class enum location** — separate file, or stays inside `postflop_concepts.json`?
5. **`mixing` block format** — is `{ choiceId: freq }` enough, or richer `{ freq, ev }` per choice?

Plus, before commit:

6. **Spot-check of 3–5 sample scenarios** by a human reviewer.
7. **Approval to commit** the v4.0.0 planning package.

---

## 9. Next Recommended Step

1. ✅ Workflow files created (DEC-001, etc.).
2. ✅ v4.0.0 planning package reviewed + approved + committed (`7849741`) + pushed.
3. ✅ v4.0.1 implementation (schema loader + audit gate) staged per brief.
4. ⏸️ **Human review of staged v4.0.1 diff**, then approve the commit.
5. ⏸️ On approval: stage commit message `v4.0.1: add postflop schema loader and audit gate`; commit; await separate "push" instruction.
6. ⏸️ Resolve the 5 open questions above (or defer with explicit notes).
7. ⏸️ After v4.0.1 commit: prepare `docs/specs/brief-v4.0.2-module1-board-texture-trainer.md` (planning only — actual UI work).

---

**Maintained by**: Orchestrator Agent. Update on every state change. Do not delete entries — annotate with status.
